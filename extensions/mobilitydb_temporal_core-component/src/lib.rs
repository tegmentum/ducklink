//! Auto-generated by `datalink_shim_duckdb_dynlink_emit::emit_dynlink`
//! (Phase A, opaque-blob scalar dispatch). Do NOT edit by hand — regenerate.
#![allow(unused_imports, dead_code)]
#![allow(unsafe_op_in_unsafe_fn)]

use std::sync::atomic::AtomicU32;

mod bindings {
    wit_bindgen::generate!({
        path: "wit",
        world: "bridge",
        generate_all,
    });
}

use bindings::duckdb::extension::types::{
    Capabilitykind, Duckerror, Duckvalue, Funcflags, Invokeinfo, Logicaltype, Resultset,
};
use bindings::duckdb::extension::runtime;
use bindings::duckdb::extension::runtime_ext;
use bindings::duckdb::extension::catalog;
use bindings::exports::duckdb::extension::guest::{self as guest_export, Guest as GuestGuest, Loadresult};
use bindings::exports::duckdb::extension::callback_dispatch::{
    self as cb_export, Guest as CallbackGuest,
};

use bindings::compose::dynlink::linker;

const PROVIDER_ID: &str = "mobilitydb_core-composed";
const EXTENSION_ROOT: &str = "mobilitydb";
const CATALOG_EXTENSION: &str = "mobilitydb";
const CATALOG_VERSION: &str = "0.1.0";

fn resolve() -> Result<linker::Instance, Duckerror> {
    linker::resolve_by_id(&PROVIDER_ID.to_string())
        .map_err(|e| Duckerror::Internal(format!("dynlink resolve('{}'): {:?}", PROVIDER_ID, e)))
}

// -----------------------------------------------------------
// CBOR envelope (mirrors provider crate's Request/Response).
// -----------------------------------------------------------

#[derive(Debug, Clone)]
enum CborValue {
    Null,
    Bool(bool),
    Int(i64),
    Uint(u64),
    Float(f64),
    Text(String),
    Bytes(Vec<u8>),
    List(Vec<CborValue>),
}

impl serde::Serialize for CborValue {
    fn serialize<S: serde::Serializer>(&self, s: S) -> Result<S::Ok, S::Error> {
        match self {
            CborValue::Null => s.serialize_unit(),
            CborValue::Bool(b) => s.serialize_bool(*b),
            CborValue::Int(i) => s.serialize_i64(*i),
            CborValue::Uint(u) => s.serialize_u64(*u),
            CborValue::Float(f) => s.serialize_f64(*f),
            CborValue::Text(t) => s.serialize_str(t),
            CborValue::Bytes(b) => s.serialize_bytes(b),
            CborValue::List(items) => {
                use serde::ser::SerializeSeq;
                let mut seq = s.serialize_seq(Some(items.len()))?;
                for item in items {
                    seq.serialize_element(item)?;
                }
                seq.end()
            }
        }
    }
}

#[derive(Debug, Clone, serde::Serialize)]
struct Request {
    #[serde(rename = "v")]
    version: u32,
    args: Vec<CborValue>,
}

#[derive(Debug, Clone, serde::Deserialize)]
struct Response {
    #[serde(default)]
    ok: Option<ResponseValue>,
    #[serde(default)]
    err: Option<String>,
}

#[derive(Debug, Clone)]
enum ResponseValue {
    Null,
    Bool(bool),
    Int(i64),
    Uint(u64),
    Float(f64),
    Text(String),
    Bytes(Vec<u8>),
    List(Vec<ResponseValue>),
}

impl<'de> serde::Deserialize<'de> for ResponseValue {
    fn deserialize<D: serde::Deserializer<'de>>(d: D) -> Result<Self, D::Error> {
        use serde::de::{Error, MapAccess, SeqAccess, Visitor};
        struct V;
        impl<'de> Visitor<'de> for V {
            type Value = ResponseValue;
            fn expecting(&self, f: &mut core::fmt::Formatter) -> core::fmt::Result {
                f.write_str("a CBOR value")
            }
            fn visit_unit<E: Error>(self) -> Result<ResponseValue, E> { Ok(ResponseValue::Null) }
            fn visit_none<E: Error>(self) -> Result<ResponseValue, E> { Ok(ResponseValue::Null) }
            fn visit_bool<E: Error>(self, v: bool) -> Result<ResponseValue, E> { Ok(ResponseValue::Bool(v)) }
            fn visit_i64<E: Error>(self, v: i64) -> Result<ResponseValue, E> { Ok(ResponseValue::Int(v)) }
            fn visit_u64<E: Error>(self, v: u64) -> Result<ResponseValue, E> {
                if v <= i64::MAX as u64 { Ok(ResponseValue::Int(v as i64)) }
                else { Ok(ResponseValue::Uint(v)) }
            }
            fn visit_f64<E: Error>(self, v: f64) -> Result<ResponseValue, E> { Ok(ResponseValue::Float(v)) }
            fn visit_str<E: Error>(self, v: &str) -> Result<ResponseValue, E> { Ok(ResponseValue::Text(v.to_string())) }
            fn visit_string<E: Error>(self, v: String) -> Result<ResponseValue, E> { Ok(ResponseValue::Text(v)) }
            fn visit_bytes<E: Error>(self, v: &[u8]) -> Result<ResponseValue, E> { Ok(ResponseValue::Bytes(v.to_vec())) }
            fn visit_byte_buf<E: Error>(self, v: Vec<u8>) -> Result<ResponseValue, E> { Ok(ResponseValue::Bytes(v)) }
            fn visit_seq<A: SeqAccess<'de>>(self, mut s: A) -> Result<ResponseValue, A::Error> {
                let mut items = Vec::new();
                while let Some(v) = s.next_element::<ResponseValue>()? { items.push(v); }
                Ok(ResponseValue::List(items))
            }
            fn visit_map<A: MapAccess<'de>>(self, mut m: A) -> Result<ResponseValue, A::Error> {
                let k: Option<String> = m.next_key()?;
                let k = k.ok_or_else(|| A::Error::custom("empty map"))?;
                let v = match k.as_str() {
                    "Null" => { let _: serde::de::IgnoredAny = m.next_value()?; ResponseValue::Null }
                    "Bool" => ResponseValue::Bool(m.next_value()?),
                    "Int"  => ResponseValue::Int(m.next_value()?),
                    "Uint" => ResponseValue::Uint(m.next_value()?),
                    "Float" => ResponseValue::Float(m.next_value()?),
                    "Text" => ResponseValue::Text(m.next_value()?),
                    "Bytes" => {
                        let b: serde_bytes::ByteBuf = m.next_value()?;
                        ResponseValue::Bytes(b.into_vec())
                    }
                    "List" => ResponseValue::List(m.next_value()?),
                    other => return Err(A::Error::custom(format!("unknown tag: {}", other))),
                };
                Ok(v)
            }
        }
        d.deserialize_any(V)
    }
}

fn encode_request(args: Vec<CborValue>) -> Result<Vec<u8>, Duckerror> {
    let mut out = Vec::new();
    ciborium::into_writer(&Request { version: 1, args }, &mut out)
        .map_err(|e| Duckerror::Internal(format!("cbor encode: {}", e)))?;
    Ok(out)
}

fn decode_response(bytes: &[u8]) -> Result<Response, Duckerror> {
    ciborium::from_reader(bytes)
        .map_err(|e| Duckerror::Internal(format!("cbor decode: {}", e)))
}

fn call(method: &str, args: Vec<CborValue>) -> Result<ResponseValue, Duckerror> {
    let inst = resolve()?;
    let payload = encode_request(args)?;
    let bytes = inst
        .invoke(&method.to_string(), &payload)
        .map_err(|e| Duckerror::Internal(format!("{}: invoke: {:?}", method, e)))?;
    let resp = decode_response(&bytes)?;
    if let Some(err) = resp.err {
        return Err(Duckerror::Internal(format!("{}: {}", method, err)));
    }
    Ok(resp.ok.unwrap_or(ResponseValue::Null))
}

// -----------------------------------------------------------
// duckvalue / ResponseValue marshalling.
// -----------------------------------------------------------

fn duckv_to_cbor(v: &Duckvalue) -> CborValue {
    // Arm names mirror the WIT-bindgen output for the
    // `duckdb:extension@4.0.0/types/duckvalue` variant. See
    // ~/git/ducklink/wit/duckdb-extension/types.wit:97-120.
    match v {
        Duckvalue::Null => CborValue::Null,
        Duckvalue::Boolean(b) => CborValue::Bool(*b),
        Duckvalue::Int8(i) => CborValue::Int(*i as i64),
        Duckvalue::Int16(i) => CborValue::Int(*i as i64),
        Duckvalue::Int32(i) => CborValue::Int(*i as i64),
        Duckvalue::Int64(i) => CborValue::Int(*i),
        Duckvalue::Uint8(u) => CborValue::Int(*u as i64),
        Duckvalue::Uint16(u) => CborValue::Int(*u as i64),
        Duckvalue::Uint32(u) => CborValue::Int(*u as i64),
        Duckvalue::Uint64(u) => CborValue::Uint(*u),
        Duckvalue::Float32(f) => CborValue::Float(*f as f64),
        Duckvalue::Float64(f) => CborValue::Float(*f),
        Duckvalue::Text(s) => CborValue::Text(s.clone()),
        Duckvalue::Blob(b) => CborValue::Bytes(b.clone()),
        // Date / Time / Timestamp / Timestamptz ferry as int64
        // instants (canonical DuckDB storage).
        Duckvalue::Timestamp(i) => CborValue::Int(*i),
        Duckvalue::Timestamptz(i) => CborValue::Int(*i),
        Duckvalue::Date(i) => CborValue::Int(*i as i64),
        Duckvalue::Time(i) => CborValue::Int(*i),
        // Structured arms (Decimal / Interval / Uuid / Complex)
        // ferry as null at Phase A. The provider owns their
        // encoding when Phase B threads structured logical types.
        Duckvalue::Decimal(_)
        | Duckvalue::Interval(_)
        | Duckvalue::Uuid(_)
        | Duckvalue::Complex(_) => CborValue::Null,
    }
}

fn response_to_duckv(v: ResponseValue) -> Duckvalue {
    match v {
        ResponseValue::Null => Duckvalue::Null,
        ResponseValue::Bool(b) => Duckvalue::Boolean(b),
        ResponseValue::Int(i) => Duckvalue::Int64(i),
        ResponseValue::Uint(u) => Duckvalue::Uint64(u),
        ResponseValue::Float(f) => Duckvalue::Float64(f),
        ResponseValue::Text(t) => Duckvalue::Text(t),
        ResponseValue::Bytes(b) => Duckvalue::Blob(b),
        ResponseValue::List(_) => Duckvalue::Null,
    }
}

// -----------------------------------------------------------
// Columnar hot-path helpers (call_scalar_batch_col).
//
// Simplified vs. the fully-typed postgis-ducklink-bridge
// reference: since dynlink Phase A dispatch is opaque-blob,
// we always lift/lower through Duckvalue::Blob columns. That
// keeps the per-row hot path scalar-Blob-only; multi-shape
// dispatch is a follow-up when the catalog carries logical
// types.
// -----------------------------------------------------------

use bindings::duckdb::extension::column_types;

fn cv_is_valid(validity: &[u8], i: usize) -> bool {
    if validity.is_empty() { return true; }
    let byte = i / 8;
    let bit = i % 8;
    (validity.get(byte).copied().unwrap_or(0) >> bit) & 1 == 1
}

fn colvec_get(cv: &column_types::Colvec, i: usize) -> Duckvalue {
    if !cv_is_valid(cv.validity.as_slice(), i) {
        return Duckvalue::Null;
    }
    match &cv.data {
        column_types::Column::Boolean(xs) => xs.get(i).copied().map(Duckvalue::Boolean).unwrap_or(Duckvalue::Null),
        column_types::Column::Int64(xs) => xs.get(i).copied().map(Duckvalue::Int64).unwrap_or(Duckvalue::Null),
        column_types::Column::Uint64(xs) => xs.get(i).copied().map(Duckvalue::Uint64).unwrap_or(Duckvalue::Null),
        column_types::Column::Float64(xs) => xs.get(i).copied().map(Duckvalue::Float64).unwrap_or(Duckvalue::Null),
        column_types::Column::Int32(xs) => xs.get(i).copied().map(Duckvalue::Int32).unwrap_or(Duckvalue::Null),
        column_types::Column::Int8(xs) => xs.get(i).copied().map(Duckvalue::Int8).unwrap_or(Duckvalue::Null),
        column_types::Column::Int16(xs) => xs.get(i).copied().map(Duckvalue::Int16).unwrap_or(Duckvalue::Null),
        column_types::Column::Uint8(xs) => xs.get(i).copied().map(Duckvalue::Uint8).unwrap_or(Duckvalue::Null),
        column_types::Column::Uint16(xs) => xs.get(i).copied().map(Duckvalue::Uint16).unwrap_or(Duckvalue::Null),
        column_types::Column::Uint32(xs) => xs.get(i).copied().map(Duckvalue::Uint32).unwrap_or(Duckvalue::Null),
        column_types::Column::Float32(xs) => xs.get(i).copied().map(Duckvalue::Float32).unwrap_or(Duckvalue::Null),
        column_types::Column::Timestamp(xs) => xs.get(i).copied().map(Duckvalue::Timestamp).unwrap_or(Duckvalue::Null),
        column_types::Column::Date(xs) => xs.get(i).copied().map(Duckvalue::Date).unwrap_or(Duckvalue::Null),
        column_types::Column::Time(xs) => xs.get(i).copied().map(Duckvalue::Time).unwrap_or(Duckvalue::Null),
        column_types::Column::Timestamptz(xs) => xs.get(i).copied().map(Duckvalue::Timestamptz).unwrap_or(Duckvalue::Null),
        column_types::Column::Text(xs) => xs.get(i).cloned().map(Duckvalue::Text).unwrap_or(Duckvalue::Null),
        column_types::Column::Blob(xs) => xs.get(i).cloned().map(Duckvalue::Blob).unwrap_or(Duckvalue::Null),
        // Decimal / Interval / Uuid / Complex column arms are
        // rendered as Null in the Phase A opaque-blob path.
        _ => Duckvalue::Null,
    }
}

fn validate_colvec_rows(args: &[column_types::Colvec]) -> Result<usize, Duckerror> {
    let n_rows = if args.is_empty() { 0 } else { args[0].rows as usize };
    for (j, cv) in args.iter().enumerate() {
        if cv.rows as usize != n_rows {
            return Err(Duckerror::Internal(format!(
                "columnar dispatch: arg-column {} has rows={} but expected {}",
                j, cv.rows, n_rows
            )));
        }
    }
    Ok(n_rows)
}

fn materialize_row(args: &[column_types::Colvec], i: usize, out: &mut Vec<Duckvalue>) {
    out.clear();
    if out.capacity() < args.len() {
        out.reserve(args.len() - out.capacity());
    }
    for cv in args {
        out.push(colvec_get(cv, i));
    }
}

/// Lower a `Vec<Duckvalue>` (per-row scalar returns) into a
/// typed `Colvec`. Uses a TWO-PASS approach so that NULL rows
/// preceding the first non-NULL row still land in the eventual
/// arm's buffer as zero placeholders (single-pass "pick on first
/// non-NULL" left the pre-arm nulls in `blobs`, giving a
/// buffer.len() < rows off-by-one when the picked arm was not
/// Blob — every subsequent lookup was misaligned).
///
/// Pass 1 walks `&values` to determine the arm (and rejects
/// heterogeneous batches / unsupported Decimal/Interval/Uuid/
/// Complex arms). Pass 2 consumes `values` and pushes real
/// values or zero placeholders into that arm's buffer so the
/// buffer index matches the row index; the validity bitmap
/// carries the actual NULL rows. A colvec of all-NULLs is
/// lowered as Blob (chosen arbitrarily: no data buffer is ever
/// addressed since the validity bitmap zeros every row).
fn values_to_colvec(values: Vec<Duckvalue>) -> Result<column_types::Colvec, Duckerror> {
    let n = values.len();
    let rows = n as u32;
    let mut bits: Vec<u8> = vec![0u8; (n + 7) / 8];
    let mut any_null = false;

    // Discriminator: which arm we picked, populated on first
    // non-NULL row. Repeated per-arm buffers avoid an enum tag
    // in the hot path and let the compiler prove exhaustiveness.
    #[derive(Clone, Copy, PartialEq, Eq, Debug)]
    enum Arm {
        Unknown,
        Boolean,
        Int8, Int16, Int32, Int64,
        Uint8, Uint16, Uint32, Uint64,
        Float32, Float64,
        Text, Blob,
        Date, Time, Timestamp, Timestamptz,
    }

    fn arm_of(v: &Duckvalue) -> Option<Arm> {
        Some(match v {
            Duckvalue::Boolean(_) => Arm::Boolean,
            Duckvalue::Int8(_) => Arm::Int8,
            Duckvalue::Int16(_) => Arm::Int16,
            Duckvalue::Int32(_) => Arm::Int32,
            Duckvalue::Int64(_) => Arm::Int64,
            Duckvalue::Uint8(_) => Arm::Uint8,
            Duckvalue::Uint16(_) => Arm::Uint16,
            Duckvalue::Uint32(_) => Arm::Uint32,
            Duckvalue::Uint64(_) => Arm::Uint64,
            Duckvalue::Float32(_) => Arm::Float32,
            Duckvalue::Float64(_) => Arm::Float64,
            Duckvalue::Text(_) => Arm::Text,
            Duckvalue::Blob(_) => Arm::Blob,
            Duckvalue::Date(_) => Arm::Date,
            Duckvalue::Time(_) => Arm::Time,
            Duckvalue::Timestamp(_) => Arm::Timestamp,
            Duckvalue::Timestamptz(_) => Arm::Timestamptz,
            Duckvalue::Null
            | Duckvalue::Decimal(_)
            | Duckvalue::Interval(_)
            | Duckvalue::Uuid(_)
            | Duckvalue::Complex(_) => return None,
        })
    }

    fn mismatch(expected: &Arm, actual: &Duckvalue) -> Duckerror {
        Duckerror::Internal(format!(
            "values_to_colvec: column arm mismatch (expected={:?}, saw variant with tag {:?}); \
             heterogeneous rows in a single columnar batch are unsupported",
            expected, core::mem::discriminant(actual),
        ))
    }

    // ---- Pass 1: pick the arm across every non-NULL row. ----
    let mut arm = Arm::Unknown;
    for (i, v) in values.iter().enumerate() {
        if matches!(v, Duckvalue::Null) {
            any_null = true;
            continue;
        }
        // Refuse unsupported (Decimal/Interval/Uuid/Complex)
        // returns explicitly — the old code silently NULLed them.
        let this_arm = arm_of(v).ok_or_else(|| Duckerror::Internal(format!(
            "values_to_colvec: unsupported column arm for row {} (Decimal/Interval/Uuid/Complex \
             lowering is not yet implemented in the dynlink emit)",
            i,
        )))?;
        if arm == Arm::Unknown {
            arm = this_arm;
        } else if arm != this_arm {
            return Err(mismatch(&arm, v));
        }
    }

    // ---- Pass 2: allocate the chosen arm's buffer at exact
    // length and materialize (real values for non-NULL rows,
    // zero placeholders for NULL rows).
    let mut booleans: Vec<bool> = Vec::new();
    let mut int8s: Vec<i8> = Vec::new();
    let mut int16s: Vec<i16> = Vec::new();
    let mut int32s: Vec<i32> = Vec::new();
    let mut int64s: Vec<i64> = Vec::new();
    let mut uint8s: Vec<u8> = Vec::new();
    let mut uint16s: Vec<u16> = Vec::new();
    let mut uint32s: Vec<u32> = Vec::new();
    let mut uint64s: Vec<u64> = Vec::new();
    let mut float32s: Vec<f32> = Vec::new();
    let mut float64s: Vec<f64> = Vec::new();
    let mut texts: Vec<String> = Vec::new();
    let mut blobs: Vec<Vec<u8>> = Vec::new();
    let mut dates: Vec<i32> = Vec::new();
    let mut times: Vec<i64> = Vec::new();
    let mut timestamps: Vec<i64> = Vec::new();
    let mut timestamptzs: Vec<i64> = Vec::new();

    for (i, v) in values.into_iter().enumerate() {
        if matches!(v, Duckvalue::Null) {
            // Placeholder so the buffer index tracks the row
            // index; the validity bit stays 0.
            match arm {
                Arm::Unknown | Arm::Blob => blobs.push(Vec::new()),
                Arm::Boolean => booleans.push(false),
                Arm::Int8 => int8s.push(0),
                Arm::Int16 => int16s.push(0),
                Arm::Int32 => int32s.push(0),
                Arm::Int64 => int64s.push(0),
                Arm::Uint8 => uint8s.push(0),
                Arm::Uint16 => uint16s.push(0),
                Arm::Uint32 => uint32s.push(0),
                Arm::Uint64 => uint64s.push(0),
                Arm::Float32 => float32s.push(0.0),
                Arm::Float64 => float64s.push(0.0),
                Arm::Text => texts.push(String::new()),
                Arm::Date => dates.push(0),
                Arm::Time => times.push(0),
                Arm::Timestamp => timestamps.push(0),
                Arm::Timestamptz => timestamptzs.push(0),
            }
            continue;
        }
        bits[i / 8] |= 1u8 << (i % 8);
        match v {
            Duckvalue::Boolean(b) => booleans.push(b),
            Duckvalue::Int8(x) => int8s.push(x),
            Duckvalue::Int16(x) => int16s.push(x),
            Duckvalue::Int32(x) => int32s.push(x),
            Duckvalue::Int64(x) => int64s.push(x),
            Duckvalue::Uint8(x) => uint8s.push(x),
            Duckvalue::Uint16(x) => uint16s.push(x),
            Duckvalue::Uint32(x) => uint32s.push(x),
            Duckvalue::Uint64(x) => uint64s.push(x),
            Duckvalue::Float32(x) => float32s.push(x),
            Duckvalue::Float64(x) => float64s.push(x),
            Duckvalue::Text(s) => texts.push(s),
            Duckvalue::Blob(b) => blobs.push(b),
            Duckvalue::Date(x) => dates.push(x),
            Duckvalue::Time(x) => times.push(x),
            Duckvalue::Timestamp(x) => timestamps.push(x),
            Duckvalue::Timestamptz(x) => timestamptzs.push(x),
            // Null / unsupported: already handled above (or
            // rejected in pass 1).
            _ => unreachable!("arm_of admitted a variant we didn't push"),
        }
    }
    let validity = if any_null { bits } else { Vec::new() };
    let data = match arm {
        Arm::Unknown | Arm::Blob => column_types::Column::Blob(blobs),
        Arm::Boolean => column_types::Column::Boolean(booleans),
        Arm::Int8 => column_types::Column::Int8(int8s),
        Arm::Int16 => column_types::Column::Int16(int16s),
        Arm::Int32 => column_types::Column::Int32(int32s),
        Arm::Int64 => column_types::Column::Int64(int64s),
        Arm::Uint8 => column_types::Column::Uint8(uint8s),
        Arm::Uint16 => column_types::Column::Uint16(uint16s),
        Arm::Uint32 => column_types::Column::Uint32(uint32s),
        Arm::Uint64 => column_types::Column::Uint64(uint64s),
        Arm::Float32 => column_types::Column::Float32(float32s),
        Arm::Float64 => column_types::Column::Float64(float64s),
        Arm::Text => column_types::Column::Text(texts),
        Arm::Date => column_types::Column::Date(dates),
        Arm::Time => column_types::Column::Time(times),
        Arm::Timestamp => column_types::Column::Timestamp(timestamps),
        Arm::Timestamptz => column_types::Column::Timestamptz(timestamptzs),
    };
    Ok(column_types::Colvec { rows, validity, data })
}

fn scalar_name_by_arm_idx(arm: usize) -> Option<&'static str> {
    match arm as u32 {
        0 => Some("bitemporal_bool_after_ts"),
        1 => Some("bitemporal_bool_as_of"),
        2 => Some("bitemporal_bool_before_ts"),
        3 => Some("bitemporal_bool_cmp"),
        4 => Some("bitemporal_bool_count_current"),
        5 => Some("bitemporal_bool_count_superseded"),
        6 => Some("bitemporal_bool_current"),
        7 => Some("bitemporal_bool_end_value"),
        8 => Some("bitemporal_bool_eq"),
        9 => Some("bitemporal_bool_ever_valid_during_count"),
        10 => Some("bitemporal_bool_from_ewkt"),
        11 => Some("bitemporal_bool_ge"),
        12 => Some("bitemporal_bool_gt"),
        13 => Some("bitemporal_bool_history_at_count"),
        14 => Some("bitemporal_bool_instant_new"),
        15 => Some("bitemporal_bool_le"),
        16 => Some("bitemporal_bool_len"),
        17 => Some("bitemporal_bool_lt"),
        18 => Some("bitemporal_bool_ne"),
        19 => Some("bitemporal_bool_over_after_ts"),
        20 => Some("bitemporal_bool_over_before_ts"),
        21 => Some("bitemporal_bool_sequence_memsize"),
        22 => Some("bitemporal_bool_sequence_new"),
        23 => Some("bitemporal_bool_start_value"),
        24 => Some("bitemporal_bool_to_ewkt"),
        25 => Some("bitemporal_bool_transaction_extent"),
        26 => Some("bitemporal_bool_valid_at"),
        27 => Some("bitemporal_bool_valid_during_count"),
        28 => Some("bitemporal_bool_valid_extent"),
        29 => Some("bitemporal_bool_value_n"),
        30 => Some("bitemporal_float_after_ts"),
        31 => Some("bitemporal_float_all_values"),
        32 => Some("bitemporal_float_as_of"),
        33 => Some("bitemporal_float_before_ts"),
        34 => Some("bitemporal_float_cmp"),
        35 => Some("bitemporal_float_count_current"),
        36 => Some("bitemporal_float_count_superseded"),
        37 => Some("bitemporal_float_current"),
        38 => Some("bitemporal_float_current_values"),
        39 => Some("bitemporal_float_end_value"),
        40 => Some("bitemporal_float_eq"),
        41 => Some("bitemporal_float_ever_valid_during_count"),
        42 => Some("bitemporal_float_from_ewkt"),
        43 => Some("bitemporal_float_ge"),
        44 => Some("bitemporal_float_gt"),
        45 => Some("bitemporal_float_history_at_count"),
        46 => Some("bitemporal_float_instant_new"),
        47 => Some("bitemporal_float_instant_round"),
        48 => Some("bitemporal_float_is_current"),
        49 => Some("bitemporal_float_is_valid_at"),
        50 => Some("bitemporal_float_le"),
        51 => Some("bitemporal_float_len"),
        52 => Some("bitemporal_float_lt"),
        53 => Some("bitemporal_float_ne"),
        54 => Some("bitemporal_float_over_after_ts"),
        55 => Some("bitemporal_float_over_before_ts"),
        56 => Some("bitemporal_float_sequence_memsize"),
        57 => Some("bitemporal_float_sequence_new"),
        58 => Some("bitemporal_float_sequence_round"),
        59 => Some("bitemporal_float_start_value"),
        60 => Some("bitemporal_float_to_ewkt"),
        61 => Some("bitemporal_float_transaction_duration"),
        62 => Some("bitemporal_float_transaction_extent"),
        63 => Some("bitemporal_float_valid_at"),
        64 => Some("bitemporal_float_valid_duration"),
        65 => Some("bitemporal_float_valid_during_count"),
        66 => Some("bitemporal_float_valid_extent"),
        67 => Some("bitemporal_float_value_n"),
        68 => Some("bitemporal_float_was_known_at"),
        69 => Some("bitemporal_int_after_ts"),
        70 => Some("bitemporal_int_all_values"),
        71 => Some("bitemporal_int_as_of"),
        72 => Some("bitemporal_int_before_ts"),
        73 => Some("bitemporal_int_cmp"),
        74 => Some("bitemporal_int_count_current"),
        75 => Some("bitemporal_int_count_superseded"),
        76 => Some("bitemporal_int_current"),
        77 => Some("bitemporal_int_current_values"),
        78 => Some("bitemporal_int_end_value"),
        79 => Some("bitemporal_int_eq"),
        80 => Some("bitemporal_int_ever_valid_during_count"),
        81 => Some("bitemporal_int_from_ewkt"),
        82 => Some("bitemporal_int_ge"),
        83 => Some("bitemporal_int_gt"),
        84 => Some("bitemporal_int_history_at_count"),
        85 => Some("bitemporal_int_instant_new"),
        86 => Some("bitemporal_int_is_current"),
        87 => Some("bitemporal_int_is_valid_at"),
        88 => Some("bitemporal_int_le"),
        89 => Some("bitemporal_int_len"),
        90 => Some("bitemporal_int_lt"),
        91 => Some("bitemporal_int_ne"),
        92 => Some("bitemporal_int_over_after_ts"),
        93 => Some("bitemporal_int_over_before_ts"),
        94 => Some("bitemporal_int_sequence_memsize"),
        95 => Some("bitemporal_int_sequence_new"),
        96 => Some("bitemporal_int_start_value"),
        97 => Some("bitemporal_int_to_ewkt"),
        98 => Some("bitemporal_int_transaction_duration"),
        99 => Some("bitemporal_int_transaction_extent"),
        100 => Some("bitemporal_int_valid_at"),
        101 => Some("bitemporal_int_valid_duration"),
        102 => Some("bitemporal_int_valid_during_count"),
        103 => Some("bitemporal_int_valid_extent"),
        104 => Some("bitemporal_int_value_n"),
        105 => Some("bitemporal_int_was_known_at"),
        106 => Some("bitemporal_text_after_ts"),
        107 => Some("bitemporal_text_as_of"),
        108 => Some("bitemporal_text_before_ts"),
        109 => Some("bitemporal_text_cmp"),
        110 => Some("bitemporal_text_count_current"),
        111 => Some("bitemporal_text_count_superseded"),
        112 => Some("bitemporal_text_current"),
        113 => Some("bitemporal_text_end_value"),
        114 => Some("bitemporal_text_eq"),
        115 => Some("bitemporal_text_ever_valid_during_count"),
        116 => Some("bitemporal_text_from_ewkt"),
        117 => Some("bitemporal_text_ge"),
        118 => Some("bitemporal_text_gt"),
        119 => Some("bitemporal_text_history_at_count"),
        120 => Some("bitemporal_text_instant_new"),
        121 => Some("bitemporal_text_le"),
        122 => Some("bitemporal_text_len"),
        123 => Some("bitemporal_text_lt"),
        124 => Some("bitemporal_text_ne"),
        125 => Some("bitemporal_text_over_after_ts"),
        126 => Some("bitemporal_text_over_before_ts"),
        127 => Some("bitemporal_text_sequence_memsize"),
        128 => Some("bitemporal_text_sequence_new"),
        129 => Some("bitemporal_text_start_value"),
        130 => Some("bitemporal_text_to_ewkt"),
        131 => Some("bitemporal_text_transaction_extent"),
        132 => Some("bitemporal_text_valid_at"),
        133 => Some("bitemporal_text_valid_during_count"),
        134 => Some("bitemporal_text_valid_extent"),
        135 => Some("bitemporal_text_value_n"),
        136 => Some("date_set_cmp"),
        137 => Some("date_set_contains"),
        138 => Some("date_set_end_value"),
        139 => Some("date_set_eq"),
        140 => Some("date_set_from_text"),
        141 => Some("date_set_ge"),
        142 => Some("date_set_gt"),
        143 => Some("date_set_intersection"),
        144 => Some("date_set_le"),
        145 => Some("date_set_len"),
        146 => Some("date_set_lt"),
        147 => Some("date_set_make"),
        148 => Some("date_set_memsize"),
        149 => Some("date_set_minus"),
        150 => Some("date_set_ne"),
        151 => Some("date_set_start_value"),
        152 => Some("date_set_to_text"),
        153 => Some("date_set_union"),
        154 => Some("date_set_value_n"),
        155 => Some("date_span_adjacent"),
        156 => Some("date_span_after"),
        157 => Some("date_span_after_value"),
        158 => Some("date_span_before"),
        159 => Some("date_span_before_value"),
        160 => Some("date_span_bins"),
        161 => Some("date_span_cmp"),
        162 => Some("date_span_contains"),
        163 => Some("date_span_contains_span"),
        164 => Some("date_span_eq"),
        165 => Some("date_span_expand"),
        166 => Some("date_span_extent"),
        167 => Some("date_span_from_text"),
        168 => Some("date_span_ge"),
        169 => Some("date_span_gt"),
        170 => Some("date_span_intersection"),
        171 => Some("date_span_le"),
        172 => Some("date_span_lt"),
        173 => Some("date_span_make"),
        174 => Some("date_span_memsize"),
        175 => Some("date_span_ne"),
        176 => Some("date_span_over_after"),
        177 => Some("date_span_over_after_value"),
        178 => Some("date_span_over_before"),
        179 => Some("date_span_over_before_value"),
        180 => Some("date_span_overlaps"),
        181 => Some("date_span_shift"),
        182 => Some("date_span_to_text"),
        183 => Some("date_span_union"),
        184 => Some("date_span_width"),
        185 => Some("date_spanset_after"),
        186 => Some("date_spanset_after_value"),
        187 => Some("date_spanset_before"),
        188 => Some("date_spanset_before_value"),
        189 => Some("date_spanset_bins"),
        190 => Some("date_spanset_cmp"),
        191 => Some("date_spanset_contains"),
        192 => Some("date_spanset_end_span"),
        193 => Some("date_spanset_eq"),
        194 => Some("date_spanset_extent"),
        195 => Some("date_spanset_from_text"),
        196 => Some("date_spanset_ge"),
        197 => Some("date_spanset_gt"),
        198 => Some("date_spanset_intersection"),
        199 => Some("date_spanset_le"),
        200 => Some("date_spanset_lt"),
        201 => Some("date_spanset_memsize"),
        202 => Some("date_spanset_minus"),
        203 => Some("date_spanset_ne"),
        204 => Some("date_spanset_num_spans"),
        205 => Some("date_spanset_over_after"),
        206 => Some("date_spanset_over_after_value"),
        207 => Some("date_spanset_over_before"),
        208 => Some("date_spanset_over_before_value"),
        209 => Some("date_spanset_overlaps"),
        210 => Some("date_spanset_span_n"),
        211 => Some("date_spanset_start_span"),
        212 => Some("date_spanset_to_text"),
        213 => Some("date_spanset_union"),
        214 => Some("date_spanset_width"),
        215 => Some("dateset_contains"),
        216 => Some("dateset_gaps"),
        217 => Some("dateset_intersection"),
        218 => Some("dateset_len"),
        219 => Some("dateset_make"),
        220 => Some("dateset_minus"),
        221 => Some("dateset_nth"),
        222 => Some("dateset_shift"),
        223 => Some("dateset_successor"),
        224 => Some("dateset_to_span"),
        225 => Some("dateset_to_text"),
        226 => Some("dateset_union"),
        227 => Some("datespan_adjacent"),
        228 => Some("datespan_contains"),
        229 => Some("datespan_contains_span"),
        230 => Some("datespan_expand"),
        231 => Some("datespan_from_text"),
        232 => Some("datespan_intersection"),
        233 => Some("datespan_lower"),
        234 => Some("datespan_make"),
        235 => Some("datespan_overlaps"),
        236 => Some("datespan_shift"),
        237 => Some("datespan_to_text"),
        238 => Some("datespan_union"),
        239 => Some("datespan_upper"),
        240 => Some("datespan_width"),
        241 => Some("datespanset_contains"),
        242 => Some("datespanset_from_text"),
        243 => Some("datespanset_intersection"),
        244 => Some("datespanset_lower"),
        245 => Some("datespanset_make"),
        246 => Some("datespanset_minus"),
        247 => Some("datespanset_num_spans"),
        248 => Some("datespanset_overlaps"),
        249 => Some("datespanset_to_text"),
        250 => Some("datespanset_union"),
        251 => Some("datespanset_upper"),
        252 => Some("datespanset_width"),
        253 => Some("float_round"),
        254 => Some("float_set_cmp"),
        255 => Some("float_set_contains"),
        256 => Some("float_set_end_value"),
        257 => Some("float_set_eq"),
        258 => Some("float_set_from_text"),
        259 => Some("float_set_ge"),
        260 => Some("float_set_gt"),
        261 => Some("float_set_intersection"),
        262 => Some("float_set_le"),
        263 => Some("float_set_len"),
        264 => Some("float_set_lt"),
        265 => Some("float_set_make"),
        266 => Some("float_set_memsize"),
        267 => Some("float_set_minus"),
        268 => Some("float_set_ne"),
        269 => Some("float_set_round"),
        270 => Some("float_set_start_value"),
        271 => Some("float_set_to_text"),
        272 => Some("float_set_union"),
        273 => Some("float_set_value_n"),
        274 => Some("float_span_adjacent"),
        275 => Some("float_span_after"),
        276 => Some("float_span_after_value"),
        277 => Some("float_span_before"),
        278 => Some("float_span_before_value"),
        279 => Some("float_span_bins"),
        280 => Some("float_span_cmp"),
        281 => Some("float_span_contains"),
        282 => Some("float_span_contains_span"),
        283 => Some("float_span_eq"),
        284 => Some("float_span_expand"),
        285 => Some("float_span_extent"),
        286 => Some("float_span_from_text"),
        287 => Some("float_span_ge"),
        288 => Some("float_span_gt"),
        289 => Some("float_span_intersection"),
        290 => Some("float_span_le"),
        291 => Some("float_span_lt"),
        292 => Some("float_span_make"),
        293 => Some("float_span_memsize"),
        294 => Some("float_span_ne"),
        295 => Some("float_span_over_after"),
        296 => Some("float_span_over_after_value"),
        297 => Some("float_span_over_before"),
        298 => Some("float_span_over_before_value"),
        299 => Some("float_span_overlaps"),
        300 => Some("float_span_round"),
        301 => Some("float_span_shift"),
        302 => Some("float_span_to_text"),
        303 => Some("float_span_union"),
        304 => Some("float_span_width"),
        305 => Some("float_spanset_after"),
        306 => Some("float_spanset_after_value"),
        307 => Some("float_spanset_before"),
        308 => Some("float_spanset_before_value"),
        309 => Some("float_spanset_bins"),
        310 => Some("float_spanset_cmp"),
        311 => Some("float_spanset_contains"),
        312 => Some("float_spanset_end_span"),
        313 => Some("float_spanset_eq"),
        314 => Some("float_spanset_extent"),
        315 => Some("float_spanset_from_text"),
        316 => Some("float_spanset_ge"),
        317 => Some("float_spanset_gt"),
        318 => Some("float_spanset_intersection"),
        319 => Some("float_spanset_le"),
        320 => Some("float_spanset_lt"),
        321 => Some("float_spanset_memsize"),
        322 => Some("float_spanset_minus"),
        323 => Some("float_spanset_ne"),
        324 => Some("float_spanset_num_spans"),
        325 => Some("float_spanset_over_after"),
        326 => Some("float_spanset_over_after_value"),
        327 => Some("float_spanset_over_before"),
        328 => Some("float_spanset_over_before_value"),
        329 => Some("float_spanset_overlaps"),
        330 => Some("float_spanset_round"),
        331 => Some("float_spanset_span_n"),
        332 => Some("float_spanset_start_span"),
        333 => Some("float_spanset_to_text"),
        334 => Some("float_spanset_union"),
        335 => Some("float_spanset_width"),
        336 => Some("floatset_contains"),
        337 => Some("floatset_gaps"),
        338 => Some("floatset_intersection"),
        339 => Some("floatset_is_disjoint"),
        340 => Some("floatset_is_subset"),
        341 => Some("floatset_len"),
        342 => Some("floatset_make"),
        343 => Some("floatset_minus"),
        344 => Some("floatset_nth"),
        345 => Some("floatset_scale"),
        346 => Some("floatset_shift"),
        347 => Some("floatset_to_span"),
        348 => Some("floatset_to_text"),
        349 => Some("floatset_union"),
        350 => Some("floatspan_adjacent"),
        351 => Some("floatspan_bucket_list"),
        352 => Some("floatspan_contains"),
        353 => Some("floatspan_contains_span"),
        354 => Some("floatspan_expand"),
        355 => Some("floatspan_from_text"),
        356 => Some("floatspan_intersection"),
        357 => Some("floatspan_lower"),
        358 => Some("floatspan_make"),
        359 => Some("floatspan_midpoint"),
        360 => Some("floatspan_minus"),
        361 => Some("floatspan_overlaps"),
        362 => Some("floatspan_scale"),
        363 => Some("floatspan_shift"),
        364 => Some("floatspan_to_text"),
        365 => Some("floatspan_union"),
        366 => Some("floatspan_upper"),
        367 => Some("floatspan_width"),
        368 => Some("floatspanset_contains"),
        369 => Some("floatspanset_from_text"),
        370 => Some("floatspanset_intersection"),
        371 => Some("floatspanset_minus"),
        372 => Some("floatspanset_num_spans"),
        373 => Some("floatspanset_overlaps"),
        374 => Some("floatspanset_to_text"),
        375 => Some("floatspanset_union"),
        376 => Some("floatspanset_width"),
        377 => Some("int_set_cmp"),
        378 => Some("int_set_contains"),
        379 => Some("int_set_end_value"),
        380 => Some("int_set_eq"),
        381 => Some("int_set_from_text"),
        382 => Some("int_set_ge"),
        383 => Some("int_set_gt"),
        384 => Some("int_set_intersection"),
        385 => Some("int_set_le"),
        386 => Some("int_set_len"),
        387 => Some("int_set_lt"),
        388 => Some("int_set_make"),
        389 => Some("int_set_memsize"),
        390 => Some("int_set_minus"),
        391 => Some("int_set_ne"),
        392 => Some("int_set_start_value"),
        393 => Some("int_set_to_text"),
        394 => Some("int_set_union"),
        395 => Some("int_set_value_n"),
        396 => Some("int_span_adjacent"),
        397 => Some("int_span_after"),
        398 => Some("int_span_after_value"),
        399 => Some("int_span_before"),
        400 => Some("int_span_before_value"),
        401 => Some("int_span_bins"),
        402 => Some("int_span_cmp"),
        403 => Some("int_span_contains"),
        404 => Some("int_span_contains_span"),
        405 => Some("int_span_eq"),
        406 => Some("int_span_expand"),
        407 => Some("int_span_extent"),
        408 => Some("int_span_from_text"),
        409 => Some("int_span_ge"),
        410 => Some("int_span_gt"),
        411 => Some("int_span_intersection"),
        412 => Some("int_span_le"),
        413 => Some("int_span_lt"),
        414 => Some("int_span_make"),
        415 => Some("int_span_memsize"),
        416 => Some("int_span_ne"),
        417 => Some("int_span_over_after"),
        418 => Some("int_span_over_after_value"),
        419 => Some("int_span_over_before"),
        420 => Some("int_span_over_before_value"),
        421 => Some("int_span_overlaps"),
        422 => Some("int_span_shift"),
        423 => Some("int_span_to_text"),
        424 => Some("int_span_union"),
        425 => Some("int_span_width"),
        426 => Some("int_spanset_after"),
        427 => Some("int_spanset_after_value"),
        428 => Some("int_spanset_before"),
        429 => Some("int_spanset_before_value"),
        430 => Some("int_spanset_bins"),
        431 => Some("int_spanset_cmp"),
        432 => Some("int_spanset_contains"),
        433 => Some("int_spanset_end_span"),
        434 => Some("int_spanset_eq"),
        435 => Some("int_spanset_extent"),
        436 => Some("int_spanset_from_text"),
        437 => Some("int_spanset_ge"),
        438 => Some("int_spanset_gt"),
        439 => Some("int_spanset_intersection"),
        440 => Some("int_spanset_le"),
        441 => Some("int_spanset_lt"),
        442 => Some("int_spanset_memsize"),
        443 => Some("int_spanset_minus"),
        444 => Some("int_spanset_ne"),
        445 => Some("int_spanset_num_spans"),
        446 => Some("int_spanset_over_after"),
        447 => Some("int_spanset_over_after_value"),
        448 => Some("int_spanset_over_before"),
        449 => Some("int_spanset_over_before_value"),
        450 => Some("int_spanset_overlaps"),
        451 => Some("int_spanset_span_n"),
        452 => Some("int_spanset_start_span"),
        453 => Some("int_spanset_to_text"),
        454 => Some("int_spanset_union"),
        455 => Some("int_spanset_width"),
        456 => Some("intset_contains"),
        457 => Some("intset_gaps"),
        458 => Some("intset_intersection"),
        459 => Some("intset_is_disjoint"),
        460 => Some("intset_is_subset"),
        461 => Some("intset_len"),
        462 => Some("intset_make"),
        463 => Some("intset_minus"),
        464 => Some("intset_nth"),
        465 => Some("intset_scale"),
        466 => Some("intset_shift"),
        467 => Some("intset_to_span"),
        468 => Some("intset_to_text"),
        469 => Some("intset_union"),
        470 => Some("intspan_adjacent"),
        471 => Some("intspan_bucket_list"),
        472 => Some("intspan_contains"),
        473 => Some("intspan_contains_span"),
        474 => Some("intspan_expand"),
        475 => Some("intspan_from_text"),
        476 => Some("intspan_intersection"),
        477 => Some("intspan_lower"),
        478 => Some("intspan_make"),
        479 => Some("intspan_minus"),
        480 => Some("intspan_overlaps"),
        481 => Some("intspan_scale"),
        482 => Some("intspan_shift"),
        483 => Some("intspan_to_text"),
        484 => Some("intspan_union"),
        485 => Some("intspan_upper"),
        486 => Some("intspan_width"),
        487 => Some("intspanset_contains"),
        488 => Some("intspanset_from_text"),
        489 => Some("intspanset_intersection"),
        490 => Some("intspanset_minus"),
        491 => Some("intspanset_num_spans"),
        492 => Some("intspanset_overlaps"),
        493 => Some("intspanset_to_text"),
        494 => Some("intspanset_union"),
        495 => Some("intspanset_width"),
        496 => Some("jsonb_array_append"),
        497 => Some("jsonb_array_elements"),
        498 => Some("jsonb_array_length"),
        499 => Some("jsonb_array_prepend"),
        500 => Some("jsonb_canonicalize"),
        501 => Some("jsonb_concat"),
        502 => Some("jsonb_contained_in"),
        503 => Some("jsonb_contains"),
        504 => Some("jsonb_contains_all_keys"),
        505 => Some("jsonb_contains_any_key"),
        506 => Some("jsonb_contains_key"),
        507 => Some("jsonb_delete_key"),
        508 => Some("jsonb_delete_path"),
        509 => Some("jsonb_equal"),
        510 => Some("jsonb_from_string"),
        511 => Some("jsonb_get_element"),
        512 => Some("jsonb_get_key"),
        513 => Some("jsonb_get_path"),
        514 => Some("jsonb_is_array"),
        515 => Some("jsonb_is_null"),
        516 => Some("jsonb_is_object"),
        517 => Some("jsonb_is_scalar"),
        518 => Some("jsonb_merge_deep"),
        519 => Some("jsonb_object_keys"),
        520 => Some("jsonb_object_length"),
        521 => Some("jsonb_parse"),
        522 => Some("jsonb_path_exists"),
        523 => Some("jsonb_path_join"),
        524 => Some("jsonb_path_split"),
        525 => Some("jsonb_pretty"),
        526 => Some("jsonb_set_key"),
        527 => Some("jsonb_set_path"),
        528 => Some("jsonb_strip_nulls"),
        529 => Some("jsonb_typeof"),
        530 => Some("tbigint_abs"),
        531 => Some("tbigint_add"),
        532 => Some("tbigint_add_scalar"),
        533 => Some("tbigint_always_eq"),
        534 => Some("tbigint_always_gt"),
        535 => Some("tbigint_always_lt"),
        536 => Some("tbigint_append_instant"),
        537 => Some("tbigint_append_sequence"),
        538 => Some("tbigint_at_max"),
        539 => Some("tbigint_at_min"),
        540 => Some("tbigint_at_period"),
        541 => Some("tbigint_at_range"),
        542 => Some("tbigint_at_value"),
        543 => Some("tbigint_at_values"),
        544 => Some("tbigint_diff"),
        545 => Some("tbigint_div"),
        546 => Some("tbigint_div_scalar"),
        547 => Some("tbigint_duration"),
        548 => Some("tbigint_end_instant"),
        549 => Some("tbigint_end_timestamp"),
        550 => Some("tbigint_end_value"),
        551 => Some("tbigint_ever_eq"),
        552 => Some("tbigint_ever_gt"),
        553 => Some("tbigint_ever_lt"),
        554 => Some("tbigint_exponential_smooth"),
        555 => Some("tbigint_from_csv"),
        556 => Some("tbigint_from_wkb"),
        557 => Some("tbigint_hash"),
        558 => Some("tbigint_instant_from_ewkt"),
        559 => Some("tbigint_instant_from_mfjson"),
        560 => Some("tbigint_instant_from_text"),
        561 => Some("tbigint_instant_n"),
        562 => Some("tbigint_instant_new"),
        563 => Some("tbigint_instant_to_mfjson"),
        564 => Some("tbigint_instant_to_text"),
        565 => Some("tbigint_instant_to_tfloat"),
        566 => Some("tbigint_instant_to_tint"),
        567 => Some("tbigint_instants"),
        568 => Some("tbigint_integral"),
        569 => Some("tbigint_kurtosis"),
        570 => Some("tbigint_lift_between"),
        571 => Some("tbigint_lift_eq"),
        572 => Some("tbigint_lift_ge"),
        573 => Some("tbigint_lift_gt"),
        574 => Some("tbigint_lift_le"),
        575 => Some("tbigint_lift_lt"),
        576 => Some("tbigint_lift_ne"),
        577 => Some("tbigint_lower_inclusive"),
        578 => Some("tbigint_max_value"),
        579 => Some("tbigint_median"),
        580 => Some("tbigint_memsize"),
        581 => Some("tbigint_merge"),
        582 => Some("tbigint_min_value"),
        583 => Some("tbigint_minus_period"),
        584 => Some("tbigint_minus_range"),
        585 => Some("tbigint_minus_value"),
        586 => Some("tbigint_minus_values"),
        587 => Some("tbigint_mod"),
        588 => Some("tbigint_mod_scalar"),
        589 => Some("tbigint_mode"),
        590 => Some("tbigint_moving_average"),
        591 => Some("tbigint_mul"),
        592 => Some("tbigint_mul_scalar"),
        593 => Some("tbigint_negate"),
        594 => Some("tbigint_num_anomalies_iqr"),
        595 => Some("tbigint_num_anomalies_zscore"),
        596 => Some("tbigint_num_instants"),
        597 => Some("tbigint_num_timestamps"),
        598 => Some("tbigint_quantile"),
        599 => Some("tbigint_quantiles"),
        600 => Some("tbigint_rolling_mean"),
        601 => Some("tbigint_rolling_stddev"),
        602 => Some("tbigint_rolling_variance"),
        603 => Some("tbigint_sequence_from_ewkt"),
        604 => Some("tbigint_sequence_from_mfjson"),
        605 => Some("tbigint_sequence_from_pairs"),
        606 => Some("tbigint_sequence_from_text"),
        607 => Some("tbigint_sequence_new"),
        608 => Some("tbigint_sequence_to_mfjson"),
        609 => Some("tbigint_sequence_to_text"),
        610 => Some("tbigint_sequence_to_tfloat"),
        611 => Some("tbigint_skewness"),
        612 => Some("tbigint_start_instant"),
        613 => Some("tbigint_start_timestamp"),
        614 => Some("tbigint_start_value"),
        615 => Some("tbigint_sub"),
        616 => Some("tbigint_sub_scalar"),
        617 => Some("tbigint_temporal_avg"),
        618 => Some("tbigint_temporal_count"),
        619 => Some("tbigint_temporal_eq"),
        620 => Some("tbigint_temporal_extent"),
        621 => Some("tbigint_temporal_ge"),
        622 => Some("tbigint_temporal_gt"),
        623 => Some("tbigint_temporal_le"),
        624 => Some("tbigint_temporal_lt"),
        625 => Some("tbigint_temporal_max"),
        626 => Some("tbigint_temporal_min"),
        627 => Some("tbigint_temporal_mode"),
        628 => Some("tbigint_temporal_ne"),
        629 => Some("tbigint_temporal_range"),
        630 => Some("tbigint_temporal_sum"),
        631 => Some("tbigint_time_span"),
        632 => Some("tbigint_timestamp_n"),
        633 => Some("tbigint_timestamps"),
        634 => Some("tbigint_to_csv"),
        635 => Some("tbigint_to_json"),
        636 => Some("tbigint_to_tint"),
        637 => Some("tbigint_to_wkb"),
        638 => Some("tbigint_twavg"),
        639 => Some("tbigint_twcount"),
        640 => Some("tbigint_twmax"),
        641 => Some("tbigint_twmin"),
        642 => Some("tbigint_twstddev"),
        643 => Some("tbigint_twsum"),
        644 => Some("tbigint_twvariance"),
        645 => Some("tbigint_upper_inclusive"),
        646 => Some("tbigint_upsample_to_target"),
        647 => Some("tbigint_value_at"),
        648 => Some("tbigint_value_bucket_index"),
        649 => Some("tbigint_value_bucket_split"),
        650 => Some("tbigint_value_extent"),
        651 => Some("tbigint_value_n"),
        652 => Some("tbigint_values"),
        653 => Some("tbigint_wavg"),
        654 => Some("tbigint_wcount"),
        655 => Some("tbigint_when_eq"),
        656 => Some("tbigint_when_ge"),
        657 => Some("tbigint_when_gt"),
        658 => Some("tbigint_when_le"),
        659 => Some("tbigint_when_lt"),
        660 => Some("tbigint_when_ne"),
        661 => Some("tbigint_wmax"),
        662 => Some("tbigint_wmin"),
        663 => Some("tbigint_wsum"),
        664 => Some("tbool_after_ts"),
        665 => Some("tbool_always_false"),
        666 => Some("tbool_always_true"),
        667 => Some("tbool_and"),
        668 => Some("tbool_and_all"),
        669 => Some("tbool_append_instant"),
        670 => Some("tbool_append_sequence"),
        671 => Some("tbool_at_false"),
        672 => Some("tbool_at_period"),
        673 => Some("tbool_at_true"),
        674 => Some("tbool_at_value"),
        675 => Some("tbool_before_ts"),
        676 => Some("tbool_count_changes"),
        677 => Some("tbool_count_false"),
        678 => Some("tbool_count_true"),
        679 => Some("tbool_duration"),
        680 => Some("tbool_end_timestamp"),
        681 => Some("tbool_end_value"),
        682 => Some("tbool_ever_false"),
        683 => Some("tbool_ever_true"),
        684 => Some("tbool_from_csv"),
        685 => Some("tbool_from_ewkt"),
        686 => Some("tbool_instant_from_ewkt"),
        687 => Some("tbool_instant_memsize"),
        688 => Some("tbool_instant_new"),
        689 => Some("tbool_instants"),
        690 => Some("tbool_merge"),
        691 => Some("tbool_minus_period"),
        692 => Some("tbool_minus_value"),
        693 => Some("tbool_not"),
        694 => Some("tbool_num_instants"),
        695 => Some("tbool_or"),
        696 => Some("tbool_or_all"),
        697 => Some("tbool_over_after_ts"),
        698 => Some("tbool_over_before_ts"),
        699 => Some("tbool_segments"),
        700 => Some("tbool_sequence_from_ewkt"),
        701 => Some("tbool_sequence_memsize"),
        702 => Some("tbool_sequence_new"),
        703 => Some("tbool_sequences"),
        704 => Some("tbool_start_timestamp"),
        705 => Some("tbool_start_value"),
        706 => Some("tbool_temporal_extent"),
        707 => Some("tbool_time_bins"),
        708 => Some("tbool_time_span"),
        709 => Some("tbool_to_csv"),
        710 => Some("tbool_to_json"),
        711 => Some("tbool_unnest"),
        712 => Some("tbool_value_at"),
        713 => Some("tbool_value_n"),
        714 => Some("tbool_when_false"),
        715 => Some("tbool_when_true"),
        716 => Some("tcount_timestamptz"),
        717 => Some("tcount_tstzset"),
        718 => Some("tcount_tstzspan"),
        719 => Some("tcount_tstzspanset"),
        720 => Some("temporal_contains"),
        721 => Some("temporal_during"),
        722 => Some("temporal_finishes"),
        723 => Some("temporal_follows"),
        724 => Some("temporal_meets"),
        725 => Some("temporal_overlaps"),
        726 => Some("temporal_precedes"),
        727 => Some("temporal_starts"),
        728 => Some("text_set_cmp"),
        729 => Some("text_set_end_value"),
        730 => Some("text_set_eq"),
        731 => Some("text_set_from_text"),
        732 => Some("text_set_ge"),
        733 => Some("text_set_gt"),
        734 => Some("text_set_le"),
        735 => Some("text_set_lt"),
        736 => Some("text_set_memsize"),
        737 => Some("text_set_ne"),
        738 => Some("text_set_start_value"),
        739 => Some("text_set_to_text"),
        740 => Some("text_set_value_n"),
        741 => Some("textset_contains"),
        742 => Some("textset_from_text"),
        743 => Some("textset_intersection"),
        744 => Some("textset_is_disjoint"),
        745 => Some("textset_is_subset"),
        746 => Some("textset_is_superset"),
        747 => Some("textset_len"),
        748 => Some("textset_make"),
        749 => Some("textset_max"),
        750 => Some("textset_min"),
        751 => Some("textset_minus"),
        752 => Some("textset_symdiff"),
        753 => Some("textset_to_text"),
        754 => Some("textset_union"),
        755 => Some("textset_values"),
        756 => Some("tfloat_abs"),
        757 => Some("tfloat_acf"),
        758 => Some("tfloat_add"),
        759 => Some("tfloat_add_scalar"),
        760 => Some("tfloat_after_ts"),
        761 => Some("tfloat_always_eq"),
        762 => Some("tfloat_always_eq_scalar"),
        763 => Some("tfloat_always_gt"),
        764 => Some("tfloat_always_gt_scalar"),
        765 => Some("tfloat_always_lt"),
        766 => Some("tfloat_always_lt_scalar"),
        767 => Some("tfloat_append_instant"),
        768 => Some("tfloat_append_instant_limits"),
        769 => Some("tfloat_append_sequence"),
        770 => Some("tfloat_arrow_schema"),
        771 => Some("tfloat_at_max"),
        772 => Some("tfloat_at_min"),
        773 => Some("tfloat_at_period"),
        774 => Some("tfloat_at_value"),
        775 => Some("tfloat_autocorrelation"),
        776 => Some("tfloat_batch_from_parquet"),
        777 => Some("tfloat_batch_to_parquet"),
        778 => Some("tfloat_before_ts"),
        779 => Some("tfloat_coefficient_of_variation"),
        780 => Some("tfloat_correlation_at"),
        781 => Some("tfloat_cos"),
        782 => Some("tfloat_count"),
        783 => Some("tfloat_count_trend_segments"),
        784 => Some("tfloat_cross_correlation"),
        785 => Some("tfloat_derivative"),
        786 => Some("tfloat_derivative_wit"),
        787 => Some("tfloat_detect_anomalies_iqr"),
        788 => Some("tfloat_detect_anomalies_moving_avg"),
        789 => Some("tfloat_detect_anomalies_zscore"),
        790 => Some("tfloat_detect_level_crossings"),
        791 => Some("tfloat_detect_peaks"),
        792 => Some("tfloat_detect_periodicity"),
        793 => Some("tfloat_detect_trend"),
        794 => Some("tfloat_detect_valleys"),
        795 => Some("tfloat_detect_zero_crossings"),
        796 => Some("tfloat_detrend"),
        797 => Some("tfloat_div"),
        798 => Some("tfloat_div_scalar"),
        799 => Some("tfloat_ema"),
        800 => Some("tfloat_end_value"),
        801 => Some("tfloat_ever_eq"),
        802 => Some("tfloat_ever_eq_scalar"),
        803 => Some("tfloat_ever_gt"),
        804 => Some("tfloat_ever_gt_scalar"),
        805 => Some("tfloat_ever_lt"),
        806 => Some("tfloat_ever_lt_scalar"),
        807 => Some("tfloat_exp"),
        808 => Some("tfloat_exponential_smooth"),
        809 => Some("tfloat_fill_gaps"),
        810 => Some("tfloat_fill_gaps_auto_interval"),
        811 => Some("tfloat_forecast_linear"),
        812 => Some("tfloat_from_csv"),
        813 => Some("tfloat_from_ewkt"),
        814 => Some("tfloat_from_ipc"),
        815 => Some("tfloat_from_parquet"),
        816 => Some("tfloat_gaussian_smooth"),
        817 => Some("tfloat_histogram"),
        818 => Some("tfloat_instant_from_ewkt"),
        819 => Some("tfloat_instant_memsize"),
        820 => Some("tfloat_instant_new"),
        821 => Some("tfloat_instant_round"),
        822 => Some("tfloat_instants"),
        823 => Some("tfloat_integral"),
        824 => Some("tfloat_iqr"),
        825 => Some("tfloat_kalman_smooth"),
        826 => Some("tfloat_kde"),
        827 => Some("tfloat_kurtosis"),
        828 => Some("tfloat_lift_between"),
        829 => Some("tfloat_lift_eq"),
        830 => Some("tfloat_lift_ge"),
        831 => Some("tfloat_lift_gt"),
        832 => Some("tfloat_lift_le"),
        833 => Some("tfloat_lift_lt"),
        834 => Some("tfloat_lift_ne"),
        835 => Some("tfloat_ln"),
        836 => Some("tfloat_log10"),
        837 => Some("tfloat_max_cross_correlation"),
        838 => Some("tfloat_max_value"),
        839 => Some("tfloat_max_value_wit"),
        840 => Some("tfloat_mean"),
        841 => Some("tfloat_median"),
        842 => Some("tfloat_merge"),
        843 => Some("tfloat_min_value"),
        844 => Some("tfloat_min_value_wit"),
        845 => Some("tfloat_minus_period"),
        846 => Some("tfloat_minus_value"),
        847 => Some("tfloat_mode_single"),
        848 => Some("tfloat_moving_avg"),
        849 => Some("tfloat_moving_max"),
        850 => Some("tfloat_moving_min"),
        851 => Some("tfloat_moving_sum"),
        852 => Some("tfloat_moving_variance"),
        853 => Some("tfloat_mul"),
        854 => Some("tfloat_mul_scalar"),
        855 => Some("tfloat_negate"),
        856 => Some("tfloat_num_instants"),
        857 => Some("tfloat_num_instants_wit"),
        858 => Some("tfloat_over_after_ts"),
        859 => Some("tfloat_over_before_ts"),
        860 => Some("tfloat_pacf"),
        861 => Some("tfloat_pct_change"),
        862 => Some("tfloat_percentile"),
        863 => Some("tfloat_pow"),
        864 => Some("tfloat_precision"),
        865 => Some("tfloat_quantile"),
        866 => Some("tfloat_remove_outliers"),
        867 => Some("tfloat_rolling_stddev"),
        868 => Some("tfloat_rolling_variance"),
        869 => Some("tfloat_sample_entropy"),
        870 => Some("tfloat_segments"),
        871 => Some("tfloat_sequence_from_ewkt"),
        872 => Some("tfloat_sequence_memsize"),
        873 => Some("tfloat_sequence_new"),
        874 => Some("tfloat_sequence_new_with_interpolation"),
        875 => Some("tfloat_sequence_round"),
        876 => Some("tfloat_sequences"),
        877 => Some("tfloat_sin"),
        878 => Some("tfloat_skewness"),
        879 => Some("tfloat_sqrt"),
        880 => Some("tfloat_start_value"),
        881 => Some("tfloat_stddev"),
        882 => Some("tfloat_sub"),
        883 => Some("tfloat_sub_scalar"),
        884 => Some("tfloat_sum"),
        885 => Some("tfloat_tan"),
        886 => Some("tfloat_temporal_correlation"),
        887 => Some("tfloat_temporal_covariance"),
        888 => Some("tfloat_temporal_entropy"),
        889 => Some("tfloat_temporal_eq"),
        890 => Some("tfloat_temporal_extent"),
        891 => Some("tfloat_temporal_ge"),
        892 => Some("tfloat_temporal_gt"),
        893 => Some("tfloat_temporal_iqr"),
        894 => Some("tfloat_temporal_kurtosis"),
        895 => Some("tfloat_temporal_le"),
        896 => Some("tfloat_temporal_lt"),
        897 => Some("tfloat_temporal_mode"),
        898 => Some("tfloat_temporal_ne"),
        899 => Some("tfloat_temporal_range"),
        900 => Some("tfloat_temporal_skewness"),
        901 => Some("tfloat_temporal_weighted_median"),
        902 => Some("tfloat_time_bins"),
        903 => Some("tfloat_time_boxes"),
        904 => Some("tfloat_time_span"),
        905 => Some("tfloat_to_csv"),
        906 => Some("tfloat_to_ipc"),
        907 => Some("tfloat_to_json"),
        908 => Some("tfloat_to_linear"),
        909 => Some("tfloat_to_parquet"),
        910 => Some("tfloat_to_stepwise"),
        911 => Some("tfloat_trend_slope"),
        912 => Some("tfloat_twavg"),
        913 => Some("tfloat_twcount"),
        914 => Some("tfloat_twmax"),
        915 => Some("tfloat_twmin"),
        916 => Some("tfloat_twstddev"),
        917 => Some("tfloat_twsum"),
        918 => Some("tfloat_twvariance"),
        919 => Some("tfloat_unnest"),
        920 => Some("tfloat_upsample_to_target"),
        921 => Some("tfloat_value_at"),
        922 => Some("tfloat_value_bins"),
        923 => Some("tfloat_value_boxes"),
        924 => Some("tfloat_value_extent"),
        925 => Some("tfloat_value_n"),
        926 => Some("tfloat_variance"),
        927 => Some("tfloat_wavg"),
        928 => Some("tfloat_wcount"),
        929 => Some("tfloat_when_eq"),
        930 => Some("tfloat_when_ge"),
        931 => Some("tfloat_when_gt"),
        932 => Some("tfloat_when_le"),
        933 => Some("tfloat_when_lt"),
        934 => Some("tfloat_when_ne"),
        935 => Some("tfloat_wmax"),
        936 => Some("tfloat_wmin"),
        937 => Some("tfloat_wsum"),
        938 => Some("tgeometry_geometry_types"),
        939 => Some("tint_abs"),
        940 => Some("tint_add"),
        941 => Some("tint_add_scalar"),
        942 => Some("tint_after_ts"),
        943 => Some("tint_always_eq"),
        944 => Some("tint_always_eq_scalar"),
        945 => Some("tint_always_gt"),
        946 => Some("tint_always_gt_scalar"),
        947 => Some("tint_always_lt"),
        948 => Some("tint_always_lt_scalar"),
        949 => Some("tint_append_instant"),
        950 => Some("tint_append_instant_limits"),
        951 => Some("tint_append_sequence"),
        952 => Some("tint_arrow_schema"),
        953 => Some("tint_at_max"),
        954 => Some("tint_at_min"),
        955 => Some("tint_at_period"),
        956 => Some("tint_at_range"),
        957 => Some("tint_at_value"),
        958 => Some("tint_at_values"),
        959 => Some("tint_before_ts"),
        960 => Some("tint_diff"),
        961 => Some("tint_div"),
        962 => Some("tint_div_scalar"),
        963 => Some("tint_end_value"),
        964 => Some("tint_ever_eq"),
        965 => Some("tint_ever_eq_scalar"),
        966 => Some("tint_ever_gt"),
        967 => Some("tint_ever_gt_scalar"),
        968 => Some("tint_ever_lt"),
        969 => Some("tint_ever_lt_scalar"),
        970 => Some("tint_exponential_smooth"),
        971 => Some("tint_from_csv"),
        972 => Some("tint_from_ewkt"),
        973 => Some("tint_from_ipc"),
        974 => Some("tint_from_parquet"),
        975 => Some("tint_instant_from_ewkt"),
        976 => Some("tint_instant_memsize"),
        977 => Some("tint_instant_new"),
        978 => Some("tint_instant_to_tbigint"),
        979 => Some("tint_instant_to_tfloat"),
        980 => Some("tint_instants"),
        981 => Some("tint_integral"),
        982 => Some("tint_kurtosis"),
        983 => Some("tint_lift_between"),
        984 => Some("tint_lift_eq"),
        985 => Some("tint_lift_ge"),
        986 => Some("tint_lift_gt"),
        987 => Some("tint_lift_le"),
        988 => Some("tint_lift_lt"),
        989 => Some("tint_lift_ne"),
        990 => Some("tint_max_value"),
        991 => Some("tint_max_value_scalar"),
        992 => Some("tint_max_value_wit"),
        993 => Some("tint_median"),
        994 => Some("tint_merge"),
        995 => Some("tint_min_value"),
        996 => Some("tint_min_value_scalar"),
        997 => Some("tint_min_value_wit"),
        998 => Some("tint_minus_period"),
        999 => Some("tint_minus_range"),
        1000 => Some("tint_minus_value"),
        1001 => Some("tint_minus_values"),
        1002 => Some("tint_mod"),
        1003 => Some("tint_mod_scalar"),
        1004 => Some("tint_mode"),
        1005 => Some("tint_moving_average"),
        1006 => Some("tint_mul"),
        1007 => Some("tint_mul_scalar"),
        1008 => Some("tint_negate"),
        1009 => Some("tint_num_anomalies_iqr"),
        1010 => Some("tint_num_anomalies_zscore"),
        1011 => Some("tint_num_instants"),
        1012 => Some("tint_num_instants_wit"),
        1013 => Some("tint_over_after_ts"),
        1014 => Some("tint_over_before_ts"),
        1015 => Some("tint_quantile"),
        1016 => Some("tint_quantiles"),
        1017 => Some("tint_rolling_mean"),
        1018 => Some("tint_rolling_stddev"),
        1019 => Some("tint_rolling_variance"),
        1020 => Some("tint_segments"),
        1021 => Some("tint_sequence_from_ewkt"),
        1022 => Some("tint_sequence_memsize"),
        1023 => Some("tint_sequence_new"),
        1024 => Some("tint_sequence_to_tfloat"),
        1025 => Some("tint_sequences"),
        1026 => Some("tint_skewness"),
        1027 => Some("tint_start_value"),
        1028 => Some("tint_sub"),
        1029 => Some("tint_sub_scalar"),
        1030 => Some("tint_temporal_avg"),
        1031 => Some("tint_temporal_eq"),
        1032 => Some("tint_temporal_extent"),
        1033 => Some("tint_temporal_ge"),
        1034 => Some("tint_temporal_gt"),
        1035 => Some("tint_temporal_le"),
        1036 => Some("tint_temporal_lt"),
        1037 => Some("tint_temporal_mode"),
        1038 => Some("tint_temporal_ne"),
        1039 => Some("tint_temporal_range"),
        1040 => Some("tint_time_bins"),
        1041 => Some("tint_time_boxes"),
        1042 => Some("tint_time_span"),
        1043 => Some("tint_to_csv"),
        1044 => Some("tint_to_ipc"),
        1045 => Some("tint_to_json"),
        1046 => Some("tint_to_parquet"),
        1047 => Some("tint_to_tbigint"),
        1048 => Some("tint_twavg"),
        1049 => Some("tint_twcount"),
        1050 => Some("tint_twmax"),
        1051 => Some("tint_twmin"),
        1052 => Some("tint_twstddev"),
        1053 => Some("tint_twsum"),
        1054 => Some("tint_twvariance"),
        1055 => Some("tint_unnest"),
        1056 => Some("tint_upsample_to_target"),
        1057 => Some("tint_value_at"),
        1058 => Some("tint_value_bins"),
        1059 => Some("tint_value_boxes"),
        1060 => Some("tint_value_bucket_index"),
        1061 => Some("tint_value_extent"),
        1062 => Some("tint_value_n"),
        1063 => Some("tint_wavg"),
        1064 => Some("tint_wcount"),
        1065 => Some("tint_when_eq"),
        1066 => Some("tint_when_ge"),
        1067 => Some("tint_when_gt"),
        1068 => Some("tint_when_le"),
        1069 => Some("tint_when_lt"),
        1070 => Some("tint_when_ne"),
        1071 => Some("tint_wmax"),
        1072 => Some("tint_wmin"),
        1073 => Some("tint_wsum"),
        1074 => Some("tjsonb_always_contains"),
        1075 => Some("tjsonb_always_contains_key"),
        1076 => Some("tjsonb_always_eq_value"),
        1077 => Some("tjsonb_always_path_exists"),
        1078 => Some("tjsonb_always_typeof"),
        1079 => Some("tjsonb_array_append"),
        1080 => Some("tjsonb_array_prepend"),
        1081 => Some("tjsonb_at_contains"),
        1082 => Some("tjsonb_at_path_exists"),
        1083 => Some("tjsonb_at_period"),
        1084 => Some("tjsonb_at_time"),
        1085 => Some("tjsonb_at_typeof"),
        1086 => Some("tjsonb_at_value"),
        1087 => Some("tjsonb_at_value_matching_key"),
        1088 => Some("tjsonb_at_value_matching_path"),
        1089 => Some("tjsonb_concat"),
        1090 => Some("tjsonb_count_contains"),
        1091 => Some("tjsonb_count_key"),
        1092 => Some("tjsonb_count_path"),
        1093 => Some("tjsonb_count_typeof"),
        1094 => Some("tjsonb_delete_key"),
        1095 => Some("tjsonb_delete_path"),
        1096 => Some("tjsonb_duration"),
        1097 => Some("tjsonb_end_instant"),
        1098 => Some("tjsonb_end_timestamp"),
        1099 => Some("tjsonb_end_value"),
        1100 => Some("tjsonb_ever_contains"),
        1101 => Some("tjsonb_ever_contains_key"),
        1102 => Some("tjsonb_ever_eq_value"),
        1103 => Some("tjsonb_ever_path_exists"),
        1104 => Some("tjsonb_ever_typeof"),
        1105 => Some("tjsonb_from_csv"),
        1106 => Some("tjsonb_from_wkb"),
        1107 => Some("tjsonb_hash"),
        1108 => Some("tjsonb_instant_from_jsonb"),
        1109 => Some("tjsonb_instant_from_mfjson"),
        1110 => Some("tjsonb_instant_from_text"),
        1111 => Some("tjsonb_instant_n"),
        1112 => Some("tjsonb_instant_new"),
        1113 => Some("tjsonb_instant_to_mfjson"),
        1114 => Some("tjsonb_instant_to_text"),
        1115 => Some("tjsonb_instants"),
        1116 => Some("tjsonb_key_at"),
        1117 => Some("tjsonb_lower_inclusive"),
        1118 => Some("tjsonb_memsize"),
        1119 => Some("tjsonb_merge_deep"),
        1120 => Some("tjsonb_minus_contains"),
        1121 => Some("tjsonb_minus_path_exists"),
        1122 => Some("tjsonb_minus_period"),
        1123 => Some("tjsonb_minus_time"),
        1124 => Some("tjsonb_minus_typeof"),
        1125 => Some("tjsonb_minus_value"),
        1126 => Some("tjsonb_minus_value_matching_key"),
        1127 => Some("tjsonb_minus_value_matching_path"),
        1128 => Some("tjsonb_num_instants"),
        1129 => Some("tjsonb_num_timestamps"),
        1130 => Some("tjsonb_path_at"),
        1131 => Some("tjsonb_project_tbool"),
        1132 => Some("tjsonb_project_tfloat"),
        1133 => Some("tjsonb_project_tint"),
        1134 => Some("tjsonb_project_ttext"),
        1135 => Some("tjsonb_sequence_from_mfjson"),
        1136 => Some("tjsonb_sequence_from_pairs"),
        1137 => Some("tjsonb_sequence_from_text"),
        1138 => Some("tjsonb_sequence_new"),
        1139 => Some("tjsonb_sequence_to_mfjson"),
        1140 => Some("tjsonb_sequence_to_text"),
        1141 => Some("tjsonb_set_key"),
        1142 => Some("tjsonb_set_path"),
        1143 => Some("tjsonb_start_instant"),
        1144 => Some("tjsonb_start_timestamp"),
        1145 => Some("tjsonb_start_value"),
        1146 => Some("tjsonb_time_span"),
        1147 => Some("tjsonb_timestamp_n"),
        1148 => Some("tjsonb_timestamps"),
        1149 => Some("tjsonb_to_csv"),
        1150 => Some("tjsonb_to_ttext"),
        1151 => Some("tjsonb_to_wkb"),
        1152 => Some("tjsonb_typeof_at"),
        1153 => Some("tjsonb_upper_inclusive"),
        1154 => Some("tjsonb_value_at"),
        1155 => Some("tjsonb_value_n"),
        1156 => Some("tjsonb_values"),
        1157 => Some("tstz_set_cmp"),
        1158 => Some("tstz_set_end_value"),
        1159 => Some("tstz_set_eq"),
        1160 => Some("tstz_set_from_text"),
        1161 => Some("tstz_set_ge"),
        1162 => Some("tstz_set_gt"),
        1163 => Some("tstz_set_le"),
        1164 => Some("tstz_set_lt"),
        1165 => Some("tstz_set_memsize"),
        1166 => Some("tstz_set_ne"),
        1167 => Some("tstz_set_start_value"),
        1168 => Some("tstz_set_to_text"),
        1169 => Some("tstz_set_value_n"),
        1170 => Some("tstz_span_after"),
        1171 => Some("tstz_span_after_value"),
        1172 => Some("tstz_span_before"),
        1173 => Some("tstz_span_before_value"),
        1174 => Some("tstz_span_bins"),
        1175 => Some("tstz_span_cmp"),
        1176 => Some("tstz_span_eq"),
        1177 => Some("tstz_span_extent"),
        1178 => Some("tstz_span_from_text"),
        1179 => Some("tstz_span_ge"),
        1180 => Some("tstz_span_gt"),
        1181 => Some("tstz_span_le"),
        1182 => Some("tstz_span_lt"),
        1183 => Some("tstz_span_memsize"),
        1184 => Some("tstz_span_ne"),
        1185 => Some("tstz_span_over_after"),
        1186 => Some("tstz_span_over_after_value"),
        1187 => Some("tstz_span_over_before"),
        1188 => Some("tstz_span_over_before_value"),
        1189 => Some("tstz_span_to_text"),
        1190 => Some("tstz_spanset_after"),
        1191 => Some("tstz_spanset_after_value"),
        1192 => Some("tstz_spanset_before"),
        1193 => Some("tstz_spanset_before_value"),
        1194 => Some("tstz_spanset_bins"),
        1195 => Some("tstz_spanset_cmp"),
        1196 => Some("tstz_spanset_end_span"),
        1197 => Some("tstz_spanset_eq"),
        1198 => Some("tstz_spanset_extent"),
        1199 => Some("tstz_spanset_from_text"),
        1200 => Some("tstz_spanset_ge"),
        1201 => Some("tstz_spanset_gt"),
        1202 => Some("tstz_spanset_le"),
        1203 => Some("tstz_spanset_lt"),
        1204 => Some("tstz_spanset_memsize"),
        1205 => Some("tstz_spanset_ne"),
        1206 => Some("tstz_spanset_over_after"),
        1207 => Some("tstz_spanset_over_after_value"),
        1208 => Some("tstz_spanset_over_before"),
        1209 => Some("tstz_spanset_over_before_value"),
        1210 => Some("tstz_spanset_span_n"),
        1211 => Some("tstz_spanset_start_span"),
        1212 => Some("tstz_spanset_to_text"),
        1213 => Some("tstzset_contains"),
        1214 => Some("tstzset_intersection"),
        1215 => Some("tstzset_len"),
        1216 => Some("tstzset_make"),
        1217 => Some("tstzset_minus"),
        1218 => Some("tstzset_nth"),
        1219 => Some("tstzset_overlaps"),
        1220 => Some("tstzset_shift"),
        1221 => Some("tstzset_successor"),
        1222 => Some("tstzset_to_text"),
        1223 => Some("tstzset_union"),
        1224 => Some("tstzspan_adjacent"),
        1225 => Some("tstzspan_contains"),
        1226 => Some("tstzspan_contains_span"),
        1227 => Some("tstzspan_duration"),
        1228 => Some("tstzspan_end"),
        1229 => Some("tstzspan_expand"),
        1230 => Some("tstzspan_from_text"),
        1231 => Some("tstzspan_intersection"),
        1232 => Some("tstzspan_make"),
        1233 => Some("tstzspan_midpoint"),
        1234 => Some("tstzspan_minus"),
        1235 => Some("tstzspan_overlaps"),
        1236 => Some("tstzspan_scale"),
        1237 => Some("tstzspan_shift"),
        1238 => Some("tstzspan_start"),
        1239 => Some("tstzspan_to_text"),
        1240 => Some("tstzspan_union"),
        1241 => Some("tstzspanset_from_text"),
        1242 => Some("tstzspanset_to_text"),
        1243 => Some("ttext_after_ts"),
        1244 => Some("ttext_always_eq"),
        1245 => Some("ttext_append_instant"),
        1246 => Some("ttext_append_sequence"),
        1247 => Some("ttext_at_period"),
        1248 => Some("ttext_at_value"),
        1249 => Some("ttext_before_ts"),
        1250 => Some("ttext_btrim"),
        1251 => Some("ttext_concat"),
        1252 => Some("ttext_concat_str"),
        1253 => Some("ttext_duration"),
        1254 => Some("ttext_end_timestamp"),
        1255 => Some("ttext_end_value"),
        1256 => Some("ttext_ever_eq"),
        1257 => Some("ttext_from_csv"),
        1258 => Some("ttext_from_ewkt"),
        1259 => Some("ttext_ilike"),
        1260 => Some("ttext_instant_from_ewkt"),
        1261 => Some("ttext_instant_memsize"),
        1262 => Some("ttext_instant_new"),
        1263 => Some("ttext_instants"),
        1264 => Some("ttext_left"),
        1265 => Some("ttext_length"),
        1266 => Some("ttext_like"),
        1267 => Some("ttext_lower"),
        1268 => Some("ttext_ltrim"),
        1269 => Some("ttext_merge"),
        1270 => Some("ttext_minus_period"),
        1271 => Some("ttext_minus_value"),
        1272 => Some("ttext_num_instants"),
        1273 => Some("ttext_num_instants_scalar"),
        1274 => Some("ttext_over_after_ts"),
        1275 => Some("ttext_over_before_ts"),
        1276 => Some("ttext_prepend_str"),
        1277 => Some("ttext_regex_match"),
        1278 => Some("ttext_regex_replace"),
        1279 => Some("ttext_right"),
        1280 => Some("ttext_rtrim"),
        1281 => Some("ttext_segments"),
        1282 => Some("ttext_sequence_from_ewkt"),
        1283 => Some("ttext_sequence_memsize"),
        1284 => Some("ttext_sequence_new"),
        1285 => Some("ttext_sequences"),
        1286 => Some("ttext_start_timestamp"),
        1287 => Some("ttext_start_value"),
        1288 => Some("ttext_temporal_extent"),
        1289 => Some("ttext_temporal_max"),
        1290 => Some("ttext_temporal_min"),
        1291 => Some("ttext_time_bins"),
        1292 => Some("ttext_time_span"),
        1293 => Some("ttext_to_csv"),
        1294 => Some("ttext_to_json"),
        1295 => Some("ttext_to_tjsonb"),
        1296 => Some("ttext_unnest"),
        1297 => Some("ttext_upper"),
        1298 => Some("ttext_value_at"),
        1299 => Some("ttext_value_n"),
        1300 => Some("ttext_when_eq"),
        1301 => Some("ttext_when_ne"),
        _ => None,
    }
}

/// SQL-name aliasing: translate a compact-form SQL name (e.g.
/// `st_geomfromtext`) to its WIT-canonical (long) form
/// (`st_geom_from_text`) before kebab-casing to the provider
/// method name. The catalog carries both spellings as scalars in
/// `leaf.scalars` (both must register with DuckDB so callers can
/// use either), and as first-class `[[aliases]]` entries. The
/// provider dispatch only matches the WIT-canonical form; the
/// bridge's `dispatch_call_scalar` calls `canonical_for(name)` so
/// both spellings route to the same provider arm.
fn canonical_for(name: &str) -> &str {
    match name {
        "tfloat_variance_aggregate" => "tfloat_variance_agg",
        "tint_count_aggregate" => "tint_count_agg",
        "tint_max_value_aggregate" => "tint_max_value_agg",
        "tint_min_value_aggregate" => "tint_min_value_agg",
        "tint_range_aggregate" => "tint_range_agg",
        "ttext_concat_aggregate" => "ttext_concat_agg",
        "dateset_contains" => "date_set_contains",
        "dateset_intersection" => "date_set_intersection",
        "dateset_len" => "date_set_len",
        "dateset_make" => "date_set_make",
        "dateset_minus" => "date_set_minus",
        "dateset_to_text" => "date_set_to_text",
        "dateset_union" => "date_set_union",
        "datespan_adjacent" => "date_span_adjacent",
        "datespan_contains" => "date_span_contains",
        "datespan_contains_span" => "date_span_contains_span",
        "datespan_expand" => "date_span_expand",
        "datespan_from_text" => "date_span_from_text",
        "datespan_intersection" => "date_span_intersection",
        "datespan_make" => "date_span_make",
        "datespan_overlaps" => "date_span_overlaps",
        "datespan_shift" => "date_span_shift",
        "datespan_to_text" => "date_span_to_text",
        "datespan_union" => "date_span_union",
        "datespan_width" => "date_span_width",
        "datespanset_contains" => "date_spanset_contains",
        "datespanset_from_text" => "date_spanset_from_text",
        "datespanset_intersection" => "date_spanset_intersection",
        "datespanset_minus" => "date_spanset_minus",
        "datespanset_num_spans" => "date_spanset_num_spans",
        "datespanset_overlaps" => "date_spanset_overlaps",
        "datespanset_to_text" => "date_spanset_to_text",
        "datespanset_union" => "date_spanset_union",
        "datespanset_width" => "date_spanset_width",
        "floatset_contains" => "float_set_contains",
        "floatset_intersection" => "float_set_intersection",
        "floatset_len" => "float_set_len",
        "floatset_make" => "float_set_make",
        "floatset_minus" => "float_set_minus",
        "floatset_to_text" => "float_set_to_text",
        "floatset_union" => "float_set_union",
        "floatspan_adjacent" => "float_span_adjacent",
        "floatspan_contains" => "float_span_contains",
        "floatspan_contains_span" => "float_span_contains_span",
        "floatspan_expand" => "float_span_expand",
        "floatspan_from_text" => "float_span_from_text",
        "floatspan_intersection" => "float_span_intersection",
        "floatspan_make" => "float_span_make",
        "floatspan_overlaps" => "float_span_overlaps",
        "floatspan_shift" => "float_span_shift",
        "floatspan_to_text" => "float_span_to_text",
        "floatspan_union" => "float_span_union",
        "floatspan_width" => "float_span_width",
        "floatspanset_contains" => "float_spanset_contains",
        "floatspanset_from_text" => "float_spanset_from_text",
        "floatspanset_intersection" => "float_spanset_intersection",
        "floatspanset_minus" => "float_spanset_minus",
        "floatspanset_num_spans" => "float_spanset_num_spans",
        "floatspanset_overlaps" => "float_spanset_overlaps",
        "floatspanset_to_text" => "float_spanset_to_text",
        "floatspanset_union" => "float_spanset_union",
        "floatspanset_width" => "float_spanset_width",
        "intset_contains" => "int_set_contains",
        "intset_intersection" => "int_set_intersection",
        "intset_len" => "int_set_len",
        "intset_make" => "int_set_make",
        "intset_minus" => "int_set_minus",
        "intset_to_text" => "int_set_to_text",
        "intset_union" => "int_set_union",
        "intspan_adjacent" => "int_span_adjacent",
        "intspan_contains" => "int_span_contains",
        "intspan_contains_span" => "int_span_contains_span",
        "intspan_expand" => "int_span_expand",
        "intspan_from_text" => "int_span_from_text",
        "intspan_intersection" => "int_span_intersection",
        "intspan_make" => "int_span_make",
        "intspan_overlaps" => "int_span_overlaps",
        "intspan_shift" => "int_span_shift",
        "intspan_to_text" => "int_span_to_text",
        "intspan_union" => "int_span_union",
        "intspan_width" => "int_span_width",
        "intspanset_contains" => "int_spanset_contains",
        "intspanset_from_text" => "int_spanset_from_text",
        "intspanset_intersection" => "int_spanset_intersection",
        "intspanset_minus" => "int_spanset_minus",
        "intspanset_num_spans" => "int_spanset_num_spans",
        "intspanset_overlaps" => "int_spanset_overlaps",
        "intspanset_to_text" => "int_spanset_to_text",
        "intspanset_union" => "int_spanset_union",
        "intspanset_width" => "int_spanset_width",
        "textset_from_text" => "text_set_from_text",
        "textset_to_text" => "text_set_to_text",
        "tstzset_to_text" => "tstz_set_to_text",
        "tstzspan_from_text" => "tstz_span_from_text",
        "tstzspan_to_text" => "tstz_span_to_text",
        "tstzspanset_from_text" => "tstz_spanset_from_text",
        "tstzspanset_to_text" => "tstz_spanset_to_text",
        other => other,
    }
}

// ────────────────────────────────────────────────────────────
// Handle table + register block.
//
// Every scalar the catalog names gets exactly one runtime
// handle (allocated by NEXT_HANDLE.fetch_add at register time)
// and one dense arm_idx (assigned by codegen). `handle_table`
// maps handle → arm_idx; the dispatch arms (call_scalar +
// call_scalar_batch_col) look up the arm and delegate to the
// per-arm provider method through the CBOR envelope.
// ────────────────────────────────────────────────────────────

fn handle_table() -> &'static std::sync::Mutex<std::collections::HashMap<u32, usize>> {
    static T: std::sync::OnceLock<std::sync::Mutex<std::collections::HashMap<u32, usize>>> =
        std::sync::OnceLock::new();
    T.get_or_init(|| std::sync::Mutex::new(std::collections::HashMap::new()))
}

// Phase 9.3.next: aggregate + table dispatch handle tables.
// Kept structurally uniform with the scalar handle_table so a
// future consolidation into a single enum-tagged table is a
// one-liner. Empty when the catalog has no aggregates / tables.
fn aggregate_handle_table() -> &'static std::sync::Mutex<std::collections::HashMap<u32, usize>> {
    static T: std::sync::OnceLock<std::sync::Mutex<std::collections::HashMap<u32, usize>>> =
        std::sync::OnceLock::new();
    T.get_or_init(|| std::sync::Mutex::new(std::collections::HashMap::new()))
}

fn table_handle_table() -> &'static std::sync::Mutex<std::collections::HashMap<u32, usize>> {
    static T: std::sync::OnceLock<std::sync::Mutex<std::collections::HashMap<u32, usize>>> =
        std::sync::OnceLock::new();
    T.get_or_init(|| std::sync::Mutex::new(std::collections::HashMap::new()))
}

fn identity_cast_handles() -> &'static std::sync::Mutex<std::collections::HashSet<u32>> {
    static T: std::sync::OnceLock<std::sync::Mutex<std::collections::HashSet<u32>>> = std::sync::OnceLock::new();
    T.get_or_init(|| std::sync::Mutex::new(std::collections::HashSet::new()))
}

static NEXT_HANDLE: AtomicU32 = AtomicU32::new(1);


fn register_scalars() -> Result<(), Duckerror> {
    // Acquire the base scalar-registry capability from the host.
    // Fix (#834 followup): the previous implementation used
    // `runtime-ext.register-scalar-ex` with an empty arg list
    // (`args: Vec::new()` + `varargs: Some(&Logicaltype::Blob)`)
    // — variadic-Blob for every scalar. That erased the per-fn
    // arity + arg logicaltypes DuckDB needs at bind time (e.g.
    // `st_distance(GEOMETRY, GEOMETRY) -> DOUBLE` bound as
    // `st_distance(BLOB...) -> BLOB`, so callers threading typed
    // arguments couldn't overload-resolve). The base
    // `runtime.scalar-registry.register` path emits the actual
    // arity + per-arg + return logicaltype from the sibling
    // shim-interface DB (see `emit_dynlink::load_scalar_sigs`).
    let capability = runtime::get_capability(Capabilitykind::Scalar)
        .ok_or_else(|| Duckerror::Internal("host did not expose scalar capability".into()))?;
    let registry = match capability {
        runtime::Capability::Scalar(r) => r,
        _ => {
            return Err(Duckerror::Internal(
                "scalar capability returned unexpected variant".into(),
            ));
        }
    };
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 0usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_bool_after_ts (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["bitemporal_bool_after_ts".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_bool_after_ts",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_bool_as_of (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["bitemporal_bool_as_of".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_bool_as_of",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 2usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_bool_before_ts (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["bitemporal_bool_before_ts".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_bool_before_ts",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 3usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_bool_cmp (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["bitemporal_bool_cmp".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_bool_cmp",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 4usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_bool_count_current (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["bitemporal_bool_count_current".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_bool_count_current",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 5usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_bool_count_superseded (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["bitemporal_bool_count_superseded".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_bool_count_superseded",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 6usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_bool_current (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["bitemporal_bool_current".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_bool_current",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 7usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_bool_end_value (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["bitemporal_bool_end_value".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_bool_end_value",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 8usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_bool_eq (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["bitemporal_bool_eq".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_bool_eq",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 9usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_bool_ever_valid_during_count (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["bitemporal_bool_ever_valid_during_count".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_bool_ever_valid_during_count",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 10usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_bool_from_ewkt (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["bitemporal_bool_from_ewkt".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_bool_from_ewkt",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 11usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_bool_ge (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["bitemporal_bool_ge".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_bool_ge",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 12usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_bool_gt (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["bitemporal_bool_gt".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_bool_gt",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 13usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_bool_history_at_count (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["bitemporal_bool_history_at_count".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_bool_history_at_count",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 14usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Boolean },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg3".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg4".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_bool_instant_new (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["bitemporal_bool_instant_new".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_bool_instant_new",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 15usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_bool_le (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["bitemporal_bool_le".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_bool_le",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 16usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_bool_len (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["bitemporal_bool_len".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_bool_len",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 17usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_bool_lt (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["bitemporal_bool_lt".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_bool_lt",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 18usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_bool_ne (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["bitemporal_bool_ne".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_bool_ne",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 19usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_bool_over_after_ts (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["bitemporal_bool_over_after_ts".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_bool_over_after_ts",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 20usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_bool_over_before_ts (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["bitemporal_bool_over_before_ts".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_bool_over_before_ts",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 21usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_bool_sequence_memsize (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["bitemporal_bool_sequence_memsize".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_bool_sequence_memsize",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 22usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_bool_sequence_new (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["bitemporal_bool_sequence_new".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_bool_sequence_new",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 23usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_bool_start_value (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["bitemporal_bool_start_value".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_bool_start_value",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 24usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_bool_to_ewkt (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["bitemporal_bool_to_ewkt".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_bool_to_ewkt",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 25usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_bool_transaction_extent (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["bitemporal_bool_transaction_extent".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_bool_transaction_extent",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 26usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_bool_valid_at (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["bitemporal_bool_valid_at".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_bool_valid_at",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 27usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_bool_valid_during_count (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["bitemporal_bool_valid_during_count".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_bool_valid_during_count",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 28usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_bool_valid_extent (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["bitemporal_bool_valid_extent".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_bool_valid_extent",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 29usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_bool_value_n (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["bitemporal_bool_value_n".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_bool_value_n",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 30usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_float_after_ts (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["bitemporal_float_after_ts".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_float_after_ts",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 31usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_float_all_values (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["bitemporal_float_all_values".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_float_all_values",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 32usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_float_as_of (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["bitemporal_float_as_of".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_float_as_of",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 33usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_float_before_ts (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["bitemporal_float_before_ts".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_float_before_ts",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 34usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_float_cmp (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["bitemporal_float_cmp".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_float_cmp",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 35usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_float_count_current (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["bitemporal_float_count_current".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_float_count_current",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 36usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_float_count_superseded (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["bitemporal_float_count_superseded".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_float_count_superseded",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 37usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_float_current (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["bitemporal_float_current".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_float_current",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 38usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_float_current_values (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["bitemporal_float_current_values".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_float_current_values",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 39usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_float_end_value (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["bitemporal_float_end_value".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_float_end_value",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 40usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_float_eq (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["bitemporal_float_eq".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_float_eq",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 41usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_float_ever_valid_during_count (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["bitemporal_float_ever_valid_during_count".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_float_ever_valid_during_count",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 42usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_float_from_ewkt (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["bitemporal_float_from_ewkt".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_float_from_ewkt",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 43usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_float_ge (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["bitemporal_float_ge".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_float_ge",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 44usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_float_gt (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["bitemporal_float_gt".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_float_gt",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 45usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_float_history_at_count (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["bitemporal_float_history_at_count".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_float_history_at_count",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 46usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg3".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg4".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_float_instant_new (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["bitemporal_float_instant_new".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_float_instant_new",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 47usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_float_instant_round (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["bitemporal_float_instant_round".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_float_instant_round",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 48usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_float_is_current (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["bitemporal_float_is_current".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_float_is_current",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 49usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_float_is_valid_at (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["bitemporal_float_is_valid_at".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_float_is_valid_at",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 50usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_float_le (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["bitemporal_float_le".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_float_le",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 51usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_float_len (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["bitemporal_float_len".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_float_len",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 52usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_float_lt (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["bitemporal_float_lt".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_float_lt",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 53usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_float_ne (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["bitemporal_float_ne".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_float_ne",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 54usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_float_over_after_ts (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["bitemporal_float_over_after_ts".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_float_over_after_ts",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 55usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_float_over_before_ts (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["bitemporal_float_over_before_ts".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_float_over_before_ts",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 56usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_float_sequence_memsize (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["bitemporal_float_sequence_memsize".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_float_sequence_memsize",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 57usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_float_sequence_new (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["bitemporal_float_sequence_new".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_float_sequence_new",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 58usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_float_sequence_round (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["bitemporal_float_sequence_round".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_float_sequence_round",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 59usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_float_start_value (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["bitemporal_float_start_value".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_float_start_value",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 60usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_float_to_ewkt (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["bitemporal_float_to_ewkt".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_float_to_ewkt",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 61usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_float_transaction_duration (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["bitemporal_float_transaction_duration".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_float_transaction_duration",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 62usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_float_transaction_extent (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["bitemporal_float_transaction_extent".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_float_transaction_extent",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 63usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_float_valid_at (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["bitemporal_float_valid_at".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_float_valid_at",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 64usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_float_valid_duration (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["bitemporal_float_valid_duration".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_float_valid_duration",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 65usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_float_valid_during_count (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["bitemporal_float_valid_during_count".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_float_valid_during_count",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 66usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_float_valid_extent (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["bitemporal_float_valid_extent".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_float_valid_extent",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 67usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_float_value_n (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["bitemporal_float_value_n".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_float_value_n",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 68usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_float_was_known_at (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["bitemporal_float_was_known_at".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_float_was_known_at",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 69usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_int_after_ts (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["bitemporal_int_after_ts".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_int_after_ts",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 70usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_int_all_values (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["bitemporal_int_all_values".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_int_all_values",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 71usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_int_as_of (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["bitemporal_int_as_of".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_int_as_of",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 72usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_int_before_ts (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["bitemporal_int_before_ts".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_int_before_ts",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 73usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_int_cmp (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["bitemporal_int_cmp".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_int_cmp",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 74usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_int_count_current (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["bitemporal_int_count_current".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_int_count_current",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 75usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_int_count_superseded (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["bitemporal_int_count_superseded".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_int_count_superseded",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 76usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_int_current (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["bitemporal_int_current".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_int_current",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 77usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_int_current_values (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["bitemporal_int_current_values".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_int_current_values",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 78usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_int_end_value (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["bitemporal_int_end_value".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_int_end_value",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 79usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_int_eq (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["bitemporal_int_eq".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_int_eq",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 80usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_int_ever_valid_during_count (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["bitemporal_int_ever_valid_during_count".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_int_ever_valid_during_count",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 81usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_int_from_ewkt (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["bitemporal_int_from_ewkt".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_int_from_ewkt",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 82usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_int_ge (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["bitemporal_int_ge".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_int_ge",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 83usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_int_gt (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["bitemporal_int_gt".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_int_gt",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 84usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_int_history_at_count (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["bitemporal_int_history_at_count".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_int_history_at_count",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 85usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg3".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg4".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_int_instant_new (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["bitemporal_int_instant_new".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_int_instant_new",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 86usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_int_is_current (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["bitemporal_int_is_current".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_int_is_current",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 87usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_int_is_valid_at (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["bitemporal_int_is_valid_at".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_int_is_valid_at",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 88usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_int_le (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["bitemporal_int_le".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_int_le",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 89usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_int_len (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["bitemporal_int_len".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_int_len",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 90usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_int_lt (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["bitemporal_int_lt".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_int_lt",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 91usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_int_ne (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["bitemporal_int_ne".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_int_ne",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 92usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_int_over_after_ts (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["bitemporal_int_over_after_ts".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_int_over_after_ts",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 93usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_int_over_before_ts (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["bitemporal_int_over_before_ts".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_int_over_before_ts",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 94usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_int_sequence_memsize (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["bitemporal_int_sequence_memsize".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_int_sequence_memsize",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 95usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_int_sequence_new (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["bitemporal_int_sequence_new".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_int_sequence_new",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 96usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_int_start_value (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["bitemporal_int_start_value".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_int_start_value",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 97usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_int_to_ewkt (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["bitemporal_int_to_ewkt".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_int_to_ewkt",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 98usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_int_transaction_duration (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["bitemporal_int_transaction_duration".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_int_transaction_duration",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 99usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_int_transaction_extent (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["bitemporal_int_transaction_extent".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_int_transaction_extent",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 100usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_int_valid_at (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["bitemporal_int_valid_at".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_int_valid_at",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 101usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_int_valid_duration (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["bitemporal_int_valid_duration".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_int_valid_duration",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 102usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_int_valid_during_count (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["bitemporal_int_valid_during_count".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_int_valid_during_count",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 103usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_int_valid_extent (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["bitemporal_int_valid_extent".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_int_valid_extent",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 104usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_int_value_n (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["bitemporal_int_value_n".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_int_value_n",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 105usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_int_was_known_at (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["bitemporal_int_was_known_at".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_int_was_known_at",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 106usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_text_after_ts (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["bitemporal_text_after_ts".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_text_after_ts",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 107usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_text_as_of (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["bitemporal_text_as_of".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_text_as_of",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 108usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_text_before_ts (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["bitemporal_text_before_ts".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_text_before_ts",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 109usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_text_cmp (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["bitemporal_text_cmp".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_text_cmp",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 110usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_text_count_current (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["bitemporal_text_count_current".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_text_count_current",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 111usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_text_count_superseded (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["bitemporal_text_count_superseded".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_text_count_superseded",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 112usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_text_current (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["bitemporal_text_current".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_text_current",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 113usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_text_end_value (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["bitemporal_text_end_value".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_text_end_value",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 114usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_text_eq (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["bitemporal_text_eq".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_text_eq",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 115usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_text_ever_valid_during_count (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["bitemporal_text_ever_valid_during_count".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_text_ever_valid_during_count",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 116usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_text_from_ewkt (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["bitemporal_text_from_ewkt".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_text_from_ewkt",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 117usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_text_ge (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["bitemporal_text_ge".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_text_ge",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 118usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_text_gt (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["bitemporal_text_gt".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_text_gt",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 119usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_text_history_at_count (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["bitemporal_text_history_at_count".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_text_history_at_count",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 120usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg3".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg4".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_text_instant_new (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["bitemporal_text_instant_new".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_text_instant_new",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 121usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_text_le (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["bitemporal_text_le".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_text_le",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 122usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_text_len (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["bitemporal_text_len".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_text_len",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 123usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_text_lt (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["bitemporal_text_lt".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_text_lt",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 124usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_text_ne (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["bitemporal_text_ne".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_text_ne",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 125usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_text_over_after_ts (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["bitemporal_text_over_after_ts".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_text_over_after_ts",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 126usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_text_over_before_ts (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["bitemporal_text_over_before_ts".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_text_over_before_ts",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 127usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_text_sequence_memsize (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["bitemporal_text_sequence_memsize".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_text_sequence_memsize",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 128usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_text_sequence_new (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["bitemporal_text_sequence_new".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_text_sequence_new",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 129usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_text_start_value (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["bitemporal_text_start_value".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_text_start_value",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 130usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_text_to_ewkt (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["bitemporal_text_to_ewkt".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_text_to_ewkt",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 131usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_text_transaction_extent (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["bitemporal_text_transaction_extent".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_text_transaction_extent",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 132usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_text_valid_at (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["bitemporal_text_valid_at".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_text_valid_at",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 133usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_text_valid_during_count (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["bitemporal_text_valid_during_count".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_text_valid_during_count",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 134usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_text_valid_extent (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["bitemporal_text_valid_extent".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_text_valid_extent",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 135usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("bitemporal_text_value_n (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["bitemporal_text_value_n".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "bitemporal_text_value_n",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 136usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("date_set_cmp (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["date_set_cmp".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "date_set_cmp",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 137usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("date_set_contains (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["date_set_contains".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "date_set_contains",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 138usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("date_set_end_value (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["date_set_end_value".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "date_set_end_value",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 139usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("date_set_eq (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["date_set_eq".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "date_set_eq",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 140usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("date_set_from_text (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["date_set_from_text".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "date_set_from_text",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 141usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("date_set_ge (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["date_set_ge".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "date_set_ge",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 142usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("date_set_gt (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["date_set_gt".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "date_set_gt",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 143usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("date_set_intersection (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["date_set_intersection".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "date_set_intersection",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 144usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("date_set_le (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["date_set_le".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "date_set_le",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 145usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("date_set_len (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["date_set_len".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "date_set_len",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 146usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("date_set_lt (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["date_set_lt".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "date_set_lt",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 147usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("date_set_make (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["date_set_make".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "date_set_make",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 148usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("date_set_memsize (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["date_set_memsize".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "date_set_memsize",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 149usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("date_set_minus (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["date_set_minus".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "date_set_minus",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 150usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("date_set_ne (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["date_set_ne".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "date_set_ne",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 151usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("date_set_start_value (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["date_set_start_value".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "date_set_start_value",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 152usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("date_set_to_text (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["date_set_to_text".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "date_set_to_text",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 153usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("date_set_union (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["date_set_union".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "date_set_union",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 154usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("date_set_value_n (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["date_set_value_n".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "date_set_value_n",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 155usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("date_span_adjacent (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["date_span_adjacent".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "date_span_adjacent",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 156usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("date_span_after (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["date_span_after".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "date_span_after",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 157usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("date_span_after_value (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["date_span_after_value".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "date_span_after_value",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 158usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("date_span_before (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["date_span_before".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "date_span_before",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 159usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("date_span_before_value (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["date_span_before_value".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "date_span_before_value",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 160usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("date_span_bins (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["date_span_bins".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "date_span_bins",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 161usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("date_span_cmp (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["date_span_cmp".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "date_span_cmp",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 162usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("date_span_contains (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["date_span_contains".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "date_span_contains",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 163usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("date_span_contains_span (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["date_span_contains_span".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "date_span_contains_span",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 164usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("date_span_eq (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["date_span_eq".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "date_span_eq",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 165usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("date_span_expand (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["date_span_expand".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "date_span_expand",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 166usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("date_span_extent (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["date_span_extent".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "date_span_extent",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 167usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("date_span_from_text (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["date_span_from_text".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "date_span_from_text",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 168usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("date_span_ge (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["date_span_ge".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "date_span_ge",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 169usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("date_span_gt (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["date_span_gt".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "date_span_gt",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 170usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("date_span_intersection (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["date_span_intersection".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "date_span_intersection",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 171usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("date_span_le (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["date_span_le".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "date_span_le",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 172usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("date_span_lt (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["date_span_lt".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "date_span_lt",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 173usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Boolean },
                runtime::Funcarg { name: Some("arg3".into()), logical: Logicaltype::Boolean },
            ];
        let opts = runtime::Funcopts {
            description: Some("date_span_make (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["date_span_make".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "date_span_make",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 174usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("date_span_memsize (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["date_span_memsize".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "date_span_memsize",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 175usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("date_span_ne (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["date_span_ne".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "date_span_ne",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 176usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("date_span_over_after (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["date_span_over_after".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "date_span_over_after",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 177usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("date_span_over_after_value (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["date_span_over_after_value".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "date_span_over_after_value",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 178usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("date_span_over_before (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["date_span_over_before".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "date_span_over_before",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 179usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("date_span_over_before_value (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["date_span_over_before_value".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "date_span_over_before_value",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 180usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("date_span_overlaps (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["date_span_overlaps".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "date_span_overlaps",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 181usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("date_span_shift (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["date_span_shift".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "date_span_shift",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 182usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("date_span_to_text (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["date_span_to_text".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "date_span_to_text",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 183usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("date_span_union (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["date_span_union".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "date_span_union",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 184usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("date_span_width (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["date_span_width".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "date_span_width",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 185usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("date_spanset_after (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["date_spanset_after".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "date_spanset_after",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 186usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("date_spanset_after_value (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["date_spanset_after_value".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "date_spanset_after_value",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 187usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("date_spanset_before (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["date_spanset_before".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "date_spanset_before",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 188usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("date_spanset_before_value (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["date_spanset_before_value".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "date_spanset_before_value",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 189usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("date_spanset_bins (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["date_spanset_bins".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "date_spanset_bins",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 190usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("date_spanset_cmp (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["date_spanset_cmp".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "date_spanset_cmp",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 191usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("date_spanset_contains (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["date_spanset_contains".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "date_spanset_contains",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 192usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("date_spanset_end_span (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["date_spanset_end_span".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "date_spanset_end_span",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 193usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("date_spanset_eq (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["date_spanset_eq".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "date_spanset_eq",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 194usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("date_spanset_extent (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["date_spanset_extent".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "date_spanset_extent",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 195usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("date_spanset_from_text (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["date_spanset_from_text".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "date_spanset_from_text",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 196usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("date_spanset_ge (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["date_spanset_ge".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "date_spanset_ge",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 197usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("date_spanset_gt (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["date_spanset_gt".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "date_spanset_gt",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 198usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("date_spanset_intersection (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["date_spanset_intersection".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "date_spanset_intersection",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 199usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("date_spanset_le (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["date_spanset_le".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "date_spanset_le",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 200usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("date_spanset_lt (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["date_spanset_lt".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "date_spanset_lt",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 201usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("date_spanset_memsize (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["date_spanset_memsize".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "date_spanset_memsize",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 202usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("date_spanset_minus (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["date_spanset_minus".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "date_spanset_minus",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 203usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("date_spanset_ne (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["date_spanset_ne".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "date_spanset_ne",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 204usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("date_spanset_num_spans (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["date_spanset_num_spans".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "date_spanset_num_spans",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 205usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("date_spanset_over_after (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["date_spanset_over_after".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "date_spanset_over_after",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 206usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("date_spanset_over_after_value (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["date_spanset_over_after_value".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "date_spanset_over_after_value",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 207usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("date_spanset_over_before (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["date_spanset_over_before".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "date_spanset_over_before",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 208usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("date_spanset_over_before_value (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["date_spanset_over_before_value".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "date_spanset_over_before_value",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 209usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("date_spanset_overlaps (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["date_spanset_overlaps".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "date_spanset_overlaps",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 210usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("date_spanset_span_n (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["date_spanset_span_n".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "date_spanset_span_n",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 211usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("date_spanset_start_span (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["date_spanset_start_span".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "date_spanset_start_span",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 212usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("date_spanset_to_text (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["date_spanset_to_text".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "date_spanset_to_text",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 213usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("date_spanset_union (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["date_spanset_union".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "date_spanset_union",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 214usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("date_spanset_width (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["date_spanset_width".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "date_spanset_width",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 215usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("dateset_contains (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["dateset_contains".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "dateset_contains",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 216usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("dateset_gaps (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["dateset_gaps".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "dateset_gaps",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 217usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("dateset_intersection (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["dateset_intersection".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "dateset_intersection",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 218usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("dateset_len (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["dateset_len".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "dateset_len",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 219usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("dateset_make (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["dateset_make".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "dateset_make",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 220usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("dateset_minus (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["dateset_minus".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "dateset_minus",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 221usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("dateset_nth (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["dateset_nth".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "dateset_nth",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 222usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("dateset_shift (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["dateset_shift".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "dateset_shift",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 223usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("dateset_successor (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["dateset_successor".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "dateset_successor",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 224usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("dateset_to_span (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["dateset_to_span".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "dateset_to_span",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 225usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("dateset_to_text (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["dateset_to_text".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "dateset_to_text",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 226usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("dateset_union (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["dateset_union".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "dateset_union",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 227usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("datespan_adjacent (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["datespan_adjacent".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "datespan_adjacent",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 228usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("datespan_contains (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["datespan_contains".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "datespan_contains",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 229usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("datespan_contains_span (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["datespan_contains_span".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "datespan_contains_span",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 230usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("datespan_expand (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["datespan_expand".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "datespan_expand",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 231usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("datespan_from_text (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["datespan_from_text".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "datespan_from_text",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 232usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("datespan_intersection (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["datespan_intersection".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "datespan_intersection",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 233usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("datespan_lower (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["datespan_lower".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "datespan_lower",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 234usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Boolean },
                runtime::Funcarg { name: Some("arg3".into()), logical: Logicaltype::Boolean },
            ];
        let opts = runtime::Funcopts {
            description: Some("datespan_make (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["datespan_make".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "datespan_make",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 235usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("datespan_overlaps (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["datespan_overlaps".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "datespan_overlaps",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 236usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("datespan_shift (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["datespan_shift".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "datespan_shift",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 237usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("datespan_to_text (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["datespan_to_text".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "datespan_to_text",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 238usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("datespan_union (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["datespan_union".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "datespan_union",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 239usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("datespan_upper (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["datespan_upper".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "datespan_upper",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 240usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("datespan_width (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["datespan_width".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "datespan_width",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 241usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("datespanset_contains (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["datespanset_contains".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "datespanset_contains",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 242usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("datespanset_from_text (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["datespanset_from_text".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "datespanset_from_text",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 243usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("datespanset_intersection (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["datespanset_intersection".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "datespanset_intersection",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 244usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("datespanset_lower (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["datespanset_lower".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "datespanset_lower",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 245usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("datespanset_make (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["datespanset_make".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "datespanset_make",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 246usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("datespanset_minus (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["datespanset_minus".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "datespanset_minus",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 247usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("datespanset_num_spans (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["datespanset_num_spans".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "datespanset_num_spans",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 248usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("datespanset_overlaps (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["datespanset_overlaps".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "datespanset_overlaps",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 249usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("datespanset_to_text (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["datespanset_to_text".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "datespanset_to_text",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 250usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("datespanset_union (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["datespanset_union".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "datespanset_union",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 251usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("datespanset_upper (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["datespanset_upper".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "datespanset_upper",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 252usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("datespanset_width (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["datespanset_width".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "datespanset_width",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 253usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("float_round (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["float_round".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "float_round",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 254usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("float_set_cmp (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["float_set_cmp".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "float_set_cmp",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 255usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("float_set_contains (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["float_set_contains".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "float_set_contains",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 256usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("float_set_end_value (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["float_set_end_value".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "float_set_end_value",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 257usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("float_set_eq (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["float_set_eq".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "float_set_eq",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 258usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("float_set_from_text (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["float_set_from_text".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "float_set_from_text",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 259usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("float_set_ge (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["float_set_ge".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "float_set_ge",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 260usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("float_set_gt (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["float_set_gt".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "float_set_gt",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 261usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("float_set_intersection (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["float_set_intersection".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "float_set_intersection",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 262usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("float_set_le (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["float_set_le".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "float_set_le",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 263usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("float_set_len (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["float_set_len".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "float_set_len",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 264usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("float_set_lt (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["float_set_lt".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "float_set_lt",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 265usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("float_set_make (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["float_set_make".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "float_set_make",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 266usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("float_set_memsize (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["float_set_memsize".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "float_set_memsize",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 267usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("float_set_minus (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["float_set_minus".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "float_set_minus",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 268usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("float_set_ne (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["float_set_ne".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "float_set_ne",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 269usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("float_set_round (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["float_set_round".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "float_set_round",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 270usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("float_set_start_value (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["float_set_start_value".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "float_set_start_value",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 271usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("float_set_to_text (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["float_set_to_text".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "float_set_to_text",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 272usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("float_set_union (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["float_set_union".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "float_set_union",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 273usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("float_set_value_n (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["float_set_value_n".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "float_set_value_n",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 274usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("float_span_adjacent (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["float_span_adjacent".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "float_span_adjacent",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 275usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("float_span_after (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["float_span_after".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "float_span_after",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 276usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("float_span_after_value (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["float_span_after_value".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "float_span_after_value",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 277usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("float_span_before (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["float_span_before".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "float_span_before",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 278usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("float_span_before_value (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["float_span_before_value".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "float_span_before_value",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 279usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("float_span_bins (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["float_span_bins".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "float_span_bins",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 280usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("float_span_cmp (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["float_span_cmp".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "float_span_cmp",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 281usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("float_span_contains (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["float_span_contains".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "float_span_contains",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 282usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("float_span_contains_span (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["float_span_contains_span".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "float_span_contains_span",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 283usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("float_span_eq (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["float_span_eq".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "float_span_eq",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 284usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("float_span_expand (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["float_span_expand".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "float_span_expand",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 285usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("float_span_extent (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["float_span_extent".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "float_span_extent",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 286usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("float_span_from_text (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["float_span_from_text".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "float_span_from_text",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 287usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("float_span_ge (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["float_span_ge".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "float_span_ge",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 288usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("float_span_gt (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["float_span_gt".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "float_span_gt",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 289usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("float_span_intersection (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["float_span_intersection".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "float_span_intersection",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 290usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("float_span_le (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["float_span_le".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "float_span_le",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 291usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("float_span_lt (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["float_span_lt".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "float_span_lt",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 292usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Boolean },
                runtime::Funcarg { name: Some("arg3".into()), logical: Logicaltype::Boolean },
            ];
        let opts = runtime::Funcopts {
            description: Some("float_span_make (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["float_span_make".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "float_span_make",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 293usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("float_span_memsize (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["float_span_memsize".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "float_span_memsize",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 294usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("float_span_ne (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["float_span_ne".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "float_span_ne",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 295usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("float_span_over_after (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["float_span_over_after".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "float_span_over_after",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 296usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("float_span_over_after_value (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["float_span_over_after_value".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "float_span_over_after_value",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 297usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("float_span_over_before (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["float_span_over_before".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "float_span_over_before",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 298usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("float_span_over_before_value (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["float_span_over_before_value".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "float_span_over_before_value",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 299usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("float_span_overlaps (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["float_span_overlaps".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "float_span_overlaps",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 300usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("float_span_round (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["float_span_round".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "float_span_round",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 301usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("float_span_shift (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["float_span_shift".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "float_span_shift",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 302usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("float_span_to_text (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["float_span_to_text".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "float_span_to_text",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 303usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("float_span_union (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["float_span_union".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "float_span_union",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 304usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("float_span_width (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["float_span_width".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "float_span_width",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 305usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("float_spanset_after (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["float_spanset_after".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "float_spanset_after",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 306usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("float_spanset_after_value (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["float_spanset_after_value".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "float_spanset_after_value",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 307usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("float_spanset_before (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["float_spanset_before".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "float_spanset_before",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 308usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("float_spanset_before_value (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["float_spanset_before_value".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "float_spanset_before_value",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 309usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("float_spanset_bins (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["float_spanset_bins".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "float_spanset_bins",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 310usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("float_spanset_cmp (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["float_spanset_cmp".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "float_spanset_cmp",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 311usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("float_spanset_contains (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["float_spanset_contains".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "float_spanset_contains",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 312usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("float_spanset_end_span (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["float_spanset_end_span".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "float_spanset_end_span",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 313usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("float_spanset_eq (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["float_spanset_eq".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "float_spanset_eq",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 314usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("float_spanset_extent (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["float_spanset_extent".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "float_spanset_extent",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 315usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("float_spanset_from_text (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["float_spanset_from_text".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "float_spanset_from_text",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 316usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("float_spanset_ge (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["float_spanset_ge".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "float_spanset_ge",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 317usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("float_spanset_gt (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["float_spanset_gt".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "float_spanset_gt",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 318usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("float_spanset_intersection (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["float_spanset_intersection".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "float_spanset_intersection",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 319usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("float_spanset_le (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["float_spanset_le".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "float_spanset_le",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 320usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("float_spanset_lt (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["float_spanset_lt".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "float_spanset_lt",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 321usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("float_spanset_memsize (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["float_spanset_memsize".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "float_spanset_memsize",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 322usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("float_spanset_minus (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["float_spanset_minus".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "float_spanset_minus",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 323usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("float_spanset_ne (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["float_spanset_ne".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "float_spanset_ne",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 324usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("float_spanset_num_spans (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["float_spanset_num_spans".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "float_spanset_num_spans",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 325usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("float_spanset_over_after (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["float_spanset_over_after".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "float_spanset_over_after",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 326usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("float_spanset_over_after_value (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["float_spanset_over_after_value".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "float_spanset_over_after_value",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 327usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("float_spanset_over_before (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["float_spanset_over_before".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "float_spanset_over_before",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 328usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("float_spanset_over_before_value (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["float_spanset_over_before_value".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "float_spanset_over_before_value",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 329usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("float_spanset_overlaps (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["float_spanset_overlaps".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "float_spanset_overlaps",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 330usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("float_spanset_round (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["float_spanset_round".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "float_spanset_round",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 331usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("float_spanset_span_n (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["float_spanset_span_n".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "float_spanset_span_n",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 332usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("float_spanset_start_span (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["float_spanset_start_span".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "float_spanset_start_span",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 333usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("float_spanset_to_text (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["float_spanset_to_text".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "float_spanset_to_text",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 334usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("float_spanset_union (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["float_spanset_union".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "float_spanset_union",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 335usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("float_spanset_width (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["float_spanset_width".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "float_spanset_width",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 336usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("floatset_contains (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["floatset_contains".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "floatset_contains",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 337usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("floatset_gaps (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["floatset_gaps".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "floatset_gaps",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 338usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("floatset_intersection (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["floatset_intersection".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "floatset_intersection",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 339usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("floatset_is_disjoint (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["floatset_is_disjoint".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "floatset_is_disjoint",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 340usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("floatset_is_subset (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["floatset_is_subset".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "floatset_is_subset",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 341usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("floatset_len (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["floatset_len".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "floatset_len",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 342usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("floatset_make (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["floatset_make".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "floatset_make",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 343usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("floatset_minus (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["floatset_minus".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "floatset_minus",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 344usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("floatset_nth (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["floatset_nth".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "floatset_nth",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 345usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("floatset_scale (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["floatset_scale".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "floatset_scale",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 346usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("floatset_shift (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["floatset_shift".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "floatset_shift",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 347usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("floatset_to_span (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["floatset_to_span".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "floatset_to_span",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 348usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("floatset_to_text (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["floatset_to_text".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "floatset_to_text",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 349usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("floatset_union (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["floatset_union".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "floatset_union",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 350usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("floatspan_adjacent (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["floatspan_adjacent".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "floatspan_adjacent",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 351usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("floatspan_bucket_list (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["floatspan_bucket_list".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "floatspan_bucket_list",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 352usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("floatspan_contains (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["floatspan_contains".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "floatspan_contains",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 353usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("floatspan_contains_span (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["floatspan_contains_span".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "floatspan_contains_span",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 354usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("floatspan_expand (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["floatspan_expand".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "floatspan_expand",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 355usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("floatspan_from_text (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["floatspan_from_text".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "floatspan_from_text",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 356usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("floatspan_intersection (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["floatspan_intersection".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "floatspan_intersection",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 357usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("floatspan_lower (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["floatspan_lower".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "floatspan_lower",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 358usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Boolean },
                runtime::Funcarg { name: Some("arg3".into()), logical: Logicaltype::Boolean },
            ];
        let opts = runtime::Funcopts {
            description: Some("floatspan_make (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["floatspan_make".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "floatspan_make",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 359usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("floatspan_midpoint (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["floatspan_midpoint".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "floatspan_midpoint",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 360usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("floatspan_minus (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["floatspan_minus".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "floatspan_minus",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 361usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("floatspan_overlaps (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["floatspan_overlaps".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "floatspan_overlaps",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 362usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("floatspan_scale (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["floatspan_scale".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "floatspan_scale",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 363usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("floatspan_shift (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["floatspan_shift".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "floatspan_shift",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 364usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("floatspan_to_text (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["floatspan_to_text".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "floatspan_to_text",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 365usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("floatspan_union (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["floatspan_union".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "floatspan_union",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 366usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("floatspan_upper (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["floatspan_upper".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "floatspan_upper",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 367usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("floatspan_width (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["floatspan_width".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "floatspan_width",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 368usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("floatspanset_contains (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["floatspanset_contains".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "floatspanset_contains",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 369usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("floatspanset_from_text (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["floatspanset_from_text".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "floatspanset_from_text",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 370usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("floatspanset_intersection (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["floatspanset_intersection".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "floatspanset_intersection",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 371usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("floatspanset_minus (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["floatspanset_minus".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "floatspanset_minus",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 372usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("floatspanset_num_spans (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["floatspanset_num_spans".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "floatspanset_num_spans",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 373usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("floatspanset_overlaps (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["floatspanset_overlaps".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "floatspanset_overlaps",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 374usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("floatspanset_to_text (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["floatspanset_to_text".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "floatspanset_to_text",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 375usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("floatspanset_union (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["floatspanset_union".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "floatspanset_union",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 376usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("floatspanset_width (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["floatspanset_width".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "floatspanset_width",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 377usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("int_set_cmp (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["int_set_cmp".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "int_set_cmp",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 378usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("int_set_contains (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["int_set_contains".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "int_set_contains",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 379usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("int_set_end_value (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["int_set_end_value".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "int_set_end_value",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 380usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("int_set_eq (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["int_set_eq".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "int_set_eq",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 381usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("int_set_from_text (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["int_set_from_text".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "int_set_from_text",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 382usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("int_set_ge (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["int_set_ge".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "int_set_ge",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 383usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("int_set_gt (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["int_set_gt".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "int_set_gt",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 384usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("int_set_intersection (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["int_set_intersection".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "int_set_intersection",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 385usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("int_set_le (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["int_set_le".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "int_set_le",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 386usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("int_set_len (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["int_set_len".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "int_set_len",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 387usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("int_set_lt (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["int_set_lt".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "int_set_lt",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 388usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("int_set_make (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["int_set_make".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "int_set_make",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 389usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("int_set_memsize (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["int_set_memsize".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "int_set_memsize",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 390usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("int_set_minus (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["int_set_minus".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "int_set_minus",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 391usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("int_set_ne (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["int_set_ne".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "int_set_ne",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 392usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("int_set_start_value (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["int_set_start_value".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "int_set_start_value",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 393usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("int_set_to_text (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["int_set_to_text".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "int_set_to_text",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 394usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("int_set_union (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["int_set_union".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "int_set_union",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 395usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("int_set_value_n (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["int_set_value_n".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "int_set_value_n",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 396usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("int_span_adjacent (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["int_span_adjacent".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "int_span_adjacent",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 397usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("int_span_after (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["int_span_after".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "int_span_after",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 398usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("int_span_after_value (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["int_span_after_value".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "int_span_after_value",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 399usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("int_span_before (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["int_span_before".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "int_span_before",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 400usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("int_span_before_value (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["int_span_before_value".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "int_span_before_value",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 401usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("int_span_bins (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["int_span_bins".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "int_span_bins",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 402usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("int_span_cmp (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["int_span_cmp".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "int_span_cmp",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 403usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("int_span_contains (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["int_span_contains".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "int_span_contains",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 404usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("int_span_contains_span (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["int_span_contains_span".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "int_span_contains_span",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 405usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("int_span_eq (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["int_span_eq".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "int_span_eq",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 406usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("int_span_expand (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["int_span_expand".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "int_span_expand",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 407usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("int_span_extent (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["int_span_extent".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "int_span_extent",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 408usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("int_span_from_text (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["int_span_from_text".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "int_span_from_text",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 409usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("int_span_ge (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["int_span_ge".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "int_span_ge",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 410usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("int_span_gt (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["int_span_gt".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "int_span_gt",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 411usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("int_span_intersection (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["int_span_intersection".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "int_span_intersection",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 412usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("int_span_le (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["int_span_le".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "int_span_le",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 413usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("int_span_lt (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["int_span_lt".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "int_span_lt",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 414usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Boolean },
                runtime::Funcarg { name: Some("arg3".into()), logical: Logicaltype::Boolean },
            ];
        let opts = runtime::Funcopts {
            description: Some("int_span_make (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["int_span_make".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "int_span_make",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 415usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("int_span_memsize (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["int_span_memsize".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "int_span_memsize",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 416usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("int_span_ne (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["int_span_ne".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "int_span_ne",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 417usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("int_span_over_after (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["int_span_over_after".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "int_span_over_after",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 418usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("int_span_over_after_value (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["int_span_over_after_value".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "int_span_over_after_value",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 419usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("int_span_over_before (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["int_span_over_before".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "int_span_over_before",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 420usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("int_span_over_before_value (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["int_span_over_before_value".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "int_span_over_before_value",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 421usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("int_span_overlaps (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["int_span_overlaps".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "int_span_overlaps",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 422usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("int_span_shift (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["int_span_shift".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "int_span_shift",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 423usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("int_span_to_text (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["int_span_to_text".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "int_span_to_text",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 424usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("int_span_union (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["int_span_union".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "int_span_union",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 425usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("int_span_width (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["int_span_width".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "int_span_width",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 426usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("int_spanset_after (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["int_spanset_after".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "int_spanset_after",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 427usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("int_spanset_after_value (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["int_spanset_after_value".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "int_spanset_after_value",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 428usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("int_spanset_before (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["int_spanset_before".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "int_spanset_before",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 429usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("int_spanset_before_value (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["int_spanset_before_value".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "int_spanset_before_value",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 430usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("int_spanset_bins (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["int_spanset_bins".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "int_spanset_bins",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 431usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("int_spanset_cmp (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["int_spanset_cmp".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "int_spanset_cmp",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 432usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("int_spanset_contains (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["int_spanset_contains".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "int_spanset_contains",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 433usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("int_spanset_end_span (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["int_spanset_end_span".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "int_spanset_end_span",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 434usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("int_spanset_eq (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["int_spanset_eq".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "int_spanset_eq",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 435usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("int_spanset_extent (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["int_spanset_extent".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "int_spanset_extent",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 436usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("int_spanset_from_text (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["int_spanset_from_text".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "int_spanset_from_text",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 437usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("int_spanset_ge (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["int_spanset_ge".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "int_spanset_ge",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 438usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("int_spanset_gt (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["int_spanset_gt".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "int_spanset_gt",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 439usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("int_spanset_intersection (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["int_spanset_intersection".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "int_spanset_intersection",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 440usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("int_spanset_le (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["int_spanset_le".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "int_spanset_le",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 441usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("int_spanset_lt (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["int_spanset_lt".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "int_spanset_lt",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 442usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("int_spanset_memsize (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["int_spanset_memsize".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "int_spanset_memsize",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 443usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("int_spanset_minus (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["int_spanset_minus".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "int_spanset_minus",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 444usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("int_spanset_ne (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["int_spanset_ne".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "int_spanset_ne",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 445usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("int_spanset_num_spans (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["int_spanset_num_spans".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "int_spanset_num_spans",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 446usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("int_spanset_over_after (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["int_spanset_over_after".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "int_spanset_over_after",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 447usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("int_spanset_over_after_value (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["int_spanset_over_after_value".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "int_spanset_over_after_value",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 448usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("int_spanset_over_before (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["int_spanset_over_before".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "int_spanset_over_before",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 449usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("int_spanset_over_before_value (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["int_spanset_over_before_value".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "int_spanset_over_before_value",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 450usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("int_spanset_overlaps (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["int_spanset_overlaps".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "int_spanset_overlaps",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 451usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("int_spanset_span_n (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["int_spanset_span_n".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "int_spanset_span_n",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 452usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("int_spanset_start_span (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["int_spanset_start_span".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "int_spanset_start_span",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 453usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("int_spanset_to_text (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["int_spanset_to_text".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "int_spanset_to_text",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 454usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("int_spanset_union (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["int_spanset_union".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "int_spanset_union",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 455usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("int_spanset_width (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["int_spanset_width".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "int_spanset_width",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 456usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("intset_contains (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["intset_contains".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "intset_contains",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 457usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("intset_gaps (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["intset_gaps".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "intset_gaps",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 458usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("intset_intersection (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["intset_intersection".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "intset_intersection",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 459usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("intset_is_disjoint (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["intset_is_disjoint".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "intset_is_disjoint",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 460usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("intset_is_subset (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["intset_is_subset".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "intset_is_subset",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 461usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("intset_len (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["intset_len".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "intset_len",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 462usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("intset_make (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["intset_make".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "intset_make",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 463usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("intset_minus (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["intset_minus".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "intset_minus",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 464usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("intset_nth (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["intset_nth".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "intset_nth",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 465usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("intset_scale (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["intset_scale".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "intset_scale",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 466usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("intset_shift (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["intset_shift".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "intset_shift",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 467usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("intset_to_span (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["intset_to_span".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "intset_to_span",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 468usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("intset_to_text (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["intset_to_text".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "intset_to_text",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 469usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("intset_union (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["intset_union".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "intset_union",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 470usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("intspan_adjacent (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["intspan_adjacent".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "intspan_adjacent",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 471usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("intspan_bucket_list (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["intspan_bucket_list".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "intspan_bucket_list",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 472usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("intspan_contains (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["intspan_contains".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "intspan_contains",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 473usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("intspan_contains_span (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["intspan_contains_span".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "intspan_contains_span",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 474usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("intspan_expand (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["intspan_expand".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "intspan_expand",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 475usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("intspan_from_text (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["intspan_from_text".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "intspan_from_text",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 476usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("intspan_intersection (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["intspan_intersection".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "intspan_intersection",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 477usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("intspan_lower (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["intspan_lower".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "intspan_lower",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 478usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Boolean },
                runtime::Funcarg { name: Some("arg3".into()), logical: Logicaltype::Boolean },
            ];
        let opts = runtime::Funcopts {
            description: Some("intspan_make (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["intspan_make".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "intspan_make",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 479usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("intspan_minus (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["intspan_minus".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "intspan_minus",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 480usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("intspan_overlaps (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["intspan_overlaps".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "intspan_overlaps",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 481usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("intspan_scale (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["intspan_scale".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "intspan_scale",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 482usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("intspan_shift (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["intspan_shift".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "intspan_shift",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 483usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("intspan_to_text (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["intspan_to_text".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "intspan_to_text",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 484usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("intspan_union (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["intspan_union".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "intspan_union",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 485usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("intspan_upper (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["intspan_upper".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "intspan_upper",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 486usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("intspan_width (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["intspan_width".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "intspan_width",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 487usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("intspanset_contains (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["intspanset_contains".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "intspanset_contains",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 488usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("intspanset_from_text (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["intspanset_from_text".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "intspanset_from_text",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 489usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("intspanset_intersection (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["intspanset_intersection".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "intspanset_intersection",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 490usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("intspanset_minus (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["intspanset_minus".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "intspanset_minus",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 491usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("intspanset_num_spans (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["intspanset_num_spans".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "intspanset_num_spans",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 492usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("intspanset_overlaps (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["intspanset_overlaps".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "intspanset_overlaps",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 493usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("intspanset_to_text (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["intspanset_to_text".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "intspanset_to_text",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 494usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("intspanset_union (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["intspanset_union".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "intspanset_union",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 495usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("intspanset_width (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["intspanset_width".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "intspanset_width",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 496usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("jsonb_array_append (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["jsonb_array_append".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "jsonb_array_append",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 497usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("jsonb_array_elements (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["jsonb_array_elements".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "jsonb_array_elements",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 498usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("jsonb_array_length (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["jsonb_array_length".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "jsonb_array_length",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 499usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("jsonb_array_prepend (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["jsonb_array_prepend".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "jsonb_array_prepend",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 500usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("jsonb_canonicalize (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["jsonb_canonicalize".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "jsonb_canonicalize",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 501usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("jsonb_concat (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["jsonb_concat".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "jsonb_concat",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 502usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("jsonb_contained_in (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["jsonb_contained_in".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "jsonb_contained_in",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 503usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("jsonb_contains (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["jsonb_contains".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "jsonb_contains",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 504usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("jsonb_contains_all_keys (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["jsonb_contains_all_keys".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "jsonb_contains_all_keys",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 505usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("jsonb_contains_any_key (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["jsonb_contains_any_key".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "jsonb_contains_any_key",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 506usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("jsonb_contains_key (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["jsonb_contains_key".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "jsonb_contains_key",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 507usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("jsonb_delete_key (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["jsonb_delete_key".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "jsonb_delete_key",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 508usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("jsonb_delete_path (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["jsonb_delete_path".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "jsonb_delete_path",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 509usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("jsonb_equal (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["jsonb_equal".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "jsonb_equal",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 510usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("jsonb_from_string (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["jsonb_from_string".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "jsonb_from_string",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 511usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("jsonb_get_element (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["jsonb_get_element".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "jsonb_get_element",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 512usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("jsonb_get_key (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["jsonb_get_key".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "jsonb_get_key",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 513usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("jsonb_get_path (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["jsonb_get_path".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "jsonb_get_path",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 514usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("jsonb_is_array (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["jsonb_is_array".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "jsonb_is_array",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 515usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("jsonb_is_null (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["jsonb_is_null".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "jsonb_is_null",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 516usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("jsonb_is_object (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["jsonb_is_object".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "jsonb_is_object",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 517usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("jsonb_is_scalar (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["jsonb_is_scalar".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "jsonb_is_scalar",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 518usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("jsonb_merge_deep (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["jsonb_merge_deep".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "jsonb_merge_deep",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 519usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("jsonb_object_keys (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["jsonb_object_keys".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "jsonb_object_keys",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 520usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("jsonb_object_length (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["jsonb_object_length".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "jsonb_object_length",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 521usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("jsonb_parse (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["jsonb_parse".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "jsonb_parse",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 522usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("jsonb_path_exists (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["jsonb_path_exists".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "jsonb_path_exists",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 523usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("jsonb_path_join (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["jsonb_path_join".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "jsonb_path_join",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 524usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("jsonb_path_split (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["jsonb_path_split".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "jsonb_path_split",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 525usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("jsonb_pretty (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["jsonb_pretty".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "jsonb_pretty",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 526usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("jsonb_set_key (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["jsonb_set_key".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "jsonb_set_key",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 527usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("jsonb_set_path (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["jsonb_set_path".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "jsonb_set_path",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 528usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("jsonb_strip_nulls (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["jsonb_strip_nulls".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "jsonb_strip_nulls",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 529usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("jsonb_typeof (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["jsonb_typeof".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "jsonb_typeof",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 530usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbigint_abs (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbigint_abs".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbigint_abs",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 531usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbigint_add (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbigint_add".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbigint_add",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 532usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbigint_add_scalar (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbigint_add_scalar".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbigint_add_scalar",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 533usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbigint_always_eq (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbigint_always_eq".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbigint_always_eq",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 534usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbigint_always_gt (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbigint_always_gt".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbigint_always_gt",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 535usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbigint_always_lt (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbigint_always_lt".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbigint_always_lt",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 536usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbigint_append_instant (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbigint_append_instant".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbigint_append_instant",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 537usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbigint_append_sequence (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbigint_append_sequence".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbigint_append_sequence",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 538usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbigint_at_max (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbigint_at_max".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbigint_at_max",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 539usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbigint_at_min (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbigint_at_min".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbigint_at_min",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 540usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbigint_at_period (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbigint_at_period".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbigint_at_period",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 541usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbigint_at_range (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbigint_at_range".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbigint_at_range",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 542usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbigint_at_value (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbigint_at_value".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbigint_at_value",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 543usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbigint_at_values (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbigint_at_values".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbigint_at_values",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 544usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbigint_diff (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbigint_diff".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbigint_diff",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 545usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbigint_div (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbigint_div".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbigint_div",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 546usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbigint_div_scalar (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbigint_div_scalar".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbigint_div_scalar",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 547usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbigint_duration (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbigint_duration".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbigint_duration",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 548usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbigint_end_instant (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbigint_end_instant".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbigint_end_instant",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 549usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbigint_end_timestamp (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbigint_end_timestamp".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbigint_end_timestamp",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 550usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbigint_end_value (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbigint_end_value".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbigint_end_value",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 551usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbigint_ever_eq (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbigint_ever_eq".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbigint_ever_eq",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 552usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbigint_ever_gt (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbigint_ever_gt".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbigint_ever_gt",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 553usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbigint_ever_lt (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbigint_ever_lt".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbigint_ever_lt",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 554usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbigint_exponential_smooth (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbigint_exponential_smooth".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbigint_exponential_smooth",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 555usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbigint_from_csv (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbigint_from_csv".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbigint_from_csv",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 556usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbigint_from_wkb (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbigint_from_wkb".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbigint_from_wkb",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 557usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbigint_hash (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbigint_hash".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbigint_hash",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 558usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbigint_instant_from_ewkt (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbigint_instant_from_ewkt".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbigint_instant_from_ewkt",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 559usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbigint_instant_from_mfjson (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbigint_instant_from_mfjson".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbigint_instant_from_mfjson",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 560usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbigint_instant_from_text (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbigint_instant_from_text".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbigint_instant_from_text",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 561usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbigint_instant_n (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbigint_instant_n".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbigint_instant_n",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 562usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbigint_instant_new (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbigint_instant_new".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbigint_instant_new",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 563usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbigint_instant_to_mfjson (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbigint_instant_to_mfjson".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbigint_instant_to_mfjson",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 564usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbigint_instant_to_text (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbigint_instant_to_text".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbigint_instant_to_text",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 565usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbigint_instant_to_tfloat (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbigint_instant_to_tfloat".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbigint_instant_to_tfloat",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 566usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbigint_instant_to_tint (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbigint_instant_to_tint".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbigint_instant_to_tint",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 567usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbigint_instants (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbigint_instants".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbigint_instants",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 568usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbigint_integral (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbigint_integral".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbigint_integral",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 569usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbigint_kurtosis (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbigint_kurtosis".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbigint_kurtosis",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 570usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbigint_lift_between (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbigint_lift_between".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbigint_lift_between",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 571usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbigint_lift_eq (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbigint_lift_eq".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbigint_lift_eq",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 572usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbigint_lift_ge (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbigint_lift_ge".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbigint_lift_ge",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 573usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbigint_lift_gt (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbigint_lift_gt".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbigint_lift_gt",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 574usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbigint_lift_le (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbigint_lift_le".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbigint_lift_le",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 575usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbigint_lift_lt (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbigint_lift_lt".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbigint_lift_lt",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 576usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbigint_lift_ne (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbigint_lift_ne".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbigint_lift_ne",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 577usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbigint_lower_inclusive (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbigint_lower_inclusive".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbigint_lower_inclusive",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 578usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbigint_max_value (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbigint_max_value".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbigint_max_value",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 579usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbigint_median (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbigint_median".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbigint_median",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 580usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbigint_memsize (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbigint_memsize".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbigint_memsize",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 581usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbigint_merge (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbigint_merge".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbigint_merge",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 582usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbigint_min_value (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbigint_min_value".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbigint_min_value",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 583usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbigint_minus_period (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbigint_minus_period".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbigint_minus_period",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 584usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbigint_minus_range (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbigint_minus_range".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbigint_minus_range",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 585usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbigint_minus_value (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbigint_minus_value".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbigint_minus_value",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 586usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbigint_minus_values (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbigint_minus_values".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbigint_minus_values",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 587usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbigint_mod (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbigint_mod".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbigint_mod",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 588usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbigint_mod_scalar (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbigint_mod_scalar".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbigint_mod_scalar",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 589usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbigint_mode (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbigint_mode".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbigint_mode",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 590usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbigint_moving_average (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbigint_moving_average".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbigint_moving_average",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 591usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbigint_mul (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbigint_mul".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbigint_mul",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 592usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbigint_mul_scalar (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbigint_mul_scalar".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbigint_mul_scalar",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 593usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbigint_negate (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbigint_negate".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbigint_negate",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 594usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbigint_num_anomalies_iqr (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbigint_num_anomalies_iqr".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbigint_num_anomalies_iqr",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 595usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbigint_num_anomalies_zscore (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbigint_num_anomalies_zscore".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbigint_num_anomalies_zscore",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 596usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbigint_num_instants (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbigint_num_instants".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbigint_num_instants",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 597usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbigint_num_timestamps (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbigint_num_timestamps".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbigint_num_timestamps",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 598usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbigint_quantile (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbigint_quantile".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbigint_quantile",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 599usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbigint_quantiles (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbigint_quantiles".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbigint_quantiles",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 600usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbigint_rolling_mean (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbigint_rolling_mean".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbigint_rolling_mean",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 601usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbigint_rolling_stddev (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbigint_rolling_stddev".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbigint_rolling_stddev",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 602usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbigint_rolling_variance (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbigint_rolling_variance".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbigint_rolling_variance",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 603usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbigint_sequence_from_ewkt (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbigint_sequence_from_ewkt".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbigint_sequence_from_ewkt",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 604usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbigint_sequence_from_mfjson (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbigint_sequence_from_mfjson".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbigint_sequence_from_mfjson",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 605usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbigint_sequence_from_pairs (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbigint_sequence_from_pairs".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbigint_sequence_from_pairs",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 606usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbigint_sequence_from_text (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbigint_sequence_from_text".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbigint_sequence_from_text",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 607usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbigint_sequence_new (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbigint_sequence_new".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbigint_sequence_new",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 608usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbigint_sequence_to_mfjson (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbigint_sequence_to_mfjson".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbigint_sequence_to_mfjson",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 609usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbigint_sequence_to_text (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbigint_sequence_to_text".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbigint_sequence_to_text",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 610usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbigint_sequence_to_tfloat (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbigint_sequence_to_tfloat".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbigint_sequence_to_tfloat",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 611usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbigint_skewness (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbigint_skewness".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbigint_skewness",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 612usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbigint_start_instant (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbigint_start_instant".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbigint_start_instant",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 613usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbigint_start_timestamp (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbigint_start_timestamp".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbigint_start_timestamp",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 614usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbigint_start_value (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbigint_start_value".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbigint_start_value",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 615usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbigint_sub (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbigint_sub".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbigint_sub",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 616usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbigint_sub_scalar (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbigint_sub_scalar".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbigint_sub_scalar",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 617usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbigint_temporal_avg (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbigint_temporal_avg".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbigint_temporal_avg",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 618usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbigint_temporal_count (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbigint_temporal_count".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbigint_temporal_count",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 619usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbigint_temporal_eq (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbigint_temporal_eq".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbigint_temporal_eq",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 620usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbigint_temporal_extent (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbigint_temporal_extent".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbigint_temporal_extent",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 621usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbigint_temporal_ge (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbigint_temporal_ge".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbigint_temporal_ge",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 622usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbigint_temporal_gt (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbigint_temporal_gt".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbigint_temporal_gt",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 623usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbigint_temporal_le (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbigint_temporal_le".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbigint_temporal_le",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 624usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbigint_temporal_lt (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbigint_temporal_lt".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbigint_temporal_lt",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 625usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbigint_temporal_max (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbigint_temporal_max".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbigint_temporal_max",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 626usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbigint_temporal_min (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbigint_temporal_min".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbigint_temporal_min",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 627usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbigint_temporal_mode (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbigint_temporal_mode".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbigint_temporal_mode",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 628usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbigint_temporal_ne (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbigint_temporal_ne".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbigint_temporal_ne",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 629usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbigint_temporal_range (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbigint_temporal_range".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbigint_temporal_range",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 630usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbigint_temporal_sum (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbigint_temporal_sum".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbigint_temporal_sum",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 631usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbigint_time_span (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbigint_time_span".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbigint_time_span",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 632usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbigint_timestamp_n (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbigint_timestamp_n".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbigint_timestamp_n",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 633usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbigint_timestamps (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbigint_timestamps".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbigint_timestamps",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 634usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbigint_to_csv (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbigint_to_csv".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbigint_to_csv",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 635usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbigint_to_json (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbigint_to_json".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbigint_to_json",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 636usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbigint_to_tint (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbigint_to_tint".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbigint_to_tint",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 637usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbigint_to_wkb (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbigint_to_wkb".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbigint_to_wkb",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 638usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbigint_twavg (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbigint_twavg".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbigint_twavg",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 639usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbigint_twcount (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbigint_twcount".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbigint_twcount",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 640usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbigint_twmax (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbigint_twmax".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbigint_twmax",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 641usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbigint_twmin (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbigint_twmin".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbigint_twmin",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 642usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbigint_twstddev (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbigint_twstddev".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbigint_twstddev",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 643usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbigint_twsum (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbigint_twsum".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbigint_twsum",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 644usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbigint_twvariance (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbigint_twvariance".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbigint_twvariance",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 645usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbigint_upper_inclusive (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbigint_upper_inclusive".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbigint_upper_inclusive",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 646usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbigint_upsample_to_target (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbigint_upsample_to_target".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbigint_upsample_to_target",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 647usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbigint_value_at (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbigint_value_at".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbigint_value_at",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 648usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbigint_value_bucket_index (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbigint_value_bucket_index".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbigint_value_bucket_index",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 649usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbigint_value_bucket_split (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbigint_value_bucket_split".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbigint_value_bucket_split",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 650usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbigint_value_extent (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbigint_value_extent".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbigint_value_extent",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 651usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbigint_value_n (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbigint_value_n".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbigint_value_n",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 652usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbigint_values (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbigint_values".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbigint_values",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 653usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbigint_wavg (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbigint_wavg".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbigint_wavg",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 654usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbigint_wcount (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbigint_wcount".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbigint_wcount",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 655usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbigint_when_eq (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbigint_when_eq".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbigint_when_eq",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 656usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbigint_when_ge (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbigint_when_ge".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbigint_when_ge",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 657usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbigint_when_gt (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbigint_when_gt".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbigint_when_gt",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 658usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbigint_when_le (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbigint_when_le".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbigint_when_le",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 659usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbigint_when_lt (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbigint_when_lt".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbigint_when_lt",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 660usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbigint_when_ne (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbigint_when_ne".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbigint_when_ne",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 661usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbigint_wmax (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbigint_wmax".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbigint_wmax",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 662usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbigint_wmin (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbigint_wmin".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbigint_wmin",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 663usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbigint_wsum (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbigint_wsum".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbigint_wsum",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 664usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbool_after_ts (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbool_after_ts".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbool_after_ts",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 665usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbool_always_false (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbool_always_false".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbool_always_false",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 666usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbool_always_true (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbool_always_true".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbool_always_true",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 667usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbool_and (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbool_and".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbool_and",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 668usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbool_and_all (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbool_and_all".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbool_and_all",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 669usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbool_append_instant (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbool_append_instant".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbool_append_instant",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 670usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbool_append_sequence (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbool_append_sequence".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbool_append_sequence",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 671usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbool_at_false (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbool_at_false".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbool_at_false",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 672usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbool_at_period (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbool_at_period".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbool_at_period",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 673usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbool_at_true (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbool_at_true".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbool_at_true",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 674usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Boolean },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbool_at_value (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbool_at_value".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbool_at_value",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 675usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbool_before_ts (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbool_before_ts".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbool_before_ts",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 676usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbool_count_changes (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbool_count_changes".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbool_count_changes",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 677usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbool_count_false (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbool_count_false".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbool_count_false",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 678usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbool_count_true (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbool_count_true".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbool_count_true",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 679usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbool_duration (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbool_duration".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbool_duration",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 680usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbool_end_timestamp (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbool_end_timestamp".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbool_end_timestamp",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 681usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbool_end_value (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbool_end_value".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbool_end_value",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 682usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbool_ever_false (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbool_ever_false".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbool_ever_false",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 683usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbool_ever_true (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbool_ever_true".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbool_ever_true",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 684usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbool_from_csv (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbool_from_csv".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbool_from_csv",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 685usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbool_from_ewkt (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbool_from_ewkt".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbool_from_ewkt",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 686usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbool_instant_from_ewkt (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbool_instant_from_ewkt".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbool_instant_from_ewkt",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 687usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbool_instant_memsize (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbool_instant_memsize".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbool_instant_memsize",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 688usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Boolean },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbool_instant_new (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbool_instant_new".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbool_instant_new",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 689usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbool_instants (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbool_instants".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbool_instants",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 690usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbool_merge (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbool_merge".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbool_merge",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 691usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbool_minus_period (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbool_minus_period".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbool_minus_period",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 692usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Boolean },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbool_minus_value (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbool_minus_value".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbool_minus_value",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 693usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbool_not (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbool_not".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbool_not",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 694usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbool_num_instants (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbool_num_instants".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbool_num_instants",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 695usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbool_or (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbool_or".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbool_or",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 696usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbool_or_all (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbool_or_all".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbool_or_all",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 697usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbool_over_after_ts (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbool_over_after_ts".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbool_over_after_ts",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 698usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbool_over_before_ts (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbool_over_before_ts".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbool_over_before_ts",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 699usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbool_segments (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbool_segments".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbool_segments",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 700usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbool_sequence_from_ewkt (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbool_sequence_from_ewkt".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbool_sequence_from_ewkt",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 701usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbool_sequence_memsize (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbool_sequence_memsize".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbool_sequence_memsize",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 702usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbool_sequence_new (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbool_sequence_new".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbool_sequence_new",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 703usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbool_sequences (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbool_sequences".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbool_sequences",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 704usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbool_start_timestamp (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbool_start_timestamp".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbool_start_timestamp",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 705usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbool_start_value (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbool_start_value".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbool_start_value",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 706usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbool_temporal_extent (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbool_temporal_extent".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbool_temporal_extent",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 707usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbool_time_bins (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbool_time_bins".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbool_time_bins",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 708usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbool_time_span (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbool_time_span".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbool_time_span",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 709usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbool_to_csv (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbool_to_csv".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbool_to_csv",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 710usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbool_to_json (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbool_to_json".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbool_to_json",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 711usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbool_unnest (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbool_unnest".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbool_unnest",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 712usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbool_value_at (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbool_value_at".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbool_value_at",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 713usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbool_value_n (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbool_value_n".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbool_value_n",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 714usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbool_when_false (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbool_when_false".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbool_when_false",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 715usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbool_when_true (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbool_when_true".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbool_when_true",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 716usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tcount_timestamptz (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tcount_timestamptz".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tcount_timestamptz",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 717usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("tcount_tstzset (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tcount_tstzset".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tcount_tstzset",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 718usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tcount_tstzspan (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tcount_tstzspan".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tcount_tstzspan",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 719usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("tcount_tstzspanset (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tcount_tstzspanset".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tcount_tstzspanset",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 720usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("temporal_contains (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["temporal_contains".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "temporal_contains",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 721usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("temporal_during (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["temporal_during".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "temporal_during",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 722usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("temporal_finishes (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["temporal_finishes".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "temporal_finishes",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 723usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("temporal_follows (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["temporal_follows".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "temporal_follows",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 724usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("temporal_meets (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["temporal_meets".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "temporal_meets",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 725usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("temporal_overlaps (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["temporal_overlaps".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "temporal_overlaps",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 726usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("temporal_precedes (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["temporal_precedes".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "temporal_precedes",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 727usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("temporal_starts (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["temporal_starts".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "temporal_starts",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 728usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("text_set_cmp (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["text_set_cmp".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "text_set_cmp",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 729usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("text_set_end_value (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["text_set_end_value".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "text_set_end_value",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 730usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("text_set_eq (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["text_set_eq".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "text_set_eq",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 731usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("text_set_from_text (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["text_set_from_text".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "text_set_from_text",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 732usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("text_set_ge (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["text_set_ge".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "text_set_ge",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 733usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("text_set_gt (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["text_set_gt".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "text_set_gt",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 734usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("text_set_le (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["text_set_le".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "text_set_le",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 735usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("text_set_lt (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["text_set_lt".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "text_set_lt",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 736usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("text_set_memsize (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["text_set_memsize".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "text_set_memsize",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 737usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("text_set_ne (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["text_set_ne".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "text_set_ne",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 738usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("text_set_start_value (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["text_set_start_value".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "text_set_start_value",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 739usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("text_set_to_text (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["text_set_to_text".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "text_set_to_text",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 740usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("text_set_value_n (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["text_set_value_n".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "text_set_value_n",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 741usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("textset_contains (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["textset_contains".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "textset_contains",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 742usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("textset_from_text (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["textset_from_text".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "textset_from_text",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 743usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("textset_intersection (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["textset_intersection".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "textset_intersection",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 744usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("textset_is_disjoint (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["textset_is_disjoint".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "textset_is_disjoint",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 745usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("textset_is_subset (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["textset_is_subset".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "textset_is_subset",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 746usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("textset_is_superset (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["textset_is_superset".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "textset_is_superset",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 747usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("textset_len (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["textset_len".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "textset_len",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 748usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("textset_make (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["textset_make".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "textset_make",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 749usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("textset_max (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["textset_max".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "textset_max",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 750usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("textset_min (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["textset_min".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "textset_min",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 751usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("textset_minus (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["textset_minus".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "textset_minus",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 752usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("textset_symdiff (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["textset_symdiff".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "textset_symdiff",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 753usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("textset_to_text (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["textset_to_text".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "textset_to_text",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 754usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("textset_union (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["textset_union".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "textset_union",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 755usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("textset_values (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["textset_values".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "textset_values",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 756usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_abs (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_abs".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_abs",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 757usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_acf (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_acf".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_acf",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 758usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_add (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_add".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_add",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 759usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_add_scalar (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_add_scalar".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_add_scalar",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 760usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_after_ts (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_after_ts".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_after_ts",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 761usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_always_eq (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_always_eq".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_always_eq",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 762usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_always_eq_scalar (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_always_eq_scalar".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_always_eq_scalar",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 763usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_always_gt (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_always_gt".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_always_gt",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 764usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_always_gt_scalar (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_always_gt_scalar".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_always_gt_scalar",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 765usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_always_lt (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_always_lt".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_always_lt",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 766usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_always_lt_scalar (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_always_lt_scalar".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_always_lt_scalar",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 767usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_append_instant (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_append_instant".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_append_instant",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 768usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg3".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_append_instant_limits (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_append_instant_limits".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_append_instant_limits",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 769usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_append_sequence (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_append_sequence".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_append_sequence",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 770usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_arrow_schema (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_arrow_schema".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_arrow_schema",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 771usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_at_max (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_at_max".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_at_max",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 772usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_at_min (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_at_min".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_at_min",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 773usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_at_period (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_at_period".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_at_period",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 774usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_at_value (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_at_value".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_at_value",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 775usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_autocorrelation (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_autocorrelation".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_autocorrelation",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 776usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_batch_from_parquet (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_batch_from_parquet".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_batch_from_parquet",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 777usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_batch_to_parquet (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_batch_to_parquet".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_batch_to_parquet",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 778usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_before_ts (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_before_ts".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_before_ts",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 779usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_coefficient_of_variation (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_coefficient_of_variation".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_coefficient_of_variation",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 780usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_correlation_at (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_correlation_at".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_correlation_at",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 781usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_cos (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_cos".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_cos",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 782usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_count (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_count".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_count",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 783usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_count_trend_segments (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_count_trend_segments".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_count_trend_segments",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 784usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_cross_correlation (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_cross_correlation".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_cross_correlation",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 785usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_derivative (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_derivative".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_derivative",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 786usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_derivative_wit (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_derivative_wit".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_derivative_wit",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 787usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_detect_anomalies_iqr (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_detect_anomalies_iqr".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_detect_anomalies_iqr",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 788usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_detect_anomalies_moving_avg (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_detect_anomalies_moving_avg".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_detect_anomalies_moving_avg",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 789usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_detect_anomalies_zscore (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_detect_anomalies_zscore".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_detect_anomalies_zscore",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 790usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_detect_level_crossings (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_detect_level_crossings".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_detect_level_crossings",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 791usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_detect_peaks (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_detect_peaks".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_detect_peaks",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 792usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_detect_periodicity (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_detect_periodicity".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_detect_periodicity",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 793usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_detect_trend (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_detect_trend".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_detect_trend",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 794usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_detect_valleys (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_detect_valleys".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_detect_valleys",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 795usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_detect_zero_crossings (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_detect_zero_crossings".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_detect_zero_crossings",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 796usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_detrend (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_detrend".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_detrend",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 797usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_div (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_div".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_div",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 798usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_div_scalar (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_div_scalar".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_div_scalar",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 799usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_ema (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_ema".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_ema",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 800usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_end_value (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_end_value".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_end_value",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 801usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_ever_eq (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_ever_eq".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_ever_eq",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 802usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_ever_eq_scalar (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_ever_eq_scalar".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_ever_eq_scalar",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 803usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_ever_gt (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_ever_gt".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_ever_gt",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 804usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_ever_gt_scalar (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_ever_gt_scalar".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_ever_gt_scalar",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 805usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_ever_lt (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_ever_lt".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_ever_lt",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 806usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_ever_lt_scalar (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_ever_lt_scalar".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_ever_lt_scalar",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 807usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_exp (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_exp".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_exp",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 808usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_exponential_smooth (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_exponential_smooth".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_exponential_smooth",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 809usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_fill_gaps (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_fill_gaps".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_fill_gaps",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 810usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_fill_gaps_auto_interval (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_fill_gaps_auto_interval".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_fill_gaps_auto_interval",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 811usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_forecast_linear (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_forecast_linear".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_forecast_linear",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 812usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_from_csv (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_from_csv".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_from_csv",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 813usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_from_ewkt (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_from_ewkt".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_from_ewkt",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 814usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_from_ipc (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_from_ipc".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_from_ipc",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 815usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_from_parquet (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_from_parquet".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_from_parquet",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 816usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_gaussian_smooth (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_gaussian_smooth".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_gaussian_smooth",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 817usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_histogram (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_histogram".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_histogram",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 818usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_instant_from_ewkt (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_instant_from_ewkt".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_instant_from_ewkt",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 819usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_instant_memsize (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_instant_memsize".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_instant_memsize",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 820usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_instant_new (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_instant_new".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_instant_new",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 821usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_instant_round (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_instant_round".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_instant_round",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 822usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_instants (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_instants".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_instants",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 823usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_integral (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_integral".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_integral",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 824usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_iqr (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_iqr".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_iqr",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 825usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_kalman_smooth (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_kalman_smooth".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_kalman_smooth",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 826usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_kde (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_kde".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_kde",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 827usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_kurtosis (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_kurtosis".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_kurtosis",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 828usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_lift_between (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_lift_between".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_lift_between",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 829usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_lift_eq (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_lift_eq".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_lift_eq",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 830usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_lift_ge (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_lift_ge".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_lift_ge",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 831usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_lift_gt (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_lift_gt".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_lift_gt",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 832usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_lift_le (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_lift_le".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_lift_le",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 833usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_lift_lt (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_lift_lt".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_lift_lt",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 834usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_lift_ne (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_lift_ne".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_lift_ne",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 835usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_ln (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_ln".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_ln",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 836usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_log10 (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_log10".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_log10",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 837usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_max_cross_correlation (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_max_cross_correlation".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_max_cross_correlation",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 838usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_max_value (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_max_value".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_max_value",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 839usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_max_value_wit (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_max_value_wit".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_max_value_wit",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 840usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_mean (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_mean".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_mean",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 841usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_median (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_median".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_median",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 842usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_merge (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_merge".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_merge",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 843usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_min_value (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_min_value".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_min_value",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 844usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_min_value_wit (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_min_value_wit".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_min_value_wit",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 845usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_minus_period (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_minus_period".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_minus_period",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 846usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_minus_value (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_minus_value".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_minus_value",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 847usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_mode_single (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_mode_single".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_mode_single",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 848usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_moving_avg (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_moving_avg".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_moving_avg",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 849usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_moving_max (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_moving_max".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_moving_max",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 850usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_moving_min (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_moving_min".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_moving_min",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 851usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_moving_sum (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_moving_sum".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_moving_sum",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 852usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_moving_variance (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_moving_variance".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_moving_variance",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 853usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_mul (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_mul".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_mul",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 854usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_mul_scalar (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_mul_scalar".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_mul_scalar",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 855usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_negate (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_negate".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_negate",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 856usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_num_instants (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_num_instants".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_num_instants",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 857usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_num_instants_wit (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_num_instants_wit".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_num_instants_wit",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 858usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_over_after_ts (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_over_after_ts".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_over_after_ts",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 859usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_over_before_ts (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_over_before_ts".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_over_before_ts",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 860usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_pacf (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_pacf".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_pacf",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 861usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_pct_change (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_pct_change".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_pct_change",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 862usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_percentile (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_percentile".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_percentile",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 863usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_pow (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_pow".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_pow",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 864usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_precision (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_precision".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_precision",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 865usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_quantile (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_quantile".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_quantile",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 866usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_remove_outliers (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_remove_outliers".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_remove_outliers",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 867usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_rolling_stddev (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_rolling_stddev".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_rolling_stddev",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 868usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_rolling_variance (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_rolling_variance".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_rolling_variance",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 869usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_sample_entropy (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_sample_entropy".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_sample_entropy",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 870usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_segments (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_segments".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_segments",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 871usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_sequence_from_ewkt (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_sequence_from_ewkt".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_sequence_from_ewkt",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 872usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_sequence_memsize (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_sequence_memsize".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_sequence_memsize",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 873usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_sequence_new (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_sequence_new".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_sequence_new",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 874usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_sequence_new_with_interpolation (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_sequence_new_with_interpolation".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_sequence_new_with_interpolation",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 875usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_sequence_round (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_sequence_round".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_sequence_round",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 876usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_sequences (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_sequences".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_sequences",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 877usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_sin (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_sin".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_sin",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 878usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_skewness (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_skewness".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_skewness",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 879usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_sqrt (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_sqrt".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_sqrt",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 880usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_start_value (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_start_value".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_start_value",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 881usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_stddev (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_stddev".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_stddev",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 882usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_sub (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_sub".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_sub",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 883usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_sub_scalar (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_sub_scalar".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_sub_scalar",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 884usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_sum (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_sum".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_sum",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 885usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_tan (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_tan".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_tan",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 886usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_temporal_correlation (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_temporal_correlation".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_temporal_correlation",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 887usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_temporal_covariance (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_temporal_covariance".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_temporal_covariance",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 888usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_temporal_entropy (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_temporal_entropy".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_temporal_entropy",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 889usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_temporal_eq (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_temporal_eq".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_temporal_eq",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 890usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_temporal_extent (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_temporal_extent".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_temporal_extent",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 891usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_temporal_ge (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_temporal_ge".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_temporal_ge",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 892usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_temporal_gt (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_temporal_gt".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_temporal_gt",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 893usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_temporal_iqr (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_temporal_iqr".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_temporal_iqr",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 894usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_temporal_kurtosis (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_temporal_kurtosis".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_temporal_kurtosis",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 895usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_temporal_le (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_temporal_le".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_temporal_le",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 896usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_temporal_lt (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_temporal_lt".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_temporal_lt",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 897usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_temporal_mode (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_temporal_mode".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_temporal_mode",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 898usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_temporal_ne (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_temporal_ne".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_temporal_ne",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 899usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_temporal_range (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_temporal_range".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_temporal_range",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 900usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_temporal_skewness (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_temporal_skewness".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_temporal_skewness",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 901usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_temporal_weighted_median (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_temporal_weighted_median".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_temporal_weighted_median",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 902usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_time_bins (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_time_bins".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_time_bins",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 903usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_time_boxes (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_time_boxes".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_time_boxes",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 904usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_time_span (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_time_span".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_time_span",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 905usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_to_csv (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_to_csv".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_to_csv",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 906usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_to_ipc (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_to_ipc".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_to_ipc",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 907usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_to_json (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_to_json".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_to_json",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 908usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_to_linear (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_to_linear".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_to_linear",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 909usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_to_parquet (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_to_parquet".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_to_parquet",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 910usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_to_stepwise (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_to_stepwise".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_to_stepwise",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 911usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_trend_slope (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_trend_slope".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_trend_slope",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 912usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_twavg (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_twavg".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_twavg",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 913usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_twcount (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_twcount".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_twcount",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 914usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_twmax (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_twmax".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_twmax",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 915usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_twmin (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_twmin".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_twmin",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 916usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_twstddev (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_twstddev".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_twstddev",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 917usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_twsum (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_twsum".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_twsum",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 918usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_twvariance (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_twvariance".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_twvariance",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 919usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_unnest (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_unnest".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_unnest",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 920usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_upsample_to_target (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_upsample_to_target".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_upsample_to_target",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 921usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_value_at (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_value_at".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_value_at",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 922usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_value_bins (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_value_bins".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_value_bins",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 923usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_value_boxes (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_value_boxes".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_value_boxes",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 924usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_value_extent (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_value_extent".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_value_extent",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 925usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_value_n (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_value_n".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_value_n",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 926usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_variance (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_variance".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_variance",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 927usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_wavg (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_wavg".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_wavg",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 928usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_wcount (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_wcount".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_wcount",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 929usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_when_eq (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_when_eq".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_when_eq",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 930usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_when_ge (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_when_ge".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_when_ge",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 931usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_when_gt (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_when_gt".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_when_gt",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 932usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_when_le (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_when_le".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_when_le",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 933usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_when_lt (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_when_lt".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_when_lt",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 934usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_when_ne (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_when_ne".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_when_ne",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 935usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_wmax (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_wmax".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_wmax",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 936usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_wmin (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_wmin".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_wmin",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 937usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_wsum (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_wsum".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_wsum",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 938usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tgeometry_geometry_types (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tgeometry_geometry_types".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tgeometry_geometry_types",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 939usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tint_abs (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tint_abs".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tint_abs",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 940usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tint_add (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tint_add".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tint_add",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 941usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tint_add_scalar (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tint_add_scalar".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tint_add_scalar",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 942usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tint_after_ts (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tint_after_ts".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tint_after_ts",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 943usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tint_always_eq (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tint_always_eq".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tint_always_eq",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 944usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tint_always_eq_scalar (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tint_always_eq_scalar".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tint_always_eq_scalar",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 945usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tint_always_gt (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tint_always_gt".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tint_always_gt",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 946usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tint_always_gt_scalar (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tint_always_gt_scalar".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tint_always_gt_scalar",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 947usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tint_always_lt (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tint_always_lt".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tint_always_lt",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 948usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tint_always_lt_scalar (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tint_always_lt_scalar".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tint_always_lt_scalar",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 949usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tint_append_instant (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tint_append_instant".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tint_append_instant",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 950usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg3".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("tint_append_instant_limits (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tint_append_instant_limits".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tint_append_instant_limits",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 951usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tint_append_sequence (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tint_append_sequence".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tint_append_sequence",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 952usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            ];
        let opts = runtime::Funcopts {
            description: Some("tint_arrow_schema (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tint_arrow_schema".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tint_arrow_schema",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 953usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tint_at_max (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tint_at_max".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tint_at_max",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 954usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tint_at_min (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tint_at_min".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tint_at_min",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 955usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tint_at_period (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tint_at_period".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tint_at_period",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 956usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tint_at_range (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tint_at_range".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tint_at_range",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 957usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tint_at_value (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tint_at_value".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tint_at_value",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 958usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("tint_at_values (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tint_at_values".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tint_at_values",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 959usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tint_before_ts (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tint_before_ts".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tint_before_ts",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 960usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tint_diff (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tint_diff".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tint_diff",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 961usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tint_div (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tint_div".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tint_div",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 962usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tint_div_scalar (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tint_div_scalar".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tint_div_scalar",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 963usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tint_end_value (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tint_end_value".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tint_end_value",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 964usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tint_ever_eq (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tint_ever_eq".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tint_ever_eq",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 965usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tint_ever_eq_scalar (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tint_ever_eq_scalar".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tint_ever_eq_scalar",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 966usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tint_ever_gt (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tint_ever_gt".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tint_ever_gt",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 967usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tint_ever_gt_scalar (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tint_ever_gt_scalar".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tint_ever_gt_scalar",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 968usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tint_ever_lt (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tint_ever_lt".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tint_ever_lt",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 969usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tint_ever_lt_scalar (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tint_ever_lt_scalar".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tint_ever_lt_scalar",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 970usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tint_exponential_smooth (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tint_exponential_smooth".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tint_exponential_smooth",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 971usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("tint_from_csv (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tint_from_csv".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tint_from_csv",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 972usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("tint_from_ewkt (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tint_from_ewkt".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tint_from_ewkt",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 973usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tint_from_ipc (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tint_from_ipc".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tint_from_ipc",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 974usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tint_from_parquet (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tint_from_parquet".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tint_from_parquet",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 975usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("tint_instant_from_ewkt (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tint_instant_from_ewkt".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tint_instant_from_ewkt",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 976usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tint_instant_memsize (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tint_instant_memsize".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tint_instant_memsize",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 977usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tint_instant_new (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tint_instant_new".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tint_instant_new",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 978usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tint_instant_to_tbigint (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tint_instant_to_tbigint".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tint_instant_to_tbigint",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 979usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tint_instant_to_tfloat (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tint_instant_to_tfloat".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tint_instant_to_tfloat",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 980usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tint_instants (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tint_instants".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tint_instants",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 981usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tint_integral (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tint_integral".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tint_integral",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 982usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tint_kurtosis (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tint_kurtosis".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tint_kurtosis",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 983usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tint_lift_between (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tint_lift_between".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tint_lift_between",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 984usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tint_lift_eq (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tint_lift_eq".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tint_lift_eq",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 985usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tint_lift_ge (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tint_lift_ge".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tint_lift_ge",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 986usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tint_lift_gt (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tint_lift_gt".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tint_lift_gt",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 987usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tint_lift_le (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tint_lift_le".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tint_lift_le",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 988usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tint_lift_lt (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tint_lift_lt".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tint_lift_lt",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 989usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tint_lift_ne (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tint_lift_ne".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tint_lift_ne",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 990usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tint_max_value (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tint_max_value".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tint_max_value",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 991usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tint_max_value_scalar (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tint_max_value_scalar".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tint_max_value_scalar",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 992usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tint_max_value_wit (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tint_max_value_wit".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tint_max_value_wit",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 993usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tint_median (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tint_median".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tint_median",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 994usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tint_merge (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tint_merge".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tint_merge",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 995usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tint_min_value (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tint_min_value".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tint_min_value",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 996usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tint_min_value_scalar (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tint_min_value_scalar".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tint_min_value_scalar",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 997usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tint_min_value_wit (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tint_min_value_wit".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tint_min_value_wit",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 998usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tint_minus_period (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tint_minus_period".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tint_minus_period",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 999usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tint_minus_range (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tint_minus_range".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tint_minus_range",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1000usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tint_minus_value (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tint_minus_value".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tint_minus_value",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1001usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("tint_minus_values (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tint_minus_values".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tint_minus_values",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1002usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tint_mod (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tint_mod".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tint_mod",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1003usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tint_mod_scalar (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tint_mod_scalar".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tint_mod_scalar",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1004usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tint_mode (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tint_mode".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tint_mode",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1005usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tint_moving_average (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tint_moving_average".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tint_moving_average",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1006usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tint_mul (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tint_mul".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tint_mul",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1007usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tint_mul_scalar (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tint_mul_scalar".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tint_mul_scalar",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1008usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tint_negate (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tint_negate".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tint_negate",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1009usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tint_num_anomalies_iqr (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tint_num_anomalies_iqr".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tint_num_anomalies_iqr",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1010usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tint_num_anomalies_zscore (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tint_num_anomalies_zscore".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tint_num_anomalies_zscore",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1011usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tint_num_instants (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tint_num_instants".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tint_num_instants",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1012usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tint_num_instants_wit (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tint_num_instants_wit".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tint_num_instants_wit",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1013usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tint_over_after_ts (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tint_over_after_ts".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tint_over_after_ts",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1014usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tint_over_before_ts (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tint_over_before_ts".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tint_over_before_ts",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1015usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tint_quantile (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tint_quantile".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tint_quantile",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1016usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("tint_quantiles (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tint_quantiles".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tint_quantiles",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1017usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tint_rolling_mean (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tint_rolling_mean".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tint_rolling_mean",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1018usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tint_rolling_stddev (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tint_rolling_stddev".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tint_rolling_stddev",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1019usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tint_rolling_variance (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tint_rolling_variance".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tint_rolling_variance",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1020usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tint_segments (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tint_segments".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tint_segments",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1021usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("tint_sequence_from_ewkt (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tint_sequence_from_ewkt".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tint_sequence_from_ewkt",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1022usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tint_sequence_memsize (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tint_sequence_memsize".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tint_sequence_memsize",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1023usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("tint_sequence_new (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tint_sequence_new".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tint_sequence_new",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1024usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tint_sequence_to_tfloat (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tint_sequence_to_tfloat".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tint_sequence_to_tfloat",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1025usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("tint_sequences (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tint_sequences".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tint_sequences",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1026usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tint_skewness (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tint_skewness".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tint_skewness",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1027usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tint_start_value (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tint_start_value".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tint_start_value",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1028usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tint_sub (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tint_sub".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tint_sub",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1029usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tint_sub_scalar (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tint_sub_scalar".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tint_sub_scalar",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1030usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("tint_temporal_avg (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tint_temporal_avg".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tint_temporal_avg",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1031usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tint_temporal_eq (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tint_temporal_eq".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tint_temporal_eq",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1032usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tint_temporal_extent (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tint_temporal_extent".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tint_temporal_extent",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1033usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tint_temporal_ge (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tint_temporal_ge".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tint_temporal_ge",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1034usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tint_temporal_gt (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tint_temporal_gt".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tint_temporal_gt",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1035usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tint_temporal_le (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tint_temporal_le".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tint_temporal_le",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1036usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tint_temporal_lt (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tint_temporal_lt".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tint_temporal_lt",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1037usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("tint_temporal_mode (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tint_temporal_mode".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tint_temporal_mode",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1038usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tint_temporal_ne (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tint_temporal_ne".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tint_temporal_ne",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1039usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("tint_temporal_range (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tint_temporal_range".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tint_temporal_range",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1040usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tint_time_bins (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tint_time_bins".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tint_time_bins",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1041usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tint_time_boxes (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tint_time_boxes".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tint_time_boxes",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1042usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tint_time_span (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tint_time_span".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tint_time_span",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1043usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tint_to_csv (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tint_to_csv".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tint_to_csv",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1044usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tint_to_ipc (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tint_to_ipc".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tint_to_ipc",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1045usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tint_to_json (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tint_to_json".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tint_to_json",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1046usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tint_to_parquet (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tint_to_parquet".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tint_to_parquet",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1047usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tint_to_tbigint (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tint_to_tbigint".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tint_to_tbigint",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1048usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tint_twavg (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tint_twavg".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tint_twavg",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1049usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tint_twcount (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tint_twcount".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tint_twcount",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1050usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tint_twmax (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tint_twmax".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tint_twmax",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1051usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tint_twmin (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tint_twmin".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tint_twmin",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1052usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tint_twstddev (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tint_twstddev".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tint_twstddev",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1053usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tint_twsum (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tint_twsum".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tint_twsum",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1054usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tint_twvariance (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tint_twvariance".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tint_twvariance",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1055usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tint_unnest (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tint_unnest".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tint_unnest",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1056usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tint_upsample_to_target (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tint_upsample_to_target".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tint_upsample_to_target",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1057usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tint_value_at (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tint_value_at".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tint_value_at",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1058usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tint_value_bins (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tint_value_bins".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tint_value_bins",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1059usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tint_value_boxes (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tint_value_boxes".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tint_value_boxes",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1060usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tint_value_bucket_index (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tint_value_bucket_index".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tint_value_bucket_index",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1061usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tint_value_extent (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tint_value_extent".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tint_value_extent",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1062usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tint_value_n (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tint_value_n".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tint_value_n",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1063usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tint_wavg (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tint_wavg".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tint_wavg",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1064usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tint_wcount (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tint_wcount".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tint_wcount",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1065usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tint_when_eq (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tint_when_eq".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tint_when_eq",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1066usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tint_when_ge (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tint_when_ge".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tint_when_ge",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1067usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tint_when_gt (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tint_when_gt".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tint_when_gt",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1068usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tint_when_le (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tint_when_le".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tint_when_le",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1069usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tint_when_lt (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tint_when_lt".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tint_when_lt",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1070usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tint_when_ne (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tint_when_ne".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tint_when_ne",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1071usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tint_wmax (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tint_wmax".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tint_wmax",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1072usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tint_wmin (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tint_wmin".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tint_wmin",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1073usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tint_wsum (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tint_wsum".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tint_wsum",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1074usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tjsonb_always_contains (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tjsonb_always_contains".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tjsonb_always_contains",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1075usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("tjsonb_always_contains_key (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tjsonb_always_contains_key".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tjsonb_always_contains_key",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1076usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tjsonb_always_eq_value (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tjsonb_always_eq_value".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tjsonb_always_eq_value",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1077usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("tjsonb_always_path_exists (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tjsonb_always_path_exists".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tjsonb_always_path_exists",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1078usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("tjsonb_always_typeof (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tjsonb_always_typeof".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tjsonb_always_typeof",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1079usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tjsonb_array_append (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tjsonb_array_append".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tjsonb_array_append",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1080usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tjsonb_array_prepend (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tjsonb_array_prepend".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tjsonb_array_prepend",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1081usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tjsonb_at_contains (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tjsonb_at_contains".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tjsonb_at_contains",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1082usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("tjsonb_at_path_exists (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tjsonb_at_path_exists".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tjsonb_at_path_exists",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1083usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tjsonb_at_period (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tjsonb_at_period".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tjsonb_at_period",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1084usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tjsonb_at_time (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tjsonb_at_time".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tjsonb_at_time",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1085usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("tjsonb_at_typeof (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tjsonb_at_typeof".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tjsonb_at_typeof",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1086usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tjsonb_at_value (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tjsonb_at_value".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tjsonb_at_value",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1087usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tjsonb_at_value_matching_key (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tjsonb_at_value_matching_key".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tjsonb_at_value_matching_key",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1088usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tjsonb_at_value_matching_path (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tjsonb_at_value_matching_path".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tjsonb_at_value_matching_path",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1089usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tjsonb_concat (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tjsonb_concat".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tjsonb_concat",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1090usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tjsonb_count_contains (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tjsonb_count_contains".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tjsonb_count_contains",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1091usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("tjsonb_count_key (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tjsonb_count_key".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tjsonb_count_key",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1092usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("tjsonb_count_path (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tjsonb_count_path".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tjsonb_count_path",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1093usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("tjsonb_count_typeof (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tjsonb_count_typeof".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tjsonb_count_typeof",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1094usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("tjsonb_delete_key (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tjsonb_delete_key".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tjsonb_delete_key",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1095usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("tjsonb_delete_path (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tjsonb_delete_path".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tjsonb_delete_path",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1096usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tjsonb_duration (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tjsonb_duration".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tjsonb_duration",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1097usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tjsonb_end_instant (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tjsonb_end_instant".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tjsonb_end_instant",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1098usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tjsonb_end_timestamp (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tjsonb_end_timestamp".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tjsonb_end_timestamp",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1099usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tjsonb_end_value (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tjsonb_end_value".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tjsonb_end_value",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1100usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tjsonb_ever_contains (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tjsonb_ever_contains".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tjsonb_ever_contains",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1101usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("tjsonb_ever_contains_key (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tjsonb_ever_contains_key".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tjsonb_ever_contains_key",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1102usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tjsonb_ever_eq_value (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tjsonb_ever_eq_value".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tjsonb_ever_eq_value",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1103usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("tjsonb_ever_path_exists (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tjsonb_ever_path_exists".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tjsonb_ever_path_exists",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1104usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("tjsonb_ever_typeof (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tjsonb_ever_typeof".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tjsonb_ever_typeof",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1105usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("tjsonb_from_csv (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tjsonb_from_csv".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tjsonb_from_csv",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1106usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tjsonb_from_wkb (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tjsonb_from_wkb".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tjsonb_from_wkb",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1107usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tjsonb_hash (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tjsonb_hash".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tjsonb_hash",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1108usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tjsonb_instant_from_jsonb (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tjsonb_instant_from_jsonb".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tjsonb_instant_from_jsonb",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1109usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("tjsonb_instant_from_mfjson (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tjsonb_instant_from_mfjson".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tjsonb_instant_from_mfjson",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1110usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("tjsonb_instant_from_text (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tjsonb_instant_from_text".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tjsonb_instant_from_text",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1111usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tjsonb_instant_n (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tjsonb_instant_n".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tjsonb_instant_n",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1112usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("tjsonb_instant_new (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tjsonb_instant_new".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tjsonb_instant_new",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1113usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tjsonb_instant_to_mfjson (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tjsonb_instant_to_mfjson".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tjsonb_instant_to_mfjson",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1114usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tjsonb_instant_to_text (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tjsonb_instant_to_text".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tjsonb_instant_to_text",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1115usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tjsonb_instants (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tjsonb_instants".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tjsonb_instants",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1116usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tjsonb_key_at (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tjsonb_key_at".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tjsonb_key_at",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1117usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tjsonb_lower_inclusive (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tjsonb_lower_inclusive".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tjsonb_lower_inclusive",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1118usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tjsonb_memsize (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tjsonb_memsize".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tjsonb_memsize",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1119usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tjsonb_merge_deep (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tjsonb_merge_deep".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tjsonb_merge_deep",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1120usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tjsonb_minus_contains (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tjsonb_minus_contains".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tjsonb_minus_contains",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1121usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("tjsonb_minus_path_exists (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tjsonb_minus_path_exists".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tjsonb_minus_path_exists",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1122usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tjsonb_minus_period (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tjsonb_minus_period".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tjsonb_minus_period",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1123usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tjsonb_minus_time (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tjsonb_minus_time".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tjsonb_minus_time",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1124usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("tjsonb_minus_typeof (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tjsonb_minus_typeof".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tjsonb_minus_typeof",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1125usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tjsonb_minus_value (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tjsonb_minus_value".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tjsonb_minus_value",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1126usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tjsonb_minus_value_matching_key (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tjsonb_minus_value_matching_key".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tjsonb_minus_value_matching_key",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1127usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tjsonb_minus_value_matching_path (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tjsonb_minus_value_matching_path".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tjsonb_minus_value_matching_path",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1128usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tjsonb_num_instants (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tjsonb_num_instants".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tjsonb_num_instants",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1129usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tjsonb_num_timestamps (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tjsonb_num_timestamps".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tjsonb_num_timestamps",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1130usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tjsonb_path_at (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tjsonb_path_at".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tjsonb_path_at",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1131usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("tjsonb_project_tbool (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tjsonb_project_tbool".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tjsonb_project_tbool",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1132usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("tjsonb_project_tfloat (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tjsonb_project_tfloat".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tjsonb_project_tfloat",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1133usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("tjsonb_project_tint (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tjsonb_project_tint".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tjsonb_project_tint",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1134usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("tjsonb_project_ttext (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tjsonb_project_ttext".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tjsonb_project_ttext",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1135usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("tjsonb_sequence_from_mfjson (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tjsonb_sequence_from_mfjson".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tjsonb_sequence_from_mfjson",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1136usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("tjsonb_sequence_from_pairs (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tjsonb_sequence_from_pairs".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tjsonb_sequence_from_pairs",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1137usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("tjsonb_sequence_from_text (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tjsonb_sequence_from_text".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tjsonb_sequence_from_text",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1138usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("tjsonb_sequence_new (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tjsonb_sequence_new".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tjsonb_sequence_new",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1139usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tjsonb_sequence_to_mfjson (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tjsonb_sequence_to_mfjson".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tjsonb_sequence_to_mfjson",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1140usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tjsonb_sequence_to_text (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tjsonb_sequence_to_text".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tjsonb_sequence_to_text",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1141usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tjsonb_set_key (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tjsonb_set_key".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tjsonb_set_key",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1142usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tjsonb_set_path (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tjsonb_set_path".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tjsonb_set_path",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1143usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tjsonb_start_instant (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tjsonb_start_instant".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tjsonb_start_instant",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1144usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tjsonb_start_timestamp (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tjsonb_start_timestamp".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tjsonb_start_timestamp",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1145usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tjsonb_start_value (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tjsonb_start_value".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tjsonb_start_value",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1146usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tjsonb_time_span (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tjsonb_time_span".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tjsonb_time_span",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1147usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tjsonb_timestamp_n (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tjsonb_timestamp_n".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tjsonb_timestamp_n",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1148usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tjsonb_timestamps (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tjsonb_timestamps".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tjsonb_timestamps",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1149usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tjsonb_to_csv (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tjsonb_to_csv".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tjsonb_to_csv",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1150usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tjsonb_to_ttext (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tjsonb_to_ttext".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tjsonb_to_ttext",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1151usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tjsonb_to_wkb (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tjsonb_to_wkb".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tjsonb_to_wkb",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1152usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tjsonb_typeof_at (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tjsonb_typeof_at".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tjsonb_typeof_at",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1153usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tjsonb_upper_inclusive (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tjsonb_upper_inclusive".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tjsonb_upper_inclusive",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1154usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tjsonb_value_at (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tjsonb_value_at".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tjsonb_value_at",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1155usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tjsonb_value_n (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tjsonb_value_n".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tjsonb_value_n",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1156usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tjsonb_values (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tjsonb_values".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tjsonb_values",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1157usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("tstz_set_cmp (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tstz_set_cmp".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tstz_set_cmp",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1158usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("tstz_set_end_value (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tstz_set_end_value".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tstz_set_end_value",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1159usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("tstz_set_eq (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tstz_set_eq".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tstz_set_eq",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1160usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("tstz_set_from_text (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tstz_set_from_text".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tstz_set_from_text",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1161usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("tstz_set_ge (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tstz_set_ge".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tstz_set_ge",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1162usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("tstz_set_gt (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tstz_set_gt".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tstz_set_gt",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1163usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("tstz_set_le (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tstz_set_le".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tstz_set_le",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1164usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("tstz_set_lt (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tstz_set_lt".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tstz_set_lt",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1165usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("tstz_set_memsize (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tstz_set_memsize".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tstz_set_memsize",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1166usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("tstz_set_ne (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tstz_set_ne".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tstz_set_ne",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1167usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("tstz_set_start_value (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tstz_set_start_value".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tstz_set_start_value",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1168usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("tstz_set_to_text (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tstz_set_to_text".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tstz_set_to_text",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1169usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tstz_set_value_n (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tstz_set_value_n".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tstz_set_value_n",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1170usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tstz_span_after (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tstz_span_after".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tstz_span_after",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1171usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tstz_span_after_value (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tstz_span_after_value".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tstz_span_after_value",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1172usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tstz_span_before (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tstz_span_before".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tstz_span_before",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1173usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tstz_span_before_value (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tstz_span_before_value".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tstz_span_before_value",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1174usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tstz_span_bins (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tstz_span_bins".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tstz_span_bins",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1175usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tstz_span_cmp (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tstz_span_cmp".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tstz_span_cmp",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1176usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tstz_span_eq (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tstz_span_eq".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tstz_span_eq",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1177usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("tstz_span_extent (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tstz_span_extent".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tstz_span_extent",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1178usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("tstz_span_from_text (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tstz_span_from_text".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tstz_span_from_text",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1179usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tstz_span_ge (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tstz_span_ge".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tstz_span_ge",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1180usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tstz_span_gt (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tstz_span_gt".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tstz_span_gt",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1181usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tstz_span_le (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tstz_span_le".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tstz_span_le",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1182usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tstz_span_lt (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tstz_span_lt".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tstz_span_lt",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1183usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tstz_span_memsize (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tstz_span_memsize".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tstz_span_memsize",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1184usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tstz_span_ne (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tstz_span_ne".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tstz_span_ne",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1185usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tstz_span_over_after (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tstz_span_over_after".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tstz_span_over_after",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1186usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tstz_span_over_after_value (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tstz_span_over_after_value".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tstz_span_over_after_value",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1187usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tstz_span_over_before (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tstz_span_over_before".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tstz_span_over_before",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1188usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tstz_span_over_before_value (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tstz_span_over_before_value".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tstz_span_over_before_value",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1189usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tstz_span_to_text (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tstz_span_to_text".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tstz_span_to_text",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1190usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("tstz_spanset_after (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tstz_spanset_after".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tstz_spanset_after",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1191usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tstz_spanset_after_value (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tstz_spanset_after_value".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tstz_spanset_after_value",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1192usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("tstz_spanset_before (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tstz_spanset_before".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tstz_spanset_before",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1193usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tstz_spanset_before_value (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tstz_spanset_before_value".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tstz_spanset_before_value",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1194usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tstz_spanset_bins (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tstz_spanset_bins".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tstz_spanset_bins",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1195usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("tstz_spanset_cmp (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tstz_spanset_cmp".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tstz_spanset_cmp",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1196usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("tstz_spanset_end_span (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tstz_spanset_end_span".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tstz_spanset_end_span",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1197usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("tstz_spanset_eq (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tstz_spanset_eq".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tstz_spanset_eq",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1198usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("tstz_spanset_extent (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tstz_spanset_extent".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tstz_spanset_extent",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1199usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("tstz_spanset_from_text (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tstz_spanset_from_text".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tstz_spanset_from_text",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1200usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("tstz_spanset_ge (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tstz_spanset_ge".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tstz_spanset_ge",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1201usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("tstz_spanset_gt (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tstz_spanset_gt".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tstz_spanset_gt",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1202usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("tstz_spanset_le (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tstz_spanset_le".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tstz_spanset_le",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1203usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("tstz_spanset_lt (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tstz_spanset_lt".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tstz_spanset_lt",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1204usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("tstz_spanset_memsize (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tstz_spanset_memsize".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tstz_spanset_memsize",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1205usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("tstz_spanset_ne (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tstz_spanset_ne".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tstz_spanset_ne",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1206usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("tstz_spanset_over_after (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tstz_spanset_over_after".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tstz_spanset_over_after",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1207usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tstz_spanset_over_after_value (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tstz_spanset_over_after_value".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tstz_spanset_over_after_value",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1208usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("tstz_spanset_over_before (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tstz_spanset_over_before".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tstz_spanset_over_before",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1209usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tstz_spanset_over_before_value (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tstz_spanset_over_before_value".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tstz_spanset_over_before_value",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1210usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tstz_spanset_span_n (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tstz_spanset_span_n".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tstz_spanset_span_n",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1211usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("tstz_spanset_start_span (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tstz_spanset_start_span".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tstz_spanset_start_span",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1212usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("tstz_spanset_to_text (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tstz_spanset_to_text".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tstz_spanset_to_text",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1213usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tstzset_contains (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tstzset_contains".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tstzset_contains",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1214usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("tstzset_intersection (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tstzset_intersection".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tstzset_intersection",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1215usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("tstzset_len (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tstzset_len".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tstzset_len",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1216usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("tstzset_make (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tstzset_make".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tstzset_make",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1217usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("tstzset_minus (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tstzset_minus".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tstzset_minus",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1218usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tstzset_nth (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tstzset_nth".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tstzset_nth",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1219usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("tstzset_overlaps (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tstzset_overlaps".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tstzset_overlaps",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1220usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tstzset_shift (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tstzset_shift".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tstzset_shift",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1221usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("tstzset_successor (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tstzset_successor".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tstzset_successor",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1222usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("tstzset_to_text (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tstzset_to_text".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tstzset_to_text",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1223usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("tstzset_union (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tstzset_union".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tstzset_union",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1224usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tstzspan_adjacent (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tstzspan_adjacent".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tstzspan_adjacent",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1225usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tstzspan_contains (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tstzspan_contains".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tstzspan_contains",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1226usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tstzspan_contains_span (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tstzspan_contains_span".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tstzspan_contains_span",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1227usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tstzspan_duration (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tstzspan_duration".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tstzspan_duration",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1228usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tstzspan_end (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tstzspan_end".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tstzspan_end",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1229usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tstzspan_expand (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tstzspan_expand".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tstzspan_expand",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1230usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("tstzspan_from_text (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tstzspan_from_text".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tstzspan_from_text",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1231usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tstzspan_intersection (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tstzspan_intersection".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tstzspan_intersection",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1232usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Boolean },
                runtime::Funcarg { name: Some("arg3".into()), logical: Logicaltype::Boolean },
            ];
        let opts = runtime::Funcopts {
            description: Some("tstzspan_make (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tstzspan_make".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tstzspan_make",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1233usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tstzspan_midpoint (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tstzspan_midpoint".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tstzspan_midpoint",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1234usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tstzspan_minus (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tstzspan_minus".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tstzspan_minus",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1235usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tstzspan_overlaps (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tstzspan_overlaps".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tstzspan_overlaps",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1236usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tstzspan_scale (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tstzspan_scale".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tstzspan_scale",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1237usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("tstzspan_shift (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tstzspan_shift".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tstzspan_shift",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1238usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tstzspan_start (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tstzspan_start".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tstzspan_start",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1239usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tstzspan_to_text (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tstzspan_to_text".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tstzspan_to_text",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1240usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tstzspan_union (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tstzspan_union".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tstzspan_union",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1241usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("tstzspanset_from_text (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tstzspanset_from_text".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tstzspanset_from_text",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1242usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("tstzspanset_to_text (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tstzspanset_to_text".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tstzspanset_to_text",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1243usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("ttext_after_ts (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["ttext_after_ts".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "ttext_after_ts",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1244usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("ttext_always_eq (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["ttext_always_eq".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "ttext_always_eq",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1245usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("ttext_append_instant (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["ttext_append_instant".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "ttext_append_instant",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1246usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("ttext_append_sequence (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["ttext_append_sequence".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "ttext_append_sequence",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1247usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("ttext_at_period (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["ttext_at_period".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "ttext_at_period",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1248usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("ttext_at_value (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["ttext_at_value".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "ttext_at_value",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1249usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("ttext_before_ts (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["ttext_before_ts".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "ttext_before_ts",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1250usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("ttext_btrim (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["ttext_btrim".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "ttext_btrim",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1251usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("ttext_concat (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["ttext_concat".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "ttext_concat",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1252usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("ttext_concat_str (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["ttext_concat_str".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "ttext_concat_str",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1253usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("ttext_duration (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["ttext_duration".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "ttext_duration",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1254usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("ttext_end_timestamp (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["ttext_end_timestamp".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "ttext_end_timestamp",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1255usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("ttext_end_value (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["ttext_end_value".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "ttext_end_value",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1256usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("ttext_ever_eq (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["ttext_ever_eq".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "ttext_ever_eq",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1257usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("ttext_from_csv (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["ttext_from_csv".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "ttext_from_csv",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1258usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("ttext_from_ewkt (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["ttext_from_ewkt".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "ttext_from_ewkt",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1259usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("ttext_ilike (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["ttext_ilike".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "ttext_ilike",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1260usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("ttext_instant_from_ewkt (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["ttext_instant_from_ewkt".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "ttext_instant_from_ewkt",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1261usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("ttext_instant_memsize (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["ttext_instant_memsize".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "ttext_instant_memsize",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1262usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("ttext_instant_new (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["ttext_instant_new".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "ttext_instant_new",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1263usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("ttext_instants (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["ttext_instants".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "ttext_instants",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1264usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("ttext_left (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["ttext_left".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "ttext_left",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1265usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("ttext_length (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["ttext_length".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "ttext_length",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1266usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("ttext_like (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["ttext_like".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "ttext_like",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1267usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("ttext_lower (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["ttext_lower".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "ttext_lower",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1268usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("ttext_ltrim (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["ttext_ltrim".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "ttext_ltrim",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1269usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("ttext_merge (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["ttext_merge".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "ttext_merge",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1270usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("ttext_minus_period (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["ttext_minus_period".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "ttext_minus_period",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1271usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("ttext_minus_value (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["ttext_minus_value".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "ttext_minus_value",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1272usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("ttext_num_instants (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["ttext_num_instants".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "ttext_num_instants",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1273usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("ttext_num_instants_scalar (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["ttext_num_instants_scalar".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "ttext_num_instants_scalar",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1274usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("ttext_over_after_ts (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["ttext_over_after_ts".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "ttext_over_after_ts",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1275usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("ttext_over_before_ts (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["ttext_over_before_ts".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "ttext_over_before_ts",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1276usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("ttext_prepend_str (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["ttext_prepend_str".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "ttext_prepend_str",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1277usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("ttext_regex_match (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["ttext_regex_match".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "ttext_regex_match",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1278usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("ttext_regex_replace (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["ttext_regex_replace".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "ttext_regex_replace",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1279usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("ttext_right (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["ttext_right".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "ttext_right",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1280usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("ttext_rtrim (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["ttext_rtrim".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "ttext_rtrim",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1281usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("ttext_segments (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["ttext_segments".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "ttext_segments",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1282usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("ttext_sequence_from_ewkt (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["ttext_sequence_from_ewkt".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "ttext_sequence_from_ewkt",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1283usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("ttext_sequence_memsize (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["ttext_sequence_memsize".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "ttext_sequence_memsize",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1284usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("ttext_sequence_new (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["ttext_sequence_new".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "ttext_sequence_new",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1285usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("ttext_sequences (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["ttext_sequences".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "ttext_sequences",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1286usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("ttext_start_timestamp (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["ttext_start_timestamp".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "ttext_start_timestamp",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1287usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("ttext_start_value (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["ttext_start_value".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "ttext_start_value",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1288usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("ttext_temporal_extent (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["ttext_temporal_extent".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "ttext_temporal_extent",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1289usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("ttext_temporal_max (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["ttext_temporal_max".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "ttext_temporal_max",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1290usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("ttext_temporal_min (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["ttext_temporal_min".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "ttext_temporal_min",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1291usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("ttext_time_bins (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["ttext_time_bins".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "ttext_time_bins",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1292usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("ttext_time_span (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["ttext_time_span".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "ttext_time_span",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1293usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("ttext_to_csv (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["ttext_to_csv".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "ttext_to_csv",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1294usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("ttext_to_json (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["ttext_to_json".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "ttext_to_json",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1295usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("ttext_to_tjsonb (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["ttext_to_tjsonb".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "ttext_to_tjsonb",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1296usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("ttext_unnest (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["ttext_unnest".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "ttext_unnest",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1297usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("ttext_upper (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["ttext_upper".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "ttext_upper",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1298usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("ttext_value_at (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["ttext_value_at".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "ttext_value_at",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1299usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("ttext_value_n (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["ttext_value_n".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "ttext_value_n",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1300usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("ttext_when_eq (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["ttext_when_eq".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "ttext_when_eq",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1301usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("ttext_when_ne (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["ttext_when_ne".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "ttext_when_ne",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    Ok(())
}

fn register_aggregates() -> Result<(), Duckerror> {
    // Best-effort: hosts that don't expose the aggregate capability
    // still let scalars register (the scalar-only bridges shipped in
    // Phase A). Log-and-continue so the emit stays useful in both
    // shapes.
    let Some(capability) = runtime::get_capability(Capabilitykind::Aggregate) else {
        if true {
            eprintln!("[dynlink-emit] host did not expose aggregate capability; skipping 47 aggregate(s)");
        }
        return Ok(());
    };
    let registry = match capability {
        runtime::Capability::Aggregate(r) => r,
        _ => {
            return Err(Duckerror::Internal(
                "aggregate capability returned unexpected variant".into(),
            ));
        }
    };
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        aggregate_handle_table()
            .lock()
            .expect("aggregate handle mutex poisoned")
            .insert(handle, 0usize);
        let callback = runtime::AggregateCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("date_span_aggregate_union (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["date_span_aggregate_union".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "date_span_aggregate_union",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        aggregate_handle_table()
            .lock()
            .expect("aggregate handle mutex poisoned")
            .insert(handle, 1usize);
        let callback = runtime::AggregateCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("date_spanset_aggregate_union (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["date_spanset_aggregate_union".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "date_spanset_aggregate_union",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        aggregate_handle_table()
            .lock()
            .expect("aggregate handle mutex poisoned")
            .insert(handle, 2usize);
        let callback = runtime::AggregateCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("float_span_aggregate_union (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["float_span_aggregate_union".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "float_span_aggregate_union",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        aggregate_handle_table()
            .lock()
            .expect("aggregate handle mutex poisoned")
            .insert(handle, 3usize);
        let callback = runtime::AggregateCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("float_spanset_aggregate_union (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["float_spanset_aggregate_union".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "float_spanset_aggregate_union",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        aggregate_handle_table()
            .lock()
            .expect("aggregate handle mutex poisoned")
            .insert(handle, 4usize);
        let callback = runtime::AggregateCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("int_span_aggregate_union (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["int_span_aggregate_union".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "int_span_aggregate_union",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        aggregate_handle_table()
            .lock()
            .expect("aggregate handle mutex poisoned")
            .insert(handle, 5usize);
        let callback = runtime::AggregateCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("int_spanset_aggregate_union (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["int_spanset_aggregate_union".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "int_spanset_aggregate_union",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        aggregate_handle_table()
            .lock()
            .expect("aggregate handle mutex poisoned")
            .insert(handle, 6usize);
        let callback = runtime::AggregateCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbigint_count_aggregate (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbigint_count_aggregate".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbigint_count_aggregate",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        aggregate_handle_table()
            .lock()
            .expect("aggregate handle mutex poisoned")
            .insert(handle, 7usize);
        let callback = runtime::AggregateCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbigint_max_value_aggregate (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbigint_max_value_aggregate".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbigint_max_value_aggregate",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        aggregate_handle_table()
            .lock()
            .expect("aggregate handle mutex poisoned")
            .insert(handle, 8usize);
        let callback = runtime::AggregateCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbigint_merge_agg (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbigint_merge_agg".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbigint_merge_agg",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        aggregate_handle_table()
            .lock()
            .expect("aggregate handle mutex poisoned")
            .insert(handle, 9usize);
        let callback = runtime::AggregateCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbigint_min_value_aggregate (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbigint_min_value_aggregate".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbigint_min_value_aggregate",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        aggregate_handle_table()
            .lock()
            .expect("aggregate handle mutex poisoned")
            .insert(handle, 10usize);
        let callback = runtime::AggregateCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbigint_range_aggregate (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbigint_range_aggregate".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbigint_range_aggregate",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        aggregate_handle_table()
            .lock()
            .expect("aggregate handle mutex poisoned")
            .insert(handle, 11usize);
        let callback = runtime::AggregateCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbigint_sum_aggregate (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbigint_sum_aggregate".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbigint_sum_aggregate",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        aggregate_handle_table()
            .lock()
            .expect("aggregate handle mutex poisoned")
            .insert(handle, 12usize);
        let callback = runtime::AggregateCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbool_merge_agg (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbool_merge_agg".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbool_merge_agg",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        aggregate_handle_table()
            .lock()
            .expect("aggregate handle mutex poisoned")
            .insert(handle, 13usize);
        let callback = runtime::AggregateCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbool_temporal_and (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbool_temporal_and".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbool_temporal_and",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        aggregate_handle_table()
            .lock()
            .expect("aggregate handle mutex poisoned")
            .insert(handle, 14usize);
        let callback = runtime::AggregateCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tbool_temporal_or (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tbool_temporal_or".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tbool_temporal_or",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        aggregate_handle_table()
            .lock()
            .expect("aggregate handle mutex poisoned")
            .insert(handle, 15usize);
        let callback = runtime::AggregateCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_merge_agg (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_merge_agg".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_merge_agg",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        aggregate_handle_table()
            .lock()
            .expect("aggregate handle mutex poisoned")
            .insert(handle, 16usize);
        let callback = runtime::AggregateCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_temporal_avg (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_temporal_avg".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_temporal_avg",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        aggregate_handle_table()
            .lock()
            .expect("aggregate handle mutex poisoned")
            .insert(handle, 17usize);
        let callback = runtime::AggregateCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_temporal_count (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_temporal_count".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_temporal_count",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        aggregate_handle_table()
            .lock()
            .expect("aggregate handle mutex poisoned")
            .insert(handle, 18usize);
        let callback = runtime::AggregateCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_temporal_max (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_temporal_max".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_temporal_max",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        aggregate_handle_table()
            .lock()
            .expect("aggregate handle mutex poisoned")
            .insert(handle, 19usize);
        let callback = runtime::AggregateCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_temporal_min (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_temporal_min".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_temporal_min",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        aggregate_handle_table()
            .lock()
            .expect("aggregate handle mutex poisoned")
            .insert(handle, 20usize);
        let callback = runtime::AggregateCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_temporal_sum (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_temporal_sum".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_temporal_sum",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        aggregate_handle_table()
            .lock()
            .expect("aggregate handle mutex poisoned")
            .insert(handle, 21usize);
        let callback = runtime::AggregateCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_total (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_total".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_total",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        aggregate_handle_table()
            .lock()
            .expect("aggregate handle mutex poisoned")
            .insert(handle, 22usize);
        let callback = runtime::AggregateCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_variance_agg (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_variance_agg".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_variance_agg",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        aggregate_handle_table()
            .lock()
            .expect("aggregate handle mutex poisoned")
            .insert(handle, 23usize);
        let callback = runtime::AggregateCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tfloat_variance_aggregate (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tfloat_variance_aggregate".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tfloat_variance_aggregate",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        aggregate_handle_table()
            .lock()
            .expect("aggregate handle mutex poisoned")
            .insert(handle, 24usize);
        let callback = runtime::AggregateCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tint_count_agg (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tint_count_agg".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tint_count_agg",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        aggregate_handle_table()
            .lock()
            .expect("aggregate handle mutex poisoned")
            .insert(handle, 25usize);
        let callback = runtime::AggregateCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tint_count_aggregate (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tint_count_aggregate".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tint_count_aggregate",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        aggregate_handle_table()
            .lock()
            .expect("aggregate handle mutex poisoned")
            .insert(handle, 26usize);
        let callback = runtime::AggregateCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tint_max_agg (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tint_max_agg".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tint_max_agg",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        aggregate_handle_table()
            .lock()
            .expect("aggregate handle mutex poisoned")
            .insert(handle, 27usize);
        let callback = runtime::AggregateCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tint_max_value_agg (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tint_max_value_agg".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tint_max_value_agg",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        aggregate_handle_table()
            .lock()
            .expect("aggregate handle mutex poisoned")
            .insert(handle, 28usize);
        let callback = runtime::AggregateCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tint_max_value_aggregate (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tint_max_value_aggregate".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tint_max_value_aggregate",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        aggregate_handle_table()
            .lock()
            .expect("aggregate handle mutex poisoned")
            .insert(handle, 29usize);
        let callback = runtime::AggregateCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tint_merge_agg (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tint_merge_agg".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tint_merge_agg",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        aggregate_handle_table()
            .lock()
            .expect("aggregate handle mutex poisoned")
            .insert(handle, 30usize);
        let callback = runtime::AggregateCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tint_min_agg (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tint_min_agg".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tint_min_agg",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        aggregate_handle_table()
            .lock()
            .expect("aggregate handle mutex poisoned")
            .insert(handle, 31usize);
        let callback = runtime::AggregateCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tint_min_value_agg (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tint_min_value_agg".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tint_min_value_agg",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        aggregate_handle_table()
            .lock()
            .expect("aggregate handle mutex poisoned")
            .insert(handle, 32usize);
        let callback = runtime::AggregateCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tint_min_value_aggregate (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tint_min_value_aggregate".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tint_min_value_aggregate",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        aggregate_handle_table()
            .lock()
            .expect("aggregate handle mutex poisoned")
            .insert(handle, 33usize);
        let callback = runtime::AggregateCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tint_range_agg (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tint_range_agg".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tint_range_agg",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        aggregate_handle_table()
            .lock()
            .expect("aggregate handle mutex poisoned")
            .insert(handle, 34usize);
        let callback = runtime::AggregateCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tint_range_aggregate (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tint_range_aggregate".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tint_range_aggregate",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        aggregate_handle_table()
            .lock()
            .expect("aggregate handle mutex poisoned")
            .insert(handle, 35usize);
        let callback = runtime::AggregateCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tint_sum_agg (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tint_sum_agg".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tint_sum_agg",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        aggregate_handle_table()
            .lock()
            .expect("aggregate handle mutex poisoned")
            .insert(handle, 36usize);
        let callback = runtime::AggregateCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tint_temporal_max (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tint_temporal_max".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tint_temporal_max",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        aggregate_handle_table()
            .lock()
            .expect("aggregate handle mutex poisoned")
            .insert(handle, 37usize);
        let callback = runtime::AggregateCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tint_temporal_min (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tint_temporal_min".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tint_temporal_min",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        aggregate_handle_table()
            .lock()
            .expect("aggregate handle mutex poisoned")
            .insert(handle, 38usize);
        let callback = runtime::AggregateCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tint_temporal_sum (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tint_temporal_sum".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tint_temporal_sum",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        aggregate_handle_table()
            .lock()
            .expect("aggregate handle mutex poisoned")
            .insert(handle, 39usize);
        let callback = runtime::AggregateCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tjsonb_sequences_agg_array (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tjsonb_sequences_agg_array".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tjsonb_sequences_agg_array",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        aggregate_handle_table()
            .lock()
            .expect("aggregate handle mutex poisoned")
            .insert(handle, 40usize);
        let callback = runtime::AggregateCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tjsonb_sequences_agg_concat (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tjsonb_sequences_agg_concat".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tjsonb_sequences_agg_concat",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        aggregate_handle_table()
            .lock()
            .expect("aggregate handle mutex poisoned")
            .insert(handle, 41usize);
        let callback = runtime::AggregateCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tjsonb_sequences_agg_merge_deep (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tjsonb_sequences_agg_merge_deep".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tjsonb_sequences_agg_merge_deep",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        aggregate_handle_table()
            .lock()
            .expect("aggregate handle mutex poisoned")
            .insert(handle, 42usize);
        let callback = runtime::AggregateCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tstz_span_aggregate_union (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tstz_span_aggregate_union".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tstz_span_aggregate_union",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        aggregate_handle_table()
            .lock()
            .expect("aggregate handle mutex poisoned")
            .insert(handle, 43usize);
        let callback = runtime::AggregateCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("tstz_spanset_aggregate_union (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["tstz_spanset_aggregate_union".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "tstz_spanset_aggregate_union",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        aggregate_handle_table()
            .lock()
            .expect("aggregate handle mutex poisoned")
            .insert(handle, 44usize);
        let callback = runtime::AggregateCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("ttext_concat_agg (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["ttext_concat_agg".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "ttext_concat_agg",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        aggregate_handle_table()
            .lock()
            .expect("aggregate handle mutex poisoned")
            .insert(handle, 45usize);
        let callback = runtime::AggregateCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("ttext_concat_aggregate (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["ttext_concat_aggregate".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "ttext_concat_aggregate",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        aggregate_handle_table()
            .lock()
            .expect("aggregate handle mutex poisoned")
            .insert(handle, 46usize);
        let callback = runtime::AggregateCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("ttext_merge_agg (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["ttext_merge_agg".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "ttext_merge_agg",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    Ok(())
}

fn register_tables() -> Result<(), Duckerror> {
    let Some(capability) = runtime::get_capability(Capabilitykind::Table) else {
        if true {
            eprintln!("[dynlink-emit] host did not expose table capability; skipping 4 table-function(s)");
        }
        return Ok(());
    };
    let registry = match capability {
        runtime::Capability::Table(r) => r,
        _ => {
            return Err(Duckerror::Internal(
                "table capability returned unexpected variant".into(),
            ));
        }
    };
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        table_handle_table()
            .lock()
            .expect("table handle mutex poisoned")
            .insert(handle, 0usize);
        let callback = runtime::TableCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Int64 },
            ];
        let columns: Vec<runtime::Columndef> = vec![runtime::Columndef {
            name: "result".into(),
            logical: Logicaltype::Blob,
        }];
        registry.register(
            "tfloat_time_split",
            &args,
            &columns,
            callback,
            None,
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        table_handle_table()
            .lock()
            .expect("table handle mutex poisoned")
            .insert(handle, 1usize);
        let callback = runtime::TableCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Float64 },
            ];
        let columns: Vec<runtime::Columndef> = vec![runtime::Columndef {
            name: "result".into(),
            logical: Logicaltype::Blob,
        }];
        registry.register(
            "tfloat_value_split",
            &args,
            &columns,
            callback,
            None,
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        table_handle_table()
            .lock()
            .expect("table handle mutex poisoned")
            .insert(handle, 2usize);
        let callback = runtime::TableCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Int64 },
            ];
        let columns: Vec<runtime::Columndef> = vec![runtime::Columndef {
            name: "result".into(),
            logical: Logicaltype::Blob,
        }];
        registry.register(
            "tint_time_split",
            &args,
            &columns,
            callback,
            None,
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        table_handle_table()
            .lock()
            .expect("table handle mutex poisoned")
            .insert(handle, 3usize);
        let callback = runtime::TableCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Int64 },
            ];
        let columns: Vec<runtime::Columndef> = vec![runtime::Columndef {
            name: "result".into(),
            logical: Logicaltype::Blob,
        }];
        registry.register(
            "tint_value_split",
            &args,
            &columns,
            callback,
            None,
        )?;
    }
    Ok(())
}

// -----------------------------------------------------------
// Guest impls.
// -----------------------------------------------------------

struct Component;

impl GuestGuest for Component {
    fn load() -> Result<Loadresult, Duckerror> {
        register_scalars()?;
        register_aggregates()?;
        register_tables()?;
        Ok(Loadresult {
            name: EXTENSION_ROOT.to_string(),
            version: Some(CATALOG_VERSION.to_string()),
            requires: vec![Capabilitykind::Scalar],
        })
    }
    fn reconfigure(_keys: Vec<String>) -> Result<bool, Duckerror> { Ok(false) }
    fn shutdown() -> Result<bool, Duckerror> { Ok(false) }
}

fn dispatch_call_scalar(
    handle: u32,
    args: Vec<Duckvalue>,
) -> Result<Duckvalue, Duckerror> {
    let arm_idx = handle_table()
        .lock()
        .expect("scalar handle mutex poisoned")
        .get(&handle)
        .copied()
        .ok_or_else(|| Duckerror::Internal(format!("unknown scalar handle {}", handle)))?;
    let name = scalar_name_by_arm_idx(arm_idx)
        .ok_or_else(|| Duckerror::Internal(format!("unknown scalar arm {}", arm_idx)))?;
    // Null-propagation: any NULL argument short-circuits to NULL.
    if args.iter().any(|v| matches!(v, Duckvalue::Null)) {
        return Ok(Duckvalue::Null);
    }
    // Alias translation: SQL name → WIT-canonical → provider method
    // (kebab-case). Compact-form SQL names (e.g. `st_geomfromtext`)
    // route through `canonical_for` to their long form
    // (`st_geom_from_text`) before kebab-casing; non-alias names
    // pass through unchanged. See `canonical_for` above.
    let canonical = canonical_for(name);
    let method = canonical.replace('_', "-");
    let cbor_args: Vec<CborValue> = args.iter().map(duckv_to_cbor).collect();
    let resp = call(&method, cbor_args)?;
    Ok(response_to_duckv(resp))
}

fn aggregate_name_by_arm_idx(arm: usize) -> Option<&'static str> {
    match arm as u32 {
        0 => Some("date_span_aggregate_union"),
        1 => Some("date_spanset_aggregate_union"),
        2 => Some("float_span_aggregate_union"),
        3 => Some("float_spanset_aggregate_union"),
        4 => Some("int_span_aggregate_union"),
        5 => Some("int_spanset_aggregate_union"),
        6 => Some("tbigint_count_aggregate"),
        7 => Some("tbigint_max_value_aggregate"),
        8 => Some("tbigint_merge_agg"),
        9 => Some("tbigint_min_value_aggregate"),
        10 => Some("tbigint_range_aggregate"),
        11 => Some("tbigint_sum_aggregate"),
        12 => Some("tbool_merge_agg"),
        13 => Some("tbool_temporal_and"),
        14 => Some("tbool_temporal_or"),
        15 => Some("tfloat_merge_agg"),
        16 => Some("tfloat_temporal_avg"),
        17 => Some("tfloat_temporal_count"),
        18 => Some("tfloat_temporal_max"),
        19 => Some("tfloat_temporal_min"),
        20 => Some("tfloat_temporal_sum"),
        21 => Some("tfloat_total"),
        22 => Some("tfloat_variance_agg"),
        23 => Some("tfloat_variance_aggregate"),
        24 => Some("tint_count_agg"),
        25 => Some("tint_count_aggregate"),
        26 => Some("tint_max_agg"),
        27 => Some("tint_max_value_agg"),
        28 => Some("tint_max_value_aggregate"),
        29 => Some("tint_merge_agg"),
        30 => Some("tint_min_agg"),
        31 => Some("tint_min_value_agg"),
        32 => Some("tint_min_value_aggregate"),
        33 => Some("tint_range_agg"),
        34 => Some("tint_range_aggregate"),
        35 => Some("tint_sum_agg"),
        36 => Some("tint_temporal_max"),
        37 => Some("tint_temporal_min"),
        38 => Some("tint_temporal_sum"),
        39 => Some("tjsonb_sequences_agg_array"),
        40 => Some("tjsonb_sequences_agg_concat"),
        41 => Some("tjsonb_sequences_agg_merge_deep"),
        42 => Some("tstz_span_aggregate_union"),
        43 => Some("tstz_spanset_aggregate_union"),
        44 => Some("ttext_concat_agg"),
        45 => Some("ttext_concat_aggregate"),
        46 => Some("ttext_merge_agg"),
        _ => None,
    }
}

fn table_name_by_arm_idx(arm: usize) -> Option<&'static str> {
    match arm as u32 {
        0 => Some("tfloat_time_split"),
        1 => Some("tfloat_value_split"),
        2 => Some("tint_time_split"),
        3 => Some("tint_value_split"),
        _ => None,
    }
}

/// Aggregate dispatch (Phase 9.3.next). Whole-group call: the host
/// hands us every row's arg columns; we lift each row to a CBOR list
/// and ship the accumulated list-of-lists to the provider under the
/// aggregate's canonical name. The provider owns the semantic
/// fold (average, extent, cluster, …); the bridge is a CBOR tunnel.
fn dispatch_call_aggregate(
    handle: u32,
    args: Vec<bindings::duckdb::extension::column_types::Colvec>,
) -> Result<Duckvalue, Duckerror> {
    let arm_idx = aggregate_handle_table()
        .lock()
        .expect("aggregate handle mutex poisoned")
        .get(&handle)
        .copied()
        .ok_or_else(|| Duckerror::Internal(format!("unknown aggregate handle {}", handle)))?;
    let name = aggregate_name_by_arm_idx(arm_idx)
        .ok_or_else(|| Duckerror::Internal(format!("unknown aggregate arm {}", arm_idx)))?;
    let n_rows = validate_colvec_rows(&args)?;
    let n_args = args.len();
    // Columnar wire shape for aggregate dispatch: each column becomes a
    // separate positional arg to the provider, containing a FLAT list
    // of that column's per-row CBOR values. For a single-arg aggregate
    // like `st_extent(geom)`, the provider sees `args=[List<Bytes>]` —
    // arg[0] is the flat geometry list, arg[0][0] is a Bytes geometry
    // (matches the shim's expected shape per the arg-shape checker's
    // error message on prior versions of this dispatch).
    //
    // Previous wire (broken) was `args=[List<List<CborValue>>]` — one
    // arg containing a list of per-row lists. The shim's arg-type
    // checker on `st-extent` etc. read `arg[0][0]` as List (a row) and
    // failed shape mismatch (expected Bytes for the geometry).
    //
    // SQL-aggregate NULL-row semantics: skip rows where ANY arg is
    // NULL (matches sum() / avg() defaults) — do the skip BEFORE
    // transposing to columns so all columns see the same row subset.
    let mut cols: Vec<Vec<CborValue>> = (0..n_args)
        .map(|_| Vec::with_capacity(n_rows))
        .collect();
    let mut row_buf: Vec<Duckvalue> = Vec::with_capacity(n_args);
    for i in 0..n_rows {
        materialize_row(&args, i, &mut row_buf);
        if row_buf.iter().any(|v| matches!(v, Duckvalue::Null)) {
            continue;
        }
        for (col_idx, v) in row_buf.iter().enumerate() {
            cols[col_idx].push(duckv_to_cbor(v));
        }
    }
    // Empty non-null row set → aggregate is NULL (matches SQL
    // `SELECT SUM(x) FROM t WHERE 1=0` = NULL). Skip the provider
    // call so the shim doesn't need to special-case empty inputs.
    if cols.first().map_or(true, |c| c.is_empty()) {
        return Ok(Duckvalue::Null);
    }
    let canonical = canonical_for(name);
    let method = canonical.replace('_', "-");
    let cbor_args: Vec<CborValue> = cols.into_iter().map(CborValue::List).collect();
    let resp = call(&method, cbor_args)?;
    Ok(response_to_duckv(resp))
}

/// Table-function dispatch (Phase 9.3.next). Single-shot call: the
/// host hands us the argv values; we ship them through the CBOR
/// envelope and expect a List-of-Bytes (one BLOB per row) response.
/// Any other shape is best-effort wrapped as a single Blob row so
/// the provider stays free to evolve its wire without breaking the
/// bridge.
fn dispatch_call_table(
    handle: u32,
    args: Vec<Duckvalue>,
) -> Result<Resultset, Duckerror> {
    let arm_idx = table_handle_table()
        .lock()
        .expect("table handle mutex poisoned")
        .get(&handle)
        .copied()
        .ok_or_else(|| Duckerror::Internal(format!("unknown table handle {}", handle)))?;
    let name = table_name_by_arm_idx(arm_idx)
        .ok_or_else(|| Duckerror::Internal(format!("unknown table arm {}", arm_idx)))?;
    let canonical = canonical_for(name);
    let method = canonical.replace('_', "-");
    let cbor_args: Vec<CborValue> = args.iter().map(duckv_to_cbor).collect();
    let resp = call(&method, cbor_args)?;
    let rows: Vec<Vec<Duckvalue>> = match resp {
        ResponseValue::List(items) => items
            .into_iter()
            .map(|item| match item {
                ResponseValue::Bytes(b) => vec![Duckvalue::Blob(b)],
                ResponseValue::List(cols) => cols.into_iter().map(response_to_duckv).collect(),
                other => vec![response_to_duckv(other)],
            })
            .collect(),
        ResponseValue::Null => Vec::new(),
        other => vec![vec![response_to_duckv(other)]],
    };
    Ok(rows)
}

impl CallbackGuest for Component {
    fn call_scalar(
        handle: u32,
        args: Vec<Duckvalue>,
        _ctx: Invokeinfo,
    ) -> Result<Duckvalue, Duckerror> {
        dispatch_call_scalar(handle, args)
    }

    fn call_scalar_batch_col(
        handle: u32,
        args: Vec<bindings::duckdb::extension::column_types::Colvec>,
        ctx: Invokeinfo,
    ) -> Result<bindings::duckdb::extension::column_types::Colvec, Duckerror> {
        // Columnar HOT path: convert per-row, delegate to the cold
        // row-major dispatch, then rebuild a colvec. Mirrors the
        // postgis-ducklink-bridge (~/git/postgis-ducklink-bridge/
        // src/lib.rs:2231-2261) reference discipline.
        let n_rows = validate_colvec_rows(&args)?;
        let n_args = args.len();
        let base = ctx.rowindex.unwrap_or(0);
        let mut out: Vec<Duckvalue> = Vec::with_capacity(n_rows);
        let mut row_buf: Vec<Duckvalue> = Vec::with_capacity(n_args);
        for i in 0..n_rows {
            materialize_row(&args, i, &mut row_buf);
            let _row_ctx = Invokeinfo {
                rowindex: Some(base + i as u64),
                iswindow: ctx.iswindow,
            };
            let row_args = core::mem::take(&mut row_buf);
            out.push(dispatch_call_scalar(handle, row_args)?);
        }
        values_to_colvec(out)
    }

    fn call_aggregate_col(
        handle: u32,
        args: Vec<bindings::duckdb::extension::column_types::Colvec>,
    ) -> Result<Duckvalue, Duckerror> {
        dispatch_call_aggregate(handle, args)
    }

    fn call_cast_col(
        handle: u32,
        arg: bindings::duckdb::extension::column_types::Colvec,
    ) -> Result<bindings::duckdb::extension::column_types::Colvec, Duckerror> {
        // Identity casts (GEOMETRY↔BLOB / BOX2D↔BLOB / ...) are
        // physical-representation-preserving: the source and target
        // are both physically BLOB, only the logical tag differs.
        // Return the input column unchanged.
        if identity_cast_handles()
            .lock()
            .expect("identity cast handles mutex poisoned")
            .contains(&handle)
        {
            return Ok(arg);
        }
        Err(Duckerror::Internal("call_cast_col: handle not identity (Phase A)".to_string()))
    }

    fn call_table(handle: u32, args: Vec<Duckvalue>) -> Result<Resultset, Duckerror> {
        dispatch_call_table(handle, args)
    }

    fn call_pragma(_handle: u32, _args: Vec<Duckvalue>) -> Result<Option<Duckvalue>, Duckerror> {
        Err(Duckerror::Internal("call_pragma: unsupported (Phase A)".to_string()))
    }

    fn call_cast(handle: u32, value: Duckvalue) -> Result<Duckvalue, Duckerror> {
        let _ = (handle, value);
        Err(Duckerror::Internal("call_cast: unsupported (Phase A)".to_string()))
    }
}

bindings::export!(Component with_types_in bindings);
