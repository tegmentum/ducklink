# Generated by `npm install`. Rebuild via `make mosaic-browser`.
node_modules/
package-lock.json

# NOTE: `dist/` IS committed. bundle.js (~800 KB) + index.html are
# `include_bytes!`'d by the wasm shim, so a plain `cargo component
# build -p mosaic-component` MUST see them. `make mosaic-browser`
# regenerates them (bumped esbuild / vgplot etc.) — commit the
# updated dist/ alongside the JS-source change.
