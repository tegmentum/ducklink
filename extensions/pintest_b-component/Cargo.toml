[package]
name = "pintest-b-component"
version = "0.1.0"
edition = "2021"
description = """
Pin-test probe B: registers pin_probe() -> INTEGER returning 222.
"""
license = "MIT"
publish = false

[lib]
crate-type = ["cdylib"]

[dependencies]
wit-bindgen = "0.41"
wit-bindgen-rt = "0.41"
# major-4 columnar dispatch bridge (colvec <-> row adapter) for the
# hand-written per-row scalar.
datalink-extcore = { path = "../../../datalink/crates/datalink-extcore" }

[package.metadata.component]
package = "duckdb:extension"

[package.metadata.component.target]
path = "./wit"
world = "duckdb:extension/duckdb-extension"
