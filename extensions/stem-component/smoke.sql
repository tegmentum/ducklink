-- stem extension smoke (Porter/Snowball English).
SELECT stem('running', 'english') AS run;
SELECT stem('happily', 'english') AS happi;
SELECT stem('argument', 'english') AS argument;
SELECT stem('continuation', 'english') AS continu;
