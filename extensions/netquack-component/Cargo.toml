[package]
name = "netquack-component"
version = "0.1.0"
edition = "2021"
description = """
Public-suffix-aware URL/domain parsing (registrable domain / eTLD+1)
"""
license = "MIT"
publish = false

[lib]
crate-type = ["cdylib"]

[dependencies]
wit-bindgen = "0.41"
wit-bindgen-rt = "0.41"

# Shared, DB-agnostic logic + capability declaration (datalink pull-up).
netquack-core = { path = "../../../datalink/extensions/netquack-core" }
datalink-extcore = { path = "../../../datalink/crates/datalink-extcore" }

[package.metadata.component]
package = "duckdb:extension"

[package.metadata.component.target]
path = "./wit"
world = "duckdb:extension/duckdb-extension"
